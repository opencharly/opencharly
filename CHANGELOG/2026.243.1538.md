# 2026.243.1538 — chore: pin all 31 missing opencharly org repos as submodules + fix policy-b gate for linked worktrees

## Summary

Completes the org map: **all 31 remaining non-archived `opencharly` org repos are now pinned as flat submodules** (path == repo name) at their default-branch (`main`) HEAD.

- 31 new gitlinks, per category: `layer-charly-*` candy repos (8: charly, build, check, image, immich, internals, internals-extra, kubernetes), `layer-omarchy-*` distro repos (13: base, boot, desktop-apps, dev, fonts, gpu-nvidia, hardware, hyprland, media, network, shell, terminal, themes), check/fixture layers (5: check-local-layer, check-stack-layer, examplestep-consumer, examplestep-deploy-consumer, stepkind-build-consumer), plugins (4: boot-kind, desktop-kind, quickshell, unit), pod-charly-mcp (1).
- Submodule count: 358 → **389**.
- Every new entry carries `branch = main` (its repo default — resolved via `git ls-remote --symref`).
- Deliberately excluded (documented policy, unchanged): `.github` (covered by the `dotgithub` submodule path so this repo keeps its own `.github/` for CI), `opencharly` (the umbrella itself), `heroic-heroic` (junk repo from the batch-2.7 scaffold word-splitting bug — operator deletion pending, scratchpad).

**Gate fix (same commit):** `scripts/check-policy-b.sh` ran `git -C charly ls-tree` which git's hook env `GIT_DIR` override (exported to hooks in linked worktrees) hijacked — the nested call resolved `box/*` against the umbrella gitdir instead of charly's own, failing the pre-commit gate on every worktree-based commit. The nested call now clears `GIT_DIR`/`GIT_WORK_TREE` (env -u, call-local only). Verified: the pre-commit hook now passes under an exported `GIT_DIR`+`GIT_INDEX_FILE`.

## How tested

1. **Pre-commit gate (hook env simulated):**
```
$ cd /tmp/umbrella-org-coverage && GIT_DIR=$(git rev-parse --absolute-git-dir) GIT_INDEX_FILE=$(git rev-parse --git-path index) bash scripts/check-policy-b.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
$ GIT_DIR=$(git rev-parse --absolute-git-dir) GIT_INDEX_FILE=$(git rev-parse --git-path index) bash scripts/check-harness-parity.sh
check-harness-parity: OK — 5 shared harness files identical to charly/
```
2. **Real commit through the hook:** `git commit` ran pre-commit's policy-B + harness-parity assertions and passed (the fixed gate).
3. **Full pinning audit (R10) on the committed tree:**
```
$ cd /tmp/umbrella-org-coverage && task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 389 submodules, policy B holds
```
Commit f581725, `git status --short` clean afterwards.

## Rulebook compliance

- R1/RCA: gate failure in linked worktrees root-caused to git's hook-env `GIT_DIR` export, not papered over; fix is call-local, not a workflow change.
- R3: no duplication — reuses `scripts/verify-pins.sh`/policy-B machinery; fix is one nested invocation.
- R4: no workarounds — no `--no-verify`, no hooksPath bypass, no force-push.
- R6: git-safety — branch off fresh `origin/main`, no destructive ops, no direct push to main.
- R7/R10: full `task verify` pasted above on the final committed tree.
- Policy (README): pin only merged refs (every gitlink is a default-branch HEAD), `branch =` matches each repo's real default (`main`), path == repo name.

## Change classification

- Class: gitlinks + one gate-script fix (config/scripting; no candy/schema/Go changes).
- Verification gate: `task verify` full pinning audit (389 submodules, policy B holds) + pre-commit gate run live.
- Attribution: `fully tested and validated` — verify-pins passed on the final committed tree and the changed gate path executed live under the failing conditions.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*