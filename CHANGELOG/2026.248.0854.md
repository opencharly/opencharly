# 2026.248.0854 — docs: add the 5 new repos to the org map (plugin-cache, crabbox family, plugin-openclaw)

## Summary

Add the 5 new org repos to the README org map (they were in the umbrella's .gitmodules but missing from the tables):

- `plugin-cache/` (Plugins) — the `charly cache status|clear|refresh|bypass` command plugin
- `plugin-crabbox/` (Plugins)
- `plugin-openclaw/` (Plugins)
- `pod-crabbox/` (Pods)
- `layer-crabbox/` (Other layers)

Table counts updated: Plugins 103→106, Pods 73→74, Other layers 137→138. The README now covers all 399 submodules (399/399 verified).

## How tested

```
$ grep -c '| `' README.md
399   # == the 399 submodules in .gitmodules
$ while read m; do grep -q "$m" README.md || echo "MISSING: $m"; done < <(grep '^\[submodule' .gitmodules | sed 's/\[submodule "\("\)\"\]/\1/')
# (no output — all covered)
```

## Rulebook compliance

- R3: the org map is the single source for repo→role; the README rows mirror the .gitmodules exactly.
- R6: git-safe.

Change classification: docs. Tier: documentation reviewed.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (documentation reviewed)*