# 2026.242.2134 — chore(sync): land the mise follow-up (plugin-mise + gate fix)

## Summary

Gitlink sync landing the **mise follow-up work** into the umbrella (policy B):

- **`plugin-mise`** → `70469ce` — the shared `sdk/kit.BuilderResolve` render (R3), per-distro R10 check beds (`check-mise-*`, one per supported distro incl. Alpine), the `curl-tar` + `mise-consumer` candies, and the libc-agnostic detection fixture (zig/go). opencharly/plugin-mise#2 (validator PASS, 7/7 disposable fresh-rebuild beds — output below).
- **`dotgithub`** → `6eace9f` — the org validator's resilient auto-merge arming (opencharly/.github#81 + #82): a PASS verdict can no longer be turned red by the arming step's errexit/race; multi-check repos (this PR's own gate is one) arm through bounded retries/update-branch.
- **`plan/mise-followup-todo.md`** — the follow-up plan + progress addendum (RCA trail).

charly (7db4f988), sdk (f97e460) and spec (443b9f5) gitlinks were already at their synced values (landed via #63) and need no change.

## How tested

```
$ task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 349 submodules, policy B holds
```
(exit 0)

```
$ task sync   # the pin source itself
  plugin-mise -> 70469ce
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'
```

R10 of the landed plugin-mise content (fresh disposable rebuilds from its final committed tree):

```
alpine:   charly check run check-mise-alpine-pod: PASS (steps=13)
arch:     charly check run check-mise-arch-pod: PASS (steps=13)
cachyos:  charly check run check-mise-cachyos-pod: PASS (steps=13)
omarchy:  charly check run check-mise-omarchy-pod: PASS (steps=13)
debian:   charly check run check-mise-debian-pod: PASS (steps=13)
fedora:   charly check run check-mise-fedora-pod: PASS (steps=13)
ubuntu:   charly check run check-mise-ubuntu-pod: PASS (steps=13)
```

## Rulebook compliance

- **Policy B** — every gitlink equals the owning default branch / charly's gitlinks (`check-policy-b: OK` above); no hand-pins.
- **R5** — the sync is the cutover: charly/sdk/spec needed no change (already recorded); the umbrella follows the landing points.
- **R6** — feat/ branch, PR-only, no force-push.
- **R7** — `task verify` output pasted above (349 submodules, policy B holds).
- **R10** — verified from the committed tree staged here.

## Change classification

- **Change class:** gitlink sync (pointer updates + plan doc)
- **Verification gate:** `task verify` pasted; upstream PRs' own validator PASSes
- **Attribution tier:** fully tested and validated

*Assisted-by: pi deepseek/deepseek-v4-flash-0731 (fully tested and validated)*