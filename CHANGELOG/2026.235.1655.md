# 2026.235.1655 — chore: add the ripgrep candy repo as a submodule (the candy de-submodule cutover)

## Summary

The candy de-submodule cutover's **first standalone candy repo**: `opencharly/ripgrep` (the ripgrep candy moved out of `charly/candy/`) is now an umbrella submodule, following its own default-branch HEAD like every other org repo (the every-other-repo rule). The charly-side reference rework (the `add_candy:` ref + the `candy/ripgrep` deletion) lands in the charly PR; this PR is the umbrella's checkout view.

## How tested

**The umbrella gate on the final tree (pasted):**

```
$ task verify
verify-pins: OK — 24 submodules, policy B holds
OK: /home/atrawog/Sync/Atrapub/coder/pi/opencharly/.pi/extensions/umbrella-gates.ts parses (node type-strip pass)
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface touched |
| R1 | N/A — no surfaced failure |
| R2 | N/A — no surfaced issue parked |
| R3 | the sync scripts remain the single pin mechanism |
| R4 | N/A — the submodule follows its own default-branch HEAD (the every-other-repo rule) |
| R5 | N/A — the legacy `candy/ripgrep` deletion is the charly PR's |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | the verify output pasted above |
| R8 | N/A — no generation changes |
| R9 | N/A — no deploy target change |
| R10 | the verify ran on the final committed tree |

## Change classification

- **Change class**: submodule add (the first standalone candy repo)
- **Verification gate**: `task verify` (24 submodules, policy B holds)
- **Attribution tier**: analysed on a live system — the verify executed live with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*