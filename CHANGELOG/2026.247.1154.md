# 2026.247.1154 — sync: crabbox wave gitlinks (layer/plugin/pod-crabbox + advanced repos)

## Summary

Phase-5 umbrella gitlink sync for the Crabbox wave. Three new submodules recorded (each at its default-branch HEAD):

- `layer-crabbox` → `85e63114`
- `plugin-crabbox` → `ccb30da4`
- `pod-crabbox` → `8c452679`

Advanced gitlinks via `task sync` (no hand-pins):

- `marketplace` → `b8f87742` (was `515ac4fc`)
- `pod-charly-automation` → `352289a9` (was `b71b7e3a`)
- `pod-charly-hooks` → `93ad746c` (was `c344b1ce`)
- `charly` → `f21430c9` (unchanged; already at main HEAD)
- `distro-cachyos` → `677dbbe0` (unchanged; policy B — equals charly's `box/cachyos` gitlink exactly; charly main has not yet re-vendored `box/cachyos` to distro's post-#62 main head `b792a365`, so the umbrella correctly mirrors charly's pin; NOT hand-pinned per rule 6)

## How tested

```
$ task sync
== charly (default-branch HEAD) ==
  charly -> f21430c9
== charly-pinned repos (exactly charly's gitlinks) ==
  distro-cachyos -> 677dbbe
== everything else (own default-branch HEAD) ==
  marketplace -> b8f8774
  pod-charly-hooks -> 93ad746
  pod-charly-automation -> 352289a
  layer-crabbox -> 85e6311
  plugin-crabbox -> ccb30da
  pod-crabbox -> 8c45267
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'
```

Gitlink-vs-main-HEAD equality check (`git ls-tree HEAD <path>` vs `gh api repos/opencharly/<repo>/commits/main --jq .sha`, first 12 chars) — all match except distro-cachyos, which is policy-B-bound (see Summary):

```
layer-crabbox  85e63114 == main 85e63114  ✓
plugin-crabbox ccb30da4 == main ccb30da4  ✓
pod-crabbox    8c452679 == main 8c452679  ✓
marketplace    b8f87742 == main b8f87742  ✓
pod-charly-automation 352289a9 == main 352289a9  ✓
pod-charly-hooks     93ad746c == main 93ad746c  ✓
charly         f21430c9 == main f21430c9  ✓
distro-cachyos 677dbbe0 != main b792a365  (expected — policy B: umbrella == charly's box/cachyos gitlink, which charly has not bumped yet)
```

```
$ task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 398 submodules, policy B holds
VERIFY_EXIT=0
```

Commits: `10e0ff0 sync: crabbox wave gitlinks — layer/plugin/pod-crabbox + advanced repos` (pre-commit gate passed: check-policy-b OK, check-harness-parity OK).

## Rulebook compliance

- R0 Skills first — N/A: pure gitlink sync; no skill surface touched (git-workflow discipline applied: worktree off origin/main, no edits in main worktree).
- R1 RCA every anomaly — applied: the sole gitlink-vs-main mismatch (distro-cachyos) was root-caused to policy B (umbrella mirrors charly's box/cachyos pin), not a sync failure.
- R2 — N/A (not part of the umbrella-scaled rulebook; umbrella AGENTS.md engineering rules list R0-R1, R3-R7, R10).
- R3 No duplication — N/A: no new implementation; sync via `task sync` (single canonical sync-gitlinks.sh / policy-b.sh), no hand-pinning.
- R4 No workarounds — no sleep/retry/hand-pins; all pins produced by `task sync`.
- R5 Delete legacy — N/A.
- R6 Git safety — worktree off origin/main only; main worktree untouched (verified read-only branch/status check); no force-push, no hook bypass (pre-commit gate ran and passed); PR-only landing.
- R7 Prove the gate — `task verify` ran on the final committed tree, output pasted above (verify-pins OK, policy B holds).
- R8/R9 — N/A (not part of the umbrella-scaled rulebook).
- R10 Fresh disposable proof — verify ran in the dedicated `umb-work-crabbox` worktree from the final committed tree (`10e0ff0`), not an edited state.

## Change classification

- Change class: gitlink sync (policy B + default-branch HEAD bumps + 3 new submodule records).
- Verification gate: `task verify` full pinning gate passed on the final tree (VERIFY_EXIT=0).
- Attribution tier: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*