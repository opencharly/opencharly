# 2026.245.2029 — feat: add plugin-herdr, plugin-omarchy, pod-herdr as submodules

## Summary

Three active org repos were missing from the umbrella: **plugin-herdr**, **plugin-omarchy**, **pod-herdr** — all created 2026-09-01/02 and not yet added as submodules.

- `plugin-herdr` — charly plugin: command:herdr + verb:herdr (Herdr terminal-multiplexer control over its NDJSON socket API)
- `plugin-omarchy` — charly plugin: the omarchy CLI surface as a check verb
- `pod-herdr` — the herdr stack box (herdr.dev terminal multiplexer) + the check-herdr-pod R10 bed

Added as flat root submodules (path == repo name, per umbrella convention) with `branch = main` in .gitmodules, and listed in the README org map: Plugins 101→103, Pods 72→73, total 391→394.

## How tested

- `git submodule add` for each repo — cloned from `opencharly/<repo>` over HTTPS, default branch `main` for all three (verified via `git ls-remote --symref` in the gate below).
- README org map row count (394) == `.gitmodules` submodule count (394).
- **`task verify` (scripts/verify-pins.sh)** on the final tree — the full pinning gate (every pin, incl. the remote-branch audit):

```
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 394 submodules, policy B holds

```

## Rulebook compliance

- **R6:** no force-push, no direct push to main; PR-only landing.
- **Policy B:** no `distro-*` pins touched; the three new pins are to their own default-branch HEADs.

## Change classification

- **Change class:** submodule additions + README org map (docs).
- **Breaking change + rollback:** none — a revert removes the three submodule entries.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*