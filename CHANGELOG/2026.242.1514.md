# 2026.242.1514 — feat: record layer-punktfunk-client as a submodule

## Summary

Records `opencharly/layer-punktfunk-client` (tagged `v2026.242.1210`) as a submodule. The umbrella is a view of the org, so a new repo gets a gitlink.

It is the other half of `layer-punktfunk`: the host candy proves punktfunk installs and answers; this one is what a second system pairs **from**, which no single-node bed could exercise.

## Policy B

Bucket 3 — not a `distro-*`, so it pins its own default-branch HEAD rather than tracking charly's gitlinks. `scripts/lib/policy-b.sh` needs no edit: the bucketing is by name and this falls in the default bucket. The gate confirms that rather than my asserting it.

## How tested

```
$ task verify
verify-pins: OK — 349 submodules, policy B holds
OK: .pi/extensions/umbrella-gates.ts parses (node type-strip pass)
task verify exit=0
```

Policy B holds across the whole set with the new entry in place.

**Conflict resolution.** `main` moved under this branch — another change added `plugin-mise`, and both appended their `[submodule]` block at the same point in `.gitmodules`. Both entries belong, so both are kept, each with its own `branch = main`. Merging also advanced `charly`, `sdk` and `spec` gitlinks, so the checkouts were re-synced (`git submodule update --init --recursive`) before re-running the gate — the first two verify attempts failed on exactly that drift (`checked-out HEAD … != gitlink …`) and are why the output above is from the merged tree rather than the original commit. The submodule was added with `git submodule add -b main`, matching the `branch = main` the sibling `layer-punktfunk` entry carries, so `task sync` treats it the same way.

## Rulebook compliance

- **R2 (no deferral)** — the plan's landing order puts the umbrella entry last; this is that step, not a follow-up.
- **R4 (no workarounds)** — a real submodule at a real tag; no vendored copy.
- **R5 (pin discipline)** — pins the merged default branch, never a PR branch.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass; `git status` clean before the change.
- **R7 (prove the gate)** — `task verify` output pasted above, run on the final tree.

## Change classification

- **Change class:** umbrella gitlink (`.gitmodules` + one gitlink; no code)
- **Verification gate:** `task verify` on the committed tree
- **Attribution tier:** `fully tested and validated` — the pinning gate passed on the final tree with the new submodule in place

*Assisted-by: Claude Code Anthropic Opus 5 (fully tested and validated)*