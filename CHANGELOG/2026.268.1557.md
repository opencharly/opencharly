# 2026.268.1557 — docs(task): rewire the umbrella Taskfile-era task references

## Summary

Sweep the last stale references to the former umbrella Taskfile task names that
the P4 `charly task` cutover (#247) left in the umbrella.

- `.opencode/instructions.md` and `.pi/APPEND_SYSTEM.md` name `charly task verify`
  instead of the deleted `task verify`;
- `scripts/sync-dispatcher.sh`'s stale-dispatcher failure hint names
  `charly task skills` (an executable script's error string).

`PLAN-mise.md` carries the same `task cue:gen` / `task verify` residue but was
authored by another session. Per AGENTS.md rule 9 it is NOT edited here; the
residue is routed to its named owner — umbrella issue #254 — for that owner to
sweep.

## How tested

The changed path is `scripts/sync-dispatcher.sh`'s stale-dispatcher branch. It
was executed live; the branch emits the new hint:

```
$ # stale target + fragment, --check path
FAIL: dispatcher is stale — the pinned marketplace carries a different table. Run `charly task skills` and commit the result.
(exit 1)
```

The script self-test and the full pinning gate both pass on the final tree:

```
$ bash scripts/sync-dispatcher.sh --self-test
sync-dispatcher: self-test OK (splice writes, steady-state passes, stale fails)

$ ./charly/bin/charly task verify
task verify: 3 step(s), 0 failed
  [pass] check — policy B — every distro-* gitlink equals charly's own box/* gitlink
  [pass] check — every submodule is clean and checked out at its recorded gitlink, and its branch equals the remote default
  [pass] check — the opencode gate plugin contract (the check-opencode-plugin gate the former verify-pins.sh ran)
```

The umbrella pre-commit gate (`hooks/pre-commit`) ran the task surface green on
this commit (no `--no-verify`):

```
task policy-b: 1 step(s), 0 failed
task harness: 1 step(s), 0 failed
task self-test: 1 step(s), 0 failed
check-opencode-plugin: OK
pre-commit: OK
```

The claim-keyed sweep covers all umbrella-NATIVE tracked files; `charly/` is a
SUBMODULE gitlink here (its own references are swept in charly#661), and
`PLAN-mise.md` is the named-batch exception above:

```
$ grep -rnE '\btask (sync|verify|map|harness|skills|hooks|self-test|policy-b)\b' $(git ls-files | grep -vE '^CHANGELOG/|^PLAN-mise.md') | grep -v 'charly task'
(empty)
```

## Rulebook compliance

- **R5 (delete legacy completely):** the removed task names are swept from the
  umbrella-native files; only `CHANGELOG/` keeps them as history, plus
  `PLAN-mise.md` which is routed to its named owning batch.
- **R2 (no parked residue):** `PLAN-mise.md`'s residue is routed to its named
  owner, umbrella issue #254, not left unowned.
- **Rule 9 (session-scoped ownership):** `PLAN-mise.md` is another session's file
  and is deliberately NOT touched.
- **R6 (no hook bypass):** the commit ran through the real `hooks/pre-commit`.
- **Rule 2 / umbrella view:** `charly/` is a submodule (gitlink), so the sweep
  legitimately excludes it — its references belong to the charly repo's PR.

## Change classification

- **Change class:** maintenance-script error string + documentation prose (not
  documentation-only: `scripts/sync-dispatcher.sh` is an executable).
- **Verification gate:** the changed dispatcher branch executed live; the script
  self-test, the umbrella pre-commit gate, and the full `charly task verify` all
  passed on the final tree.
- **Attribution tier:** `fully tested and validated`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*