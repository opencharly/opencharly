# 2026.254.1823 — chore(umbrella): sync the eval-omarchy gitlink to the V4.1 lane pin HEAD

## Summary

Umbrella gitlink sync (policy B): `opencharly/eval-omarchy` merged PR #70 —
the lane's plugin-pipeline pin now carries the DeepSeek V4.1 default
(`v2026.254.1528`, tag v2026.254.1808). The umbrella pins it at its new
default-branch HEAD.

```
$ git diff origin/main...HEAD --stat
 eval-omarchy | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

The committed range between the old and new pins (verbatim):

```
$ git -C eval-omarchy log --oneline 241995ab..f70f7587
f70f758 docs: add CHANGELOG 2026.254.1808 for PR #70
09b02c4 chore(lane): pin plugin-pipeline v2026.254.1528 (deepseek-v4.1-flash default) (#70)
```

## How tested

1. **Pin == remote default HEAD (verbatim, machine-derived):**

```
$ git -C eval-omarchy ls-remote origin refs/heads/main
f70f75873da963dce2debbc6c52e42e05267c045	refs/heads/main
```

The staged gitlink equals that SHA exactly (the umbrella tree entry after
`git add eval-omarchy` is `f70f7587…`).

2. **Gate, executed with pasted output (validator finding 2):**

```
$ git commit -q -F /tmp/ph2-msg.txt
pre-commit: running the umbrella gate…
check-policy-b:   OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

3. **The pinned commit's runtime behavior was validated live** by the cutover
chain itself: #70's pin change (the range's config commit) PASSed its own
validator review (rounds 2–3 findings all fixed, round-4 PASS) with the
controlled on-tree A/B discriminator on the final tree
(`model=deepseek-v4-flash:cloud` on the old-tag arm vs
`model=deepseek-v4.1-flash:cloud` on the bumped pin — the eval-lane ADE
checks + `box validate` 0/0 also executed).

## Rulebook compliance

- **R0** — `git-workflow`; PR-only landing, no direct push.
- **R1** — no anomaly surfaced by this change (mechanical pin sync).
- **R2** — no parking: this IS the named Phase H sync of the cutover plan.
- **R3** — single pin source: default-branch HEAD per `scripts/sync-gitlinks.sh`.
- **R4** — no workaround: machine-derived pin (`ls-remote`), not hand-set.
- **R5** — old pin replaced in the same commit (single-commit diff above).
- **R6** — `git status` inspected; only `eval-omarchy` staged.
- **R7** — umbrella gate scripts green on the commit (pasted above).
- **R8/R9/R10** — N/A (gitlink only; no generation/artifact/runtime path; the
  pinned commit's runtime was validated in #70 — cross-referenced above).

## Change classification

- **Change class:** config — umbrella gitlink pin sync (the committed range
  includes a config change, #70's lane pin; not all-documentation).
- **Verification gate:** pin == `ls-remote` default HEAD (pasted); range shown;
  umbrella gate green (pasted); the pinned behavior validated in #70
  (cross-referenced, with its own pasted A/B).
- **Attribution tier:** `analysed on a live system` — the pin value is
  machine-derived and the range's runtime path (the bumped lane pin) executed
  live in #70's validated run; the changed line itself (a gitlink) has no
  runtime of its own to execute. The footer below matches this tier and the
  OBSERVED authoring model (session log: `"model":"deepseek-v4-flash:0731"`,
  provider ollama-cloud).

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*