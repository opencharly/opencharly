# 2026.241.1945 — feat(pi): add pi-simple-team package and teams escalation guidance

## Summary

Add the **pi-simple-team** extension to the umbrella's per-project pi config and teach the system prompt to escalate to teams when sub-agents are insufficient.

- **`.pi/settings.json`**: add `git:github.com/giladbarnea/pi-simple-team@v1.0.9` to `packages` (pinned to the latest tag, same pattern as `pi-goal`).
- **`.pi/APPEND_SYSTEM.md`** (new): teams escalation guidance — spawn a TEAM (via "Spawn a team") when a job exceeds a single sub-agent: parallel workstreams that must coordinate, adversarial review loops, or missions needing multiple perspectives. Teams are the escalation path AFTER sub-agents; `/team` gives a live read-only dashboard.

## How tested

All verification ran live with `pi -p` (non-interactive mode) from the umbrella root:

1. **Boot test** — `pi -p "Reply with exactly: PACKAGE_TEST_OK"` → `PACKAGE_TEST_OK`
2. **System prompt carries the guidance** — `pi -p` confirmed the `# Teams (pi-simple-team)` section is in the system prompt, quoting verbatim: *"Teams are the escalation path AFTER sub-agents: prefer a single sub-agent for bounded, isolated work; use a team when the work is too large, too parallel, or needs internal challenge to converge."*
3. **Extension registered** — `pi -p` listed the 5 team tools: `team_spawn`, `team_shutdown`, `teamstatus`, `teamsend`, `teamlog`
4. **End-to-end team spawn** — `pi -p` spawned a 2-agent team (implementer + reviewer), implementer computed 6×7=42, reviewer independently verified, team shut down → `TEAM_ANSWER=42`
5. **Pre-commit gate** — `check-policy-b: OK`, `check-harness-parity: OK` (5 shared harness files identical to charly/)

## Rulebook compliance

- **R0**: skills loaded first (git-workflow for the PR landing path).
- **R6**: `git status` audited before staging; only the two intended files staged (pre-existing untracked `plan/*.md` left alone); no force-push, no direct push to main.
- **R7**: pre-commit gate ran and passed on the committed tree.
- **Harness parity (rule 8)**: `.pi/settings.json` + `.pi/APPEND_SYSTEM.md` are umbrella-root harness config; `check-harness-parity` passed (the shared files are unchanged; this change is additive to the umbrella's own package list and prompt, not a fork of a shared file).

## Change classification

- **Change class**: configuration + prompt guidance (no runtime code in charly or submodules).
- **Verification gate**: live `pi -p` runs (boot, prompt content, extension registration, team spawn) — changed paths executed live.
- **Attribution tier**: `analysed on a live system` — the changed runtime path (pi session boot with the new package + prompt) ran live with retained output; the full `task verify` pinning gate is unaffected by this change (no pins touched).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*