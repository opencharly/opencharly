# 2026.235.2110 — chore: the batch-1 candy repos become submodules (the candy cutover)

## Summary

The candy cutover's **batch 1**: the 15 most-required base candies move to their standalone kind-prefixed repos (`layer-supervisord`, `layer-nodejs`, `layer-golang`, `pod-dbus`, `pod-pipewire`, `layer-ffmpeg`, `pod-sway`, `layer-cuda`, `layer-tmux`, `layer-socat`, `pod-selkies`, `layer-chrome`, `layer-python`, `layer-gemini`, `pod-chrome-cdp`). The umbrella checks them out like every other org repo, following their own default-branch HEAD (the every-other-repo rule).

## How tested

**The umbrella gate on the final tree (pasted):**

```
$ task verify
verify-pins: OK — 39 submodules, policy B holds
OK: /home/atrawog/Sync/Atrapub/coder/pi/opencharly/.pi/extensions/umbrella-gates.ts parses (node type-strip pass)
```

**The repos are live at their tags (pasted):**

```
$ git ls-remote https://github.com/opencharly/layer-supervisord.git | grep -E "HEAD|tags"
… refs/heads/main
… refs/tags/v2026.235.2056
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface touched |
| R1 | the scaffold's bootstrap path surfaced two failures (the direct-push gate + the remote default-branch drift) — fixed in-tree (PR bootstrap + the default-branch reset) |
| R2 | N/A — no parked issue |
| R3 | the sync scripts remain the single pin mechanism |
| R4 | N/A — the submodules follow their own default-branch HEAD |
| R5 | N/A — the legacy deletion is the charly PR's |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | the verify output pasted above |
| R8 | N/A — no generation changes |
| R9 | N/A — no deploy target change |
| R10 | the verify ran on the final committed tree |

## Change classification

- **Change class**: submodule adds (15 candy repos)
- **Verification gate**: `task verify` (39 submodules, policy B holds)
- **Attribution tier**: analysed on a live system — the verify executed live with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*