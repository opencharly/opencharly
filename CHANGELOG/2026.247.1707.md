# 2026.247.1707 — chore: advance family gitlinks + cachyos re-vendor pins

## Summary

Advances the umbrella gitlinks for the openclaw/selkies pin-hygiene chain (landed upstream: layer-wl-record/screenshot-pixelflux, pod-kde-selkies, marketplace, pod-selkies-core, pod-selkies-desktop, layer-selkies-kde-desktop) and the cachyos re-vendor (charly #537 advanced box/cachyos to the post-#65 distro main):

- family repos → their current default-branch heads;
- distro-cachyos → b14e4157 (equals charly's box/cachyos gitlink — policy B);
- charly → 9d9ee1b3 (post-#537 main, tag v2026.247.1701).

## How tested

Fresh-clone gate (the objective's required evidence; clone of this branch with nested submodule init, no sibling bed dirt):

```
$ task verify   # fresh clone, feat/sync-family-pin-advances (content through 253f101)
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 398 submodules, policy B holds
VERIFY_EXIT=0
```

Head advance 3194cc2 (charly gitlink -> 9d9ee1b3, = charly's current main) re-ran the pre-commit gate at commit time:

```
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

All pins are merged refs (tag-on-merge CalVers, verified via gh api). Full log: /tmp/cbx-umb-verify.log (398 submodules, policy B holds).

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow skill loaded before the sync. |
| R1 — RCA every anomaly | Each family gitlink lagged its repo's merged main; the charly + distro-cachyos pins track the #537 re-vendor in lockstep. |
| R3 — no duplication | Gitlinks are the single record; policy B mirrors charly's pins (no parallel pins). |
| R4 — no workarounds | sync-gitlinks mechanism + direct verified-pointer advance; no overrides. |
| R6 — git safety | No force-push; branch-per-change PR; no direct main push. |
| R7 — prove behaviour | task verify GREEN pasted (fresh clone) + pre-commit gate re-run at each commit. |
| R10 — fresh disposable proof | Gate run on a fresh clone of the branch (nested submodule init), not the development tree. |

## Change classification

Gitlink sync (pointer-only). **Attribution:** fully tested and validated (fresh-clone task verify GREEN — 398 submodules, policy B holds — pasted; every pointer verified against its repo's current merged main). Footer confidence: high confidence.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (high confidence)*