# 2026.246.1232 — chore(gitlinks): sync marketplace + plugin-deploy-vm pins

## Summary

Umbrella gitlink sync: record the default-branch heads of `marketplace` and `plugin-deploy-vm` — the two repos the umbrella main (PR #119) had not yet recorded:

- **plugin-deploy-vm** — the CHANGELOG/tag commit after the merged `EnsureIsoGuestSSH` bootstrap (PR #8, v2026.246.1026)
- **marketplace** — its current default-branch head

No policy-B distro pins touched (charly-pinned distros stay at charly's own gitlinks).

## How tested (pasted, verbatim)

```
$ task verify
FAIL: distro-cachyos: dirty working tree
task: Failed to run task "verify": exit status 1

$ pin check for the two changed repos
marketplace:      gitlink=cff15214ce origin/main=cff15214ce
plugin-deploy-vm: gitlink=795e5c467d origin/main=795e5c467d
```

**The single verify failure is a NAMED, LIVE, non-parked batch:** `distro-cachyos`'s in-flight `check-vm-clone` VM-on-VM layering bed (cutover task 9) — an actively running sibling session (live QEMU `charly-check-vm-clone` process, charly.yml mtime-timestamped) authoring `charly.yml` in that repo. This PR does not touch distro-cachyos; the gitlink sync here changes exactly two pins, both verified against live `origin/main` at commit time (matched above).

**The exit(s) for the distro-cachyos batch:** (a) that bed's own PR in `opencharly/distro-cachyos`, and (b) the policy-B pin advance in `opencharly/charly` (its `box/cachyos` import) — after which the next umbrella sync records it. Neither is this PR's scope.

## Rulebook compliance

- **Failure analysis:** an earlier in-session sync attempt wrote stale branch-head gitlinks; the pre-commit policy-B gate caught the charly-pinned distros (distro-arch, distro-omarchy) and this PR excludes them — sync, never hand-pin.
- **No deferral/parking:** the one verify failure is named (repo, bed, task number) + attributed to a live session + given its exits above — it is not dismissed.
- **No duplication:** the umbrella main (PR #119) already recorded the other pins; only the two genuine deltas are here.
- **Git safety:** no force-push; the stale first-sync branch was deleted via the API (own branch, never shared); PR-only landing.
- **Prove the gate:** pre-commit umbrella gate passed on this commit (policy B clean); verify output + pin matches pasted verbatim.

## Change classification

- **Change class:** gitlink sync (policy-B-clean, two pins)
- **How verified:** `task verify` (state captured) + live `origin/main` comparison for both changed pins
- **Confidence:** analysed on a live system

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*