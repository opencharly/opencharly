# 2026.243.1654 — docs: rewrite the org map as per-category alphabetical tables covering all 389 submodules

## Summary

Rewrites the umbrella README's org map: it previously listed ~20 headline repos; the umbrella now pins **all 389** non-archived org repos as submodules (`#95` completed coverage), so the map is regenerated as **11 categorized tables**, each **alphabetically ordered**, with a role per repo sourced from the org's repo descriptions (category fallback where GitHub has no description on record):

- Core & contract (5) — charly, sdk, spec, marketplace, docs
- Distro image families (6) — distro-*
- Package repositories (10) — charly-*, pkg-*, plugin-generate-packages
- Product & tooling (5) — charly-streamer, dotgithub, gst-wayland-display, pi-review-action, pixelflux
- Charly candy layers (22), Omarchy layers (13), Check & fixture layers (15), Other layers (137)
- Plugins (101), Pods (72), VMs (3)

Also updated: the intro states the 389 count; the deliberate non-submodules are documented (the umbrella itself, `.github` pinned as `dotgithub/`, archived `heroic-heroic`); the pinning policy's "everything else" list is generalized to cover all repo families (`layer-*`, `plugin-*`, `pod-*`, `vm-*`, …). Contract sections (policy B, house rules, harness parity, helpers, license) are preserved.

## How tested

1. **Pre-commit gate on the commit** (hook env simulated — linked worktree):
```
$ GIT_DIR=$(git rev-parse --absolute-git-dir) GIT_INDEX_FILE=$(git rev-parse --git-path index) bash scripts/check-policy-b.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
$ GIT_DIR=$(git rev-parse --absolute-git-dir) GIT_INDEX_FILE=$(git rev-parse --git-path index) bash scripts/check-harness-parity.sh
check-harness-parity: OK — 5 shared harness files identical to charly/
```
2. **Real commit through the hook** (commit 126ba47) — pre-commit passed.
3. **Table integrity**: 389 table rows, all 389 submodule paths present exactly once (duplicate check clean), each category sorted alphabetically.
4. **Full pinning audit (R10) on the committed tree**:
```
$ cd /tmp/umbrella-readme-rewrite && task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 389 submodules, policy B holds
```
Post-audit `git status --short` clean.

## Rulebook compliance

- R1: the map was stale-by-design (manual curated list); regenerated from live data (`.gitmodules` + org repo API).
- R3/R10: verified on the final committed tree with the full pinning gate; no duplication (each repo exactly once).
- R6: PR-only landing via feat/ branch off fresh `origin/main`; no force-push, no direct push.

## Change classification

- Change class: docs-only (README.md).
- Verification gate: `task verify` full pinning audit + pre-commit gate, both passed live.
- Attribution: `documentation reviewed` — docs-only change class; no runtime path.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*