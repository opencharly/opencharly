# 2026.268.1936 — chore: sync gitlinks for the check-bed RunEnv cutover

## Summary

Record the merged **check-bed roster RunEnv cutover** gitlinks (plan §4.2 / F8).
Five owning-repo PRs landed:

| Repo | PR | What |
|---|---|---|
| `opencharly/spec` | #159 | `RunEnv` per-invocation env; `ResolveBedForRoot`; short-bounded deploy-config lock |
| `opencharly/sdk` | #287 | required ctx on the member/deploy-family; ctx-aware overlay resolution |
| `opencharly/plugin-check` | #58 | retire bed `os.Setenv` isolation; namespace-aware bed refs; per-bed temp-dir guard |
| `opencharly/plugin-fleet` | #21 | ctx to member bring-up/teardown + member persist |
| `opencharly/charly` | #663 | pin bump adopting the four producers |

This umbrella PR is the gitlink sync: `charly`, `plugin-check`, `plugin-fleet`,
`sdk`, `spec` each recorded at its merged default-branch HEAD. No submodule is
edited here; all five changes landed by PR to their owning repos (umbrella rule 1).

## Change class

**Cross-repo code/config pin bump** — the five gitlinks adopt the RunEnv cutover
CODE into `charly`, `sdk`, `plugin-check`, `plugin-fleet` (plus the `spec`
contract). This is NOT docs-only: the runtime behavior rides in through these pins.

## How R10-tested — exact gate, commands, and pasted output

- **(a) Umbrella gate:** `charly task verify` — proves gitlink consistency only.
- **(b) Runtime proof:** the pinned combination was executed live by the charly
  consumer leg (`opencharly/charly#663`, whose tree carries exactly these four
  producer pins and whose merged HEAD is the `charly` gitlink recorded here) via a
  `disposable: true` bed. Full output pasted below.

The disposable target is `charly.check-sidecar-pod` (`disposable: true` in
`charly/charly.yml`), which exercises both the plugin-check isolation path and the
plugin-fleet member path. The operator's real overlay was left untouched.

*1. Umbrella gate — `charly task verify` (final tree):*

```
$ ./charly/bin/charly task verify
task verify: 3 step(s), 0 failed
  [pass] check — policy B — every distro-* gitlink equals charly's own box/* gitlink
  [pass] check — every submodule is clean and checked out at its recorded gitlink, and its branch equals the remote default
  [pass] check — the opencode gate plugin contract
```

*2. Runtime proof — `charly check run check-sidecar-pod` on the pinned tree (complete 17 steps):*

```
[vm-build-check-sidecar-pod-ephvm] PASS after 13.025s [allowlisted=1]
[image-build] PASS after 1m5.621s [allowlisted=1 cache=133/141]
[check-image] PASS after 25.034s [allowlisted=1]
[deploy-add] PASS after 24.502s [allowlisted=1]
[config] PASS after 21.454s [allowlisted=1]
[start] PASS after 13.124s [allowlisted=1]
[bring-up-members] PASS after 1m33.143s
[check-live] PASS after 1m55.087s [allowlisted=1]
[feature-run] PASS after 21.003s [allowlisted=1]
[rebuild-members-down] PASS after 43.347s
[update] PASS after 3m20.047s [allowlisted=8]
[re-bring-up-members] PASS after 1m34.627s
[rebuild-ready] PASS after 545ms
[check-live-rebuild] PASS after 1m54.512s [allowlisted=1]
[feature-run-rebuild] PASS after 19.765s [allowlisted=1]
[cleanup] PASS after 14.517s [allowlisted=1]
[cleanup-members] PASS after 43.271s
charly check run check-sidecar-pod: PASS (steps=17)
```

All 17 steps `ok: true`; only the allowlisted `dev-worktree-binary-notice`.
Issue-7 proof: operator overlay keys `{version, cache, system}` before AND after —
`grep -cE '^(check-sidecar-pod|vm:check-sidecar-pod|ledger):' ~/.config/charly/charly.yml` = `0`.

*3. diff-stats:*

```
$ git diff --stat origin/main...HEAD
 5 files changed, 5 insertions(+), 5 deletions(-)
```

## Rulebook compliance

- **Rule 1** — never edit inside a submodule; this PR only records gitlinks.
- **Rule 5/6** — only merged default-branch refs are pinned (policy B); no branch
  pin.
- **Rule 9** — only this session's five theme gitlinks are staged; unrelated
  submodule drift surfaced by `task sync` was restored to the recorded gitlinks.
- **R7** — `task verify` output pasted above.

## Change classification

- **Change class:** cross-repo code/config pin bump (not docs).
- **Verification gate:** `charly task verify` (pointer consistency) + the charly
  leg's live `charly check run check-sidecar-pod` (PASS, 17 steps) on the same
  pinned tree.
- **Attribution tier:** `analysed on a live system` — the pinned runtime path ran
  live with retained output; the umbrella's own complete fresh-rebuild R10 was not
  run here (its gate is pointer consistency).

*Assisted-by: opencode deepseek-v4.1-flash (analysed on a live system)*