# 2026.244.0438 — chore: sync gitlinks after the skill-description tightening pass

## Summary

Advance 12 submodule gitlinks to their owning repos' default-branch HEADs after the trigger-style skill-description tightening landed upstream (the corpus-wide pass that tightened all skill `description:` frontmatter to trigger-style index lines, validated and merged in each owning repo):

- layer-charly-build, layer-charly-check, layer-charly-image, layer-charly-internals, layer-charly-kubernetes, layer-charly-local, layer-charly-pod
- marketplace (053f388 → 4ddd41c — the refs bump + regenerated corpus with the tightened descriptions)
- plugin-wl, pod-charly-automation, pod-charly-core, vm-charly-vm

## How tested

`task verify` on the committed tree:
```
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 389 submodules, policy B holds
```
EXIT=0, tree clean after the run (only the 12 staged gitlinks).

## Rulebook compliance

- **Rule 5 (pin discipline)** — only merged refs (default-branch HEADs), never PR branches.
- **Rule 6 (Policy B)** — distro-* pins equal charly's gitlinks; verify-pins OK.
- **Rule 2 (git op rule)** — all submodule git ops via `git -C` from the umbrella root.
- No direct push to main; PR-only landing.

## Change classification

- **Change class:** gitlink sync (submodule pointer bumps to merged default-branch HEADs)
- **Verification gate:** `task verify` GREEN (EXIT=0, 389 submodules, policy B holds)
- **Attribution tier:** `analysed on a live system` — the pinning gate ran live with retained output.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*