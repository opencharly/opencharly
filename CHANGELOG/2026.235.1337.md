# 2026.235.1337 — chore: sync the pins — charly 45cdc36, marketplace a1c5e8b

## Summary

The marketplace cutover's **final pin sync**: charly#383 + marketplace#230 merged (the MARKETPLACE_REF advance `e8efdb15` + the regenerated corpus at the ref-advance charly). The umbrella's charly pin moves to `45cdc36` (the ref-advance main) and the marketplace submodule to `a1c5e8b` (the regenerated corpus main), per the every-other-repo rule.

## How tested

**Sync (pasted):**

```
$ bash scripts/sync-gitlinks.sh
  charly -> 45cdc36b
  marketplace -> a1c5e8b
  docs -> e4dff7b
```

**Gates on the final tree (pasted):**

```
$ task verify
verify-pins: OK — 23 submodules, policy B holds
OK: /home/atrawog/Sync/Atrapub/coder/pi/opencharly/.pi/extensions/umbrella-gates.ts parses (node type-strip pass)
$ task harness
check-harness-parity: OK — 5 shared harness files identical to charly/
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface touched |
| R1 | N/A — no surfaced failure |
| R2 | N/A — no surfaced issue parked |
| R3 | sync-gitlinks.sh remains the single pin mechanism |
| R4 | N/A — the sync is the policy's own operation |
| R5 | N/A — no legacy path remains (plugins is gone; marketplace follows its own HEAD) |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | sync + verify + parity outputs pasted above |
| R8 | N/A — no generation changes |
| R9 | N/A — no deploy target change |
| R10 | the sync + verify + parity ran on the final committed tree |

## Change classification

- **Change class**: gitlink sync (charly + marketplace pin advance)
- **Verification gate**: `task verify` (23 submodules, policy B holds) + `task harness`
- **Attribution tier**: analysed on a live system — the sync + gates executed live with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*