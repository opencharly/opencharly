# 2026.252.1444 — chore(umbrella): sync the eval-omarchy submodule pin to v2026.252.1425

## Summary

Syncs the umbrella's recorded eval-omarchy submodule gitlink from 8d305ca to **d52ed26** (the eval-omarchy tag v2026.252.1425, the merged main HEAD).

The eval-omarchy chain landed the charly-native eval workflow (PRs #49-#55, tags v2026.252.1018-v2026.252.1425):

1. The **eval-lane-plan** pipeline entity (the generic agent/workflow engine's eval lane): the channel-select → triage (the Config Oracle agent) → bed-render → config-audit → eval (real beds) → assemble-media → evidence-audit → report → cold-read → render-report → publish (the EVAL_PUBLISH-gated) stages, concurrency lanes 16.
2. The **validation-only entity plan** (RCA 2026.252.1152: the entity plan's run: steps execute once and are then skipped by the install ledger on re-provisions) — all golden mutations moved to the omarchy-eval-harden candy (the install timeline, re-applies on every deploy-add).
3. The **placeholder-bed control** (RCA 2026.252.1206): the bed-render's validate REJECTS placeholder/empty-sha beds at render time — exercised deterministically (REJECTED + exit 1 on the reconstructed pre-fix bed).
4. The **local-ollama LLM default** (RCA 2026.252, the lane 401): the lane's LLM endpoint defaults to the local ollama (deepseek-v4-flash:cloud) — implemented upstream in opencharly/plugin-pipeline#9 (the authored entity llm block + the env-override layer + the built-in default, uniform precedence, the auth header only when the key is non-empty).

## How tested (pasted)

```
$ ./scripts/sync-gitlinks.sh
SYNC_EXIT=0 - sync-gitlinks: done — review with 'git status' and 'git diff --submodule'
  eval-omarchy -> d52ed26

$ git diff --submodule=short eval-omarchy
Submodule eval-omarchy a8e8fa1..d52ed26 (new commits)

$ /tmp/charly-r10-fixed/bin/charly box validate   (run INSIDE the bumped eval-omarchy)
charly box validate: OK — checked 186 candies, 21 boxes, 71 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

Only the eval-omarchy gitlink is committed; the other submodules' worktree movements (other sessions' landings) were restored to their recorded shas - never committed here.

## Rulebook compliance

- R0 (skills first): loaded - git-workflow, root-cause-analyzer, check, agents, umbrella AGENTS.md.
- R1: every failure in the eval chain (the 401, the placeholder beds, the missed control, the entity-plan skip) separately RCA'd from live evidence before remediation (the numbered RCAs above).
- R2 (no split landings): the gitlink bump is one commit; the eval-omarchy side landed as its own merged chain.
- R3 (no duplication): only the eval-omarchy gitlink is bumped (the other submodules' pins left for their own sessions).
- R4 (no workarounds): the upstream fixes are root-cause fixes (the typed llm config, the implemented validate contract).
- R4a: N/A - no host aliases.
- R5 (delete legacy): the dead var entries + the entity plan's dead run steps deleted upstream (merged).
- R6 (git safety): no force-push, no hook bypass, PR-only landing.
- R7 (prove the gate): the sync output + the bumped-repo box validate green pasted.
- R8 (docs): N/A - no docs surface changed.
- R9 (submodule hygiene): the pin is a merged ref (the tag), never a PR branch; the other submodules restored, not committed.
- RDD/ADE/SDD: the schema-side change is upstream (#9, CUE + generated types); the ADE grading surface unchanged.
- Concurrency mandate: the lane's concurrency (lanes 16) unchanged.
- Blast radius: the recorded pin moves to the merged chain; no other submodule pin moves in this PR.

## Change classification

- Change class: chore (submodule pin sync)
- Verification gate: R10 - the sync output + the bumped-repo box validate green (both pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*