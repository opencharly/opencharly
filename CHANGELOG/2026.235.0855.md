# 2026.235.0855 — chore: docs de-submodule policy + sync — docs pins itself, harness hooks synced

## Summary

The **docs de-submodule cutover's umbrella leg** (charly#381 dropped the docs submodule; plugins#228 landed the corpus; the docs repo now pins charly in *its* `.gitmodules` and its deploy workflow owns the site pipeline):

- **`scripts/sync-gitlinks.sh` + `scripts/verify-pins.sh`** — `docs` dropped from `CHARLY_PINNED` and from the default-pin skip list: docs now follows the every-other-repo rule (its own default-branch HEAD), exactly as sdk/spec did. Policy-B set shrinks to `plugins` + `box/*`.
- **`README.md`** — policy-B text (point 2 loses `docs`; point 3 gains it) + the repo table (charly's nested set is plugins/box only; docs row notes it pins charly and owns the pipeline).
- **Harness parity** — `.claude/hooks/{pre-push-gate.sh,gate_test.py}` synced to charly main (the PR-body regex block retired there — the org `pr-validator` is the merge gate, per the hooks doctrine); `check-harness-parity` reports 6 files identical.
- **Pins synced** — `charly` → `92df8e63` (the merged cutover), `plugins` → `f4f5980` (the corpus merge — charly's own pin), `docs` → `bec9143` (its default HEAD, no longer charly-pinned), sdk/spec unchanged.

## How tested

**Sync — executed with the updated scripts (pasted):**

```
$ bash scripts/sync-gitlinks.sh
  charly -> 92df8e63
  plugins -> f4f5980
  docs -> bec9143
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'
```

**Harness parity — executed (pasted):**

```
$ bash scripts/check-harness-parity.sh
check-harness-parity: OK — 6 shared harness files identical to charly/
```

**Gate — executed on the final tree:**

```
$ task verify
verify-pins: OK — 23 submodules, policy B holds
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface touched (scripts/README/hooks mirror; no skills edited) |
| R1 | the docs-pinning leftovers in the umbrella scripts root-caused to the charly cutover's policy change — fixed here |
| R2 | N/A — no surfaced issue parked; all leftovers fixed in-tree (see the R1 row) |
| R3 | sync-gitlinks.sh + verify-pins.sh remain the single pin mechanism; the parity script owns the hooks mirror |
| R4 | no workarounds — the sync is the policy's own operation |
| R5 | every docs-pinned reference in the umbrella scripts/README updated in this commit |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | sync + parity + verify outputs above |
| R8 | N/A — no generation/emitted-artifact changes |
| R9 | N/A — no deploy target added or changed |
| R10 | pin-sync + script changes; the gate is `task verify` on the final tree |

## Change classification

- **Change class**: gitlink sync + policy script/README edits + harness mirror
- **Verification gate**: `task verify` (verify-pins, policy B holds) + `task harness`
- **Attribution tier**: analysed on a live system — sync + parity + verify executed live with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*