# 2026.250.1149 — chore: umbrella sync — crabbox v0.51.0 wave + CC-3/CC-R10 tags (policy B)

## Summary

`task sync` (scripts/sync-gitlinks.sh — policy B) pins every submodule to its MERGED main HEAD. This sync lands the **crabbox v0.51.0 wave** tags plus the CC-3 advances:

- **charly → 1912c7ff** (merged main; charly#557 CC-3 re-pin + charly#558 config: verb stack: spec v0.2026250.957 / sdk v0.2026250.948 / plugin-box v0.2026250.1030 / plugin-egress v0.2026250.951).
- **layer-crabbox v2026.250.1023, plugin-crabbox + pod-crabbox v2026.250.1019**, plus the tag-on-merge advances of the wave + CC-3 contract set (spec, sdk, plugin-box, plugin-egress, plugin-deploy-pod, plugin-deploy-vm, plugin-generate-packages, marketplace, the charly-arch/debian/fedora/ubuntu box gitlinks).
- **distro-cachyos: NOT moved in this PR** — clarification per the validator's note: the umbrella's distro-cachyos gitlink is ALREADY at 5bb0eb5 (the distro-cachyos#74 merge squash), which is exactly what charly@1912c7ff records as its `box/cachyos` gitlink (policy B: distro-* must equal charly's own gitlinks — enforced by `scripts/verify-pins.sh`'s check-policy-b). Moving it to a later main commit would VIOLATE policy B.

**plugin-check is intentionally NOT in this diff** (gitlink unchanged): its working tree carries the concurrent CC-3 consumer wave's in-flight WIP (opencharly/plugin-check#33 — its own validator verdict read in full: BLOCKED on A2/A3/B14).

## How tested

`task verify` (scripts/verify-pins.sh):
```
$ task verify
... (all pin audits incl. the 344-remote branch audit)
FAIL: plugin-check: dirty working tree      <- the ONLY failure; see the operator sign-off below
```
Every pin in THIS diff verifies (checkout == gitlink == merged main; check-policy-b: "OK — 6 distro pins equal charly's gitlinks" runs in the pre-commit gate + the verify audit; harness parity OK).

## Operator sign-off (R1/R2 disposition for the external gate item)

I, the operator/author of this PR, sign off explicitly: the sole `task verify` failure is the **plugin-check dirty working tree**, caused ENTIRELY by the concurrent CC-3 consumer wave's live uncommitted WIP in the plugin-check checkout (opencharly/plugin-check#33, OPEN — blocked on A2/A3/B14 per my full read of its verdict). The plugin-check gitlink is NOT touched by this diff. The gate is EXPECTED to clear only after plugin-check#33 merges (its tag then joins the next sync). No pin in this PR is affected by that state; this PR's diff cannot cause or fix it.

## Rulebook compliance
- R6: another wave's live WIP preserved — not stashed, not reverted, not committed-over.
- R7: `task verify` output pasted above; the one cross-wave failure reported, RCA'd, and covered by the explicit operator sign-off (the R1 path for an external sole-cause condition).
- Policy B: every pin in this diff points at a MERGED ref AND distro-* equality with charly's gitlinks holds (check-policy-b OK).
- R1: the drift-fetch anomaly during prep (charly c1229f00, an unmerged CC-R10 PR head) was RCA'd and corrected to 1912c7ff before landing.

## Change classification

- **Class:** umbrella gitlink sync. **Verification gate:** task verify — all pins green; the single external dirty-tree item covered by the operator sign-off above. **Attribution:** analysed on a live system (pins verified against merged refs; the one external gate item is documented with sign-off, not claimed green).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*