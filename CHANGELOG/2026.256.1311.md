# 2026.256.1311 — chore: drop the residual plugin-group submodule

## Summary

Removes the residual `plugin-group` submodule **mount** and its README table row. The
`opencharly/plugin-group` repository itself is archived separately; this PR only stops the umbrella
from mounting it.

- `git rm plugin-group` (gitlink removed), `[submodule "plugin-group"]` dropped from `.gitmodules`,
  README row removed.

## R7 — the pinning gate is provably unaffected (policy-B scope)

`distro-*` is the only pin family policy B constrains ("Policy B is the contract: `distro-*` must
equal charly's own gitlinks"). `plugin-group` is not a `distro-*`; removing its mount cannot change
any `distro-*` gitlink. Executed evidence on this branch tree:

```
$ git ls-files -s plugin-group
(empty)                                    # gitlink removed
$ grep -c 'plugin-group' .gitmodules
0
$ git config -f .gitmodules --get-regexp '^submodule\..*\.path$' | grep -E 'distro-(arch|cachyos|debian|fedora|omarchy|ubuntu)'
submodule.distro-arch.path distro-arch
submodule.distro-cachyos.path distro-cachyos
submodule.distro-debian.path distro-debian
submodule.distro-fedora.path distro-fedora
submodule.distro-omarchy.path distro-omarchy
submodule.distro-ubuntu.path distro-ubuntu
```

`scripts/verify-pins.sh` audits `distro-*` (and the remote branch set); this diff touches neither.

## R5 — sweep (real output)

```
$ grep -rn 'plugin-group' --exclude-dir=.git --exclude-dir=CHANGELOG .
(no output)
```

Only CHANGELOG history (excluded) and the deleted paths carried the name.

## R2 — siblings, all open and named

The `group` kind was removed by the kernel's member-tree cutover (authored `group:` is rejected). The
retirement is split by owning repo, none parked:

- **opencharly/charly#608** — drop the stale `plugin-group` mention in `registry_bootstrap.go`.
- **opencharly/docs#120** — remove the corpus pin + concept-page `group` and regenerate the census.
- **opencharly/opencharly#203** — this mount removal.

## R0 / R4

- **R0** — the owning skill (`/charly-internals:plugin`) was consulted for plugin placement.
- **R4** — no ad-hoc workaround; the change is a manifest edit landed by PR.

## Change classification

- Class: **submodule-manifest (config) change**, no submodule content change.
- Gate: the pinning gate's policy-B surface is unaffected (evidence above); no image/deploy path.
- Confidence: **analysed on a live system** — the changed manifest/submodule inventory was inspected
  live (gitls/git config output above); the full `task verify` remote-branch audit was not run locally.

*Assisted-by: opencode deepseek-v4.1-flash (analysed on a live system)*