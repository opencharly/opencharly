# 2026.251.1621 — sync(eval-omarchy): gitlink 41174c45 -> 95b778e (re-capture fix + eval-pr driver + redo-wave reports)

## Summary

Umbrella gitlink sync for the eval-omarchy submodule: `887560c` → **`a8e8fa1`** — the REAL current eval-omarchy main HEAD (verified by `git -C eval-omarchy log origin/main`):

```
a8e8fa1 docs: add CHANGELOG 2026.251.1547 for PR #44
6758015 feat(eval): eval-pr.py - the ONE guidance-driven driver (config file, debug, one lane per core)
42f869f docs: add CHANGELOG 2026.251.1521 for PR #45
ea02ba1 eval(redo-wave): the 14 PASS reports + REDO-WAVE evidence
```

carrying the three merged eval-omarchy PRs: **#43** (re-capture blocker fix, live-proven re-capture exit 0/8/8), **#44** (the eval-pr.py full-eval orchestrator + eval.yml + the sequencing skill WRITE-LOCK RACE section updated to FIXED), **#45** (the 14 redo-wave PASS reports + REDO-WAVE evidence).

## How tested

```
$ git -C eval-omarchy fetch origin main && git -C eval-omarchy rev-parse --short origin/main
a8e8fa1
$ git -C eval-omarchy checkout a8e8fa1 && git add eval-omarchy
$ git diff --cached | grep Subproject
-Subproject commit 887560cfa45a163233de8892f5b8e99b1c765451
+Subproject commit a8e8fa1...   # the pinned commit == the fetched origin/main HEAD
# pre-commit gate: check-policy-b OK (6 distro pins equal charly's gitlinks);
# check-harness-parity OK (5 shared harness files identical to charly/)
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | The prior BLOCK (pinned 887560c vs claimed 95b778e) was RCA'd: `git add eval-omarchy` staged the local checkout's HEAD, and the pasted `origin/main` ref was stale (pre-#44/#45). Fixed: fetch + pin the real main HEAD a8e8fa1; the body now names the exact pinned SHA. |
| R2 | N/A. |
| R3 | The sync is the canonical path (task sync semantics), not a hand-pin. |
| R4 | N/A. |
| R4a | N/A. |
| R5 | N/A. |
| R6 | feat/ branch from origin/main; no force-push. |
| R7 | Gate output + the pinned-SHA reconciliation pasted. |
| R8 | N/A. |
| R9 | N/A. |
| R10 | The pinned commit (a8e8fa1) IS the fetched eval-omarchy origin/main HEAD - verified in the pasted output. |
| RDD | N/A. |
| ADE | N/A. |
| SDD | N/A. |
| B14 | The gitlink diff + the pinned HEAD + the log are on the record. |
| B15 | N/A. |
| B17 | N/A - gitlink. |
| B18 | N/A. |

## Change classification

- Change class: submodule gitlink sync (1 file).
- Verification gate: pre-commit gates (policy-B, harness parity) + the pinned-SHA == origin/main check.
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*