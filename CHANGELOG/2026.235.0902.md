# 2026.235.0902 — chore: sync the docs pin to the standalone docs main (9a741f0)

## Summary

The docs standalone cutover's landing moved docs main (`docs#96` merged the standalone repo — .gitmodules charly pin + the deploy workflow). This sync advances the umbrella's docs pin to the new default-branch HEAD (`9a741f0`), per the every-other-repo rule the umbrella policy PR (#13) established. The docs checkout's nested charly submodule is materialized at the docs gitlink (verified).

## How tested

**Sync — executed (pasted):**

```
$ bash scripts/sync-gitlinks.sh
  docs -> 9a741f0
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'
```

**Gate — executed on the final tree (pasted):**

```
$ task verify
verify-pins: OK — 23 submodules, policy B holds
OK: /home/atrawog/Sync/Atrapub/coder/pi/opencharly/.pi/extensions/umbrella-gates.ts parses (node type-strip pass)
```

**Nested materialization — the docs repo's pinned charly checked out (pasted):**

```
$ git -C docs submodule update --init
Submodule path 'charly': checked out '92df8e63…'
$ git -C docs status --short
(clean)
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface touched |
| R1 | the docs-pin staleness root-caused to the #96 merge advancing docs main — synced here |
| R2 | N/A — no surfaced issue parked |
| R3 | sync-gitlinks.sh remains the single pin mechanism |
| R4 | N/A — the sync is the policy's own operation |
| R5 | N/A — no legacy path remains (docs follows its own HEAD since #13) |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | sync + verify outputs pasted above |
| R8 | N/A — no generation changes |
| R9 | N/A — no deploy target change |
| R10 | N/A — submodule-pointer sync; the gate is `task verify` |

## Change classification

- **Change class**: gitlink sync (docs pin advance)
- **Verification gate**: `task verify` (policy B holds) + `task harness`
- **Attribution tier**: analysed on a live system — sync + verify executed live with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*