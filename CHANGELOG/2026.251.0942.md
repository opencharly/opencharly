# 2026.251.0942 — feat(agents): reference the eval skills at .agents/skills/omarchy-eval-* + the R0 loading mandate for the rerun

## Summary

Sweep the four committed omarchy PR-eval pi agent definitions to reference the eval skills at their post-#41 standard location — `.agents/skills/omarchy-eval-*` inside the eval-omarchy checkout — instead of the stale `skills/` path, and add the supervisor's **R0 skills-loading mandate (binding)** to the Ownership section.

Changed:
- `.pi/agents/omarchy-cold-reader.md` — contract reference → `eval-omarchy .agents/skills/omarchy-eval-full-loop/SKILL.md`
- `.pi/agents/omarchy-config-oracle.md` — same contract-reference correction
- `.pi/agents/omarchy-eval-runner.md` — same contract-reference correction
- `.pi/agents/omarchy-eval-supervisor.md` — contract-reference correction + new binding ownership line: every lane run STARTS by loading `.agents/skills/omarchy-eval-lane/SKILL.md` plus the per-lane skills; skipping them is a contract violation verified by the CONFIG AUDIT

## How tested

```
$ grep -c '.agents/skills/omarchy-eval' .pi/agents/omarchy-cold-reader.md   # 1
$ grep -c '.agents/skills/omarchy-eval' .pi/agents/omarchy-config-oracle.md # 1
$ grep -c '.agents/skills/omarchy-eval' .pi/agents/omarchy-eval-runner.md   # 1
$ grep -c '.agents/skills/omarchy-eval' .pi/agents/omarchy-eval-supervisor.md # 2
1 / 1 / 1 / 2 — every agent references the .agents/skills/omarchy-eval-* location
```

```
$ ./scripts/check-omarchy-eval-agents.sh
check-omarchy-eval-agents: OK — 5 unique agents tracked + parseable, staged agents ignored
```

`git diff origin/main..HEAD --check` → clean (exit 0, no whitespace errors).

`git diff origin/main..HEAD --stat`: 4 files, 5 insertions(+), 4 deletions(-) — only the intended agent files.

## Rulebook compliance

- R6 (git safety): no force-push, no hook bypass — cherry-picked `2388c62` onto `feat/skills-r0-load` from current `origin/main`; the pre-commit gate ran and passed on the cherry-pick commit.
- R7 (prove the gate): the agent gate script output pasted above (exit 0, `set -euo pipefail`).
- Umbrella policy 1: no submodule edits — only `.pi/agents/*` in the umbrella; no gitlink changes.
- R3: no duplication — refs point at the single canonical skill location in the eval-omarchy checkout.

## Change classification

- Change class: harness config (agent markdown definitions).
- Verification gate: R7 local gate (script output pasted above); `task verify` full gate not run for this docs/harness-only change.
- Attribution tier: analysed on a live system (changed paths executed live on this checkout: grep counts, gate script, diff --check).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*