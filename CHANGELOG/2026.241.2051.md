# 2026.241.2051 — feat(pi): add keep-skills-in-sync-with-reality rule to the system prompt

## Summary

Adds the "Skills are living documents — keep them in sync with reality" rule to the pi system prompt (`.pi/APPEND_SYSTEM.md`). The rule operationalizes R1 for the skill corpus: a doc-vs-reality divergence is an incident (RCA + fix in the same change, sweeping sibling docs), a live bed contradicting a skill means the SKILL is stale (RDD — the running system is ground truth), the `/charly-*` skills are generated corpus (fix the source candy entity, regenerate with `charly marketplace generate`, land both PRs — never hand-edit generated `SKILL.md` files), and the umbrella's harness docs mirror charly's (`scripts/check-harness-parity.sh`).

## How tested

- `git show --stat HEAD` → 1 file changed, 21 insertions (only `.pi/APPEND_SYSTEM.md`).
- The file is not in the parity SHARED list (`scripts/check-harness-parity.sh` covers only the gate scripts + `.reasonix/settings.json`) — umbrella-specific fork, no charly mirror needed (`charly/.pi/` has no `APPEND_SYSTEM.md`).
- `bash scripts/check-harness-parity.sh` → OK (no parity drift introduced).

## Rulebook compliance

- R1: the rule itself mandates RCA + sweep for doc-vs-reality divergences.
- Rule 8 (harness parity): the addition is umbrella-specific (not in the SHARED list), verified against the parity script.

## Change classification

- **Change class:** documentation (harness system prompt)
- **Verification gate:** documentation reviewed
- **Attribution tier:** documentation reviewed

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*