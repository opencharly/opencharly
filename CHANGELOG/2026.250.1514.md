# 2026.250.1514 — chore: umbrella sync — MCP/systemd wave gitlinks (charly/plugin-check/sdk/distro-cachyos/layer-charly-{build,check,image}/marketplace)

## Summary

Umbrella gitlink sync for the MCP/systemd wave (policy B + every-other-repo rules). Ran `bash scripts/sync-gitlinks.sh` on the final tree and staged the resulting pins:

| repo | staged pin | basis |
|---|---|---|
| charly | 81f3cf23 | origin/main (e7ce2788 confirmed ancestor — main advanced with #559/#560/#561) |
| plugin-check | b7b3b673 | origin/main |
| sdk | fc9c81db | origin/main (#238 — the use_packaged systemd drop-in fix) |
| distro-cachyos | 5bb0eb5 | **UNCHANGED** — already at the policy-B pin (charly's `box/cachyos` gitlink, byte-for-byte) — no gitlink change in this PR |
| layer-charly-build | 3c07741 | origin/main (the mcp-cmd skill docs, #10) |
| layer-charly-check | dff796e | origin/main (the mcp: VM-venue + addr: VM-vars docs, #10) |
| layer-charly-image | 1093fea | origin/main (the service overrides → drop-in docs, #8) |
| marketplace | 22b8316 | origin/main at sync time (the corpus PR #289 merged after the sync; its gitlink follows in the next sync) |

16 gitlinks changed in total (all other repos rolled to their own default-branch HEADs). 400 gitlinks staged, 0 untracked files.

## How tested

**Verify gate (the final tree):**

```
$ task verify   # scripts/verify-pins.sh
FAIL: plugin-check: dirty working tree
task: Failed to run task "verify": exit status 1
```

**RCA of the single FAIL (R7/R1) + pasted evidence — every pin assertion passes; the sole FAIL is an external dirty tree:**

```
# the wave's staged gitlinks vs each repo's fresh origin/HEAD (real, current):
charly                 staged=81f3cf23 upstream=81f3cf23
plugin-check           staged=b7b3b673 upstream=b7b3b673
sdk                    staged=fc9c81db upstream=fc9c81db
layer-charly-build     staged=3c07741b upstream=3c07741b
layer-charly-check     staged=dff796ea upstream=dff796ea
layer-charly-image     staged=1093feaf upstream=1093feaf
marketplace            staged=22b83161 upstream=22b83161
distro-cachyos         staged=5bb0eb5f == charly's box/cachyos gitlink (policy B):
  $ git -C charly ls-tree HEAD box/cachyos   5bb0eb5f8983ec1024440c7a7733fb8730a777b3
  $ git ls-tree HEAD: distro-cachyos         5bb0eb5f8983ec1024440c7a7733fb8730a777b3

# the ONLY dirty working tree (the verify's sole FAIL — external, untouched):
$ git -C plugin-check status --short
 M candy/plugin-check/session_service.go
$ git -C plugin-check diff --stat
 1 file changed, 41 insertions(+), 9 deletions(-)
```

Every one of the 16 staged gitlinks equals its repo's merged upstream HEAD (verified against fresh fetches above; the full 400-submodule sweep confirmed plugin-check is the ONLY dirty tree and no other pin mismatches). The sole verify failure is `plugin-check`'s dirty working tree — `candy/plugin-check/session_service.go` +41/−9, another session's live in-flight edit (not this sync's work, untouched per R6). **Operator sign-off requested:** the wave's pins are all at merged heads; the single external dirty tree is recorded here as the sole-cause item with its exact file + diff (the same excusal umbrella PR #153 recorded for the CC-3 consumer wave), pending its owning session's cleanup.

**The sync's first-pass collision (B5/R1/B14 — RCA'd, not dismissed):** the umbrella root is the SHARED checkout every session's worker runs git in; the first sync pass collided with a sibling worker's git write (git's standard `.git/index.lock` concurrent-writer guard — the collision is the shared-clone concurrency, not a sync defect). The idempotent second pass completed, and the final staged state was INDEPENDENTLY verified afterwards: the 400-submodule sweep + the verify gate above + the per-repo origin/main byte comparisons — evidence of the final tree, not a re-run artifact.

## Rulebook compliance

- **R0:** git-workflow + the sync pattern.
- **R1:** the verify failure + the lock collision are RCA'd above (external WIP + shared-clone concurrency), each with its evidence.
- **R3:** sync-gitlinks.sh is the single pinning implementation.
- **R4:** no workarounds — the sync is idempotent + verified.
- **R6:** worktree/PR only; the plugin-check dirty tree is another session's, untouched.
- **R7:** the verify gate's output is pasted + RCA'd.
- **AI Attribution:** footer below.

## Change classification

- **Change class:** config (gitlink/pin sync).
- **Verification gate:** the pin-assertion sweep passed; the verify's sole failure is the RCA'd external dirty tree.
- **Attribution tier:** analysed on a live system — the sync + the verify ran live with retained output.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*