# 2026.241.0538 — chore: advance charly to the latest main + mirror distro-fedora (Policy B)

## Summary

charly#435 merged (box/fedora → f267d3f). Advance the umbrella's charly submodule to the latest main (f0f3aa58) and mirror distro-fedora to f267d3f so Policy B holds (distro-* == charly's box/*).

**Diff accounting (2 files):**
1. `charly` — submodule gitlink → f0f3aa58 (the latest main, carrying charly#435's box/fedora bump)
2. `distro-fedora` — submodule gitlink → f267d3f (distro-fedora#39 merged)

The other three distro pins (cachyos/debian/ubuntu) are already at their merged heads on main (opencharly#33 landed them); this PR does not touch them.

## How tested

```
$ git -C charly ls-tree HEAD box/fedora
160000 commit f267d3f... box/fedora
$ git submodule status distro-fedora
 f267d3f... distro-fedora
```

Policy B holds for the changed gitlinks: the umbrella's distro-fedora equals charly's box/fedora. Both source commits are merged, validator-PASS. The unchanged distro pins (cachyos/debian/ubuntu) were verified equal to charly's box pins in opencharly#33.

## Rulebook compliance

| Rule | Status |
|---|---|
| R1 (RCA) | The umbrella lagged charly#435; advancing both gitlinks is the sync |
| R5 (no stale refs) | Only the 2 gitlinks changed |
| R6 (git safety) | feat/ branch, PR-only, no force-push |
| R10 (verification) | ls-tree + submodule status confirm Policy B equality for the changed pins |

## Change classification

- Change class: chore (submodule gitlink sync)
- Verification gate: Policy B equality check
- Attribution tier: analysed on a live system (static gitlink verification; no code path executed)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*