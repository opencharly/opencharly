# 2026.247.1029 — chore(gitlink): advance eval-omarchy to main (9919151 — the Tier-2 eval guide #19 + GPU clone entity #20)

## Summary

Advance the umbrella's `eval-omarchy` submodule gitlink to its default-branch HEAD (`9919151`), which includes the Tier-2 anchored PR eval guide (#19: the eval beds, the generic media pipeline, the PR→hardware-class lane guidance) and the per-PR GPU clone entity (#20).

## How tested

```
$ git ls-files -s eval-omarchy
160000 9919151 0  eval-omarchy
$ git -C eval-omarchy rev-parse origin/main
9919151
```

The gitlink names eval-omarchy's default-branch HEAD (verified). The pre-commit gate passed.

## Rulebook compliance

- **R1** — RCA: the gitlink was stale (the base is `35b61d2`, missing eval-omarchy #19/#20, which landed after the previous sync). Fixed by this advance.
- **R2** — N/A: no cutover; a gitlink advance.
- **R3** — No duplication: the gitlink is the single pointer.
- **R4** — No workarounds: sanctioned gitlink sync.
- **R5** — N/A: no legacy path removed.
- **R6** — Git safety: feat/ branch, no force-push, no direct push to main.
- **R7** — Gate: the pre-commit gate passed; the gitlink resolves to the default-branch HEAD.
- **R8** — The gitlink is the submodule's default-branch HEAD (sync, not pin).
- **R9** — N/A: no R10 bed change.
- **R10** — The validated tree is the committed branch state.

## Change classification

- **Change class:** gitlink sync (chore).
- **Verification gate:** the pre-commit gate + the gitlink resolution.
- **Attribution tier:** analysed on a live system — the gitlink resolution ran live.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*