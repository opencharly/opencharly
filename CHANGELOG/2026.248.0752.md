# 2026.248.0752 — chore: add plugin-cache submodule + sync charly/spec/eval-omarchy gitlinks

## Summary

Add the `plugin-cache` submodule (the new `charly cache` command candy — the git-ref cache operator surface, migrated OUT of core per the architecture-gap fix) + sync the charly/spec/eval-omarchy gitlinks to their merged main heads:

- `plugin-cache` — NEW submodule (opencharly/plugin-cache, main @ 31e07d9): the `charly cache status|clear|refresh|bypass` command, dual-placement (compiled-in + external), operating on spec/refs directly.
- `charly` → f133defcb (charly #539 MERGED: the cache command moved to the plugin; `cache_cmd.go` + the `Cache` CLI field + the `--no-git-cache` flag deleted; plugin-cache wired as a compiled-in command plugin).
- `spec` → c9bbbcce1 (spec #95 MERGED: `refs.GitClient.SetBypass` — the persisted bypass the plugin owns).
- `eval-omarchy` → 8349d9bc2 (eval-omarchy #26 MERGED: the M4 wave).
- `sdk` → 2e9d81d (sdk #207 MERGED: the ssh-fragment write-race serialize fix — the 16-lane eval stall root-fix).

## How tested

```
$ git ls-files -s charly eval-omarchy spec sdk plugin-cache
160000 f133defcbbdd622ef4e9887799d1e39ee23770c8 0  charly
160000 8349d9bc204faf194c656e2013dd302a704e1e29 0  eval-omarchy
160000 31e07d98d12520fa15d49ea7ea095d2280a988ce 0  plugin-cache
160000 c9bbbcce18ea15584331a9791c28601a6d07206d 0  spec
160000 2e9d81da155ed6604ba4c6ad9c7c54bc6bc6db13 0  sdk
$ task verify  # the full pinning gate
```

## Rulebook compliance

- Policy B: the distro-* gitlinks equal charly's gitlinks (check-policy-b OK in the pre-commit gate).
- R6: git-safe; R8/R9: all pins are merged main heads (no PR branches).

Change classification: chore (submodule + gitlink sync). Tier: analysed on a live system (the gitlinks verified against the merged main heads; the full task verify gate is the umbrella's required evidence).

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*