# 2026.253.0720 — chore(umbrella): sync the eval-omarchy submodule pin to the plan-completion gaps

## Summary

Syncs the umbrella's recorded eval-omarchy submodule gitlink to the plan-completion gaps (eval-omarchy#58, merged).

The chain: the NOT_TESTABLE_PR path (a draft/WIP PR never runs a bed — the short factual report renders), the cold-read NO_VALIDATION publish gate (unit-locked upstream plugin-pipeline#14), and the lane metrics. The R10 exercises ran live (the draft lane + the full TESTED chain, pasted in #58).

## How tested (pasted)

```
$ ./scripts/sync-gitlinks.sh
SYNC_EXIT=0 - sync-gitlinks: done
  eval-omarchy -> 1455832

$ git diff --cached --submodule=short eval-omarchy
Submodule eval-omarchy 64c29f6..1455832 (new commits)
```

Only the eval-omarchy gitlink is committed; the other submodules' worktree movements (other sessions' landings) were restored to their recorded shas.

## Rulebook compliance

- R0: git-workflow + the umbrella AGENTS.md loaded.
- R1: the validator's findings (the boolean what, the gitignore patterns) each fixed at the root in the upstream PR.
- R3: only the eval-omarchy gitlink is bumped.
- R4: no workarounds — the upstream fixes are root fixes.
- R6: no force-push, no hook bypass, PR-only landing.
- R7: the sync output + the consumer R10 pasted.
- R9: the pin is a merged ref, never a PR branch.
- R10: the consumer's live exercises (referenced; pasted in #58).

## Change classification

- Change class: chore (submodule pin sync)
- Verification gate: the sync output + the consumer R10
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*