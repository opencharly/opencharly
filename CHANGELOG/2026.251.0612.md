# 2026.251.0612 — feat(agents): the FULL LOOP — every stage grades the previous stage and can trigger a change

## Summary

Implements the FULL LOOP in the four committed omarchy PR-eval agent contracts (opencharly/opencharly plan/eval-omarchy-pi-plugin.md §THE WORKFLOW + the new contract eval-omarchy:eval/references/full-loop.md):

- **Every stage grades the previous stage and can trigger a change**: the runner grades the oracle's config via a mandatory **CONFIG AUDIT** (pr-apply seam present; every PR-specific check present in both beds + known-red diff-ADDED marker at a proven-landing path; the FULL record:/spice: loop; add_candy = only the two plugin providers; the clone targets the PR's CHANNEL golden at lean 2G/1cpu; box validate green) — any failure → **redo-plan**, never an eval on a defective config.
- **Cold-reader**: PROCESS verdict maps findings to triggers (config fit / known-red / tier → redo-plan; incomplete media → redo-read); ACCEPT only when SUBJECT valid + PROCESS clean.
- **Oracle**: on redo-plan, re-authors the charly.yml incorporating EVERY finding (a real diff; identical re-emission = loop-guard violation).
- **Supervisor**: loop guard — redo entrances counted in the ledger; ≥3 → escalate (council), never a silent re-run; every redo round carries the finding evidence + the config/cause diff.
- The 4G-era lane note is superseded: lean = 2G (the committed beds are the record).

## How tested

```
$ ./scripts/check-omarchy-eval-agents.sh
check-omarchy-eval-agents: OK — 5 unique agents tracked + parseable, staged agents ignored
$ git show --stat HEAD | head -8
 .pi/agents/omarchy-cold-reader.md     |  6 ++++++
 .pi/agents/omarchy-config-oracle.md   |  7 +++++++
 .pi/agents/omarchy-eval-runner.md     | 17 ++++++
 .pi/agents/omarchy-eval-supervisor.md | 10 ++++-
$ git status --porcelain .pi/agents/   # empty
(clean)
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | The missing grading/redo between stages was an operator-raised gap — implemented as the full-loop contract + triggers. |
| R2 | N/A. |
| R3 | One contract (eval-omarchy:eval/references/full-loop.md); the agents point at it, never restate. |
| R4 | N/A — the loop is a contract, not a workaround. |
| R4a | N/A. |
| R5 | The old one-way pipe is replaced; no dual paths. |
| R6 | feat/ branch; no force-push. |
| R7 | Agent gate + stat pasted. |
| R8 | N/A. |
| R9 | N/A. |
| R10 | N/A for contract wording; the running batch exercises the loop live. |
| RDD | N/A. |
| ADE | Strengthened: every stage's work is graded before the next. |
| SDD | N/A. |
| B14 | Every finding must be evidence-tied; identical re-emission is explicitly a guard violation. |
| B15 | N/A. |
| B17 | N/A. |
| B18 | N/A. |

## Change classification

- Change class: harness config (agent contracts).
- Verification gate: agent gate + pushed tree; pr-validator.
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*