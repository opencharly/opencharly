# 2026.243.1126 — chore: sync gitlinks — the GUI acceptance suites and two blocking wl verbs

## Summary
Produced by task sync, not hand-pinned. Four gitlinks:

    charly                  a5fdeec1 -> 0f18d857   (its default branch)
    distro-omarchy          2081960d -> 77df17b6   (policy B: exactly what charly pins)
    pod-hyprland            advanced to c29ed062
    layer-cstream-desktop   advanced to 54b47273

Policy B verified independently of the script: distro-omarchy's pin equals THE COMMIT CHARLY
PINS at box/omarchy, not distro-omarchy's latest main.

## What it brings in

    distro-omarchy #12  upstream omarchy's five GUI acceptance suites - session, apps, menu,
                        panels, shell-surfaces - taking check-omarchy-desktop-vm from 16 steps
                        to 99, every assertion a typed check verb
    plugin-wl      #9   wl exec and wl clipboard set never returned for commands that do not
                        exit; both reported failure for operations that had succeeded
    pod-hyprland   #9   with layer-cstream-desktop #11 and layer-omarchy-hyprland #4 - the four
                        ordered repins that kept plugin-wl at ONE version across the closure
    charly         #511 the box/omarchy gitlink and import pin

The wl defects are the substantive ones. exec backgrounded a job but the child inherited
stdout and stderr, so the capture pipe never reached EOF and the verb blocked for its whole
deadline - an app launched, mapped its window and closed on request while the verb that
started it reported DeadlineExceeded. wl-copy does the same, because it stays alive to own the
Wayland selection. A false FAIL for correct behaviour is the worst shape a check verb can take:
it teaches the author to widen the timeout or delete the step.

## Why only four gitlinks and not the 14 task sync moved

task sync advances EVERY submodule to its default-branch HEAD, and this run moved 14. The
other 10 are unrelated to this work and opencharly#66 is in flight covering that ground.

The four committed here are the values task sync produced - not hand-picked - and they are
exactly what Policy B requires. The remaining 10 are left to #66 rather than raced.

## How tested

    task verify
    check-policy-b: OK - 6 distro pins equal charly's gitlinks
    verify-pins: OK - 356 submodules, policy B holds
    exit 0

The umbrella's own pre-commit gate ran on this commit:

```
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

The commit is exactly four files:

```
$ git show --stat HEAD
 charly                | 2 +-
 distro-omarchy        | 2 +-
 layer-cstream-desktop | 2 +-
 pod-hyprland          | 2 +-
 4 files changed, 4 insertions(+), 4 deletions(-)
```

## Rulebook compliance

- **R1 — RCA every anomaly.** Every pin here exists because a failure was root-caused: two
  check verbs that blocked on commands which never exit, a bed whose app-launch steps failed
  for a verb defect rather than a desktop defect, and a plugin-wl version split this work
  opened and then closed at all four links.
- **R2 — Nothing parked.** No "pre-existing" or "out of scope" classification. The nine
  OCR-gated assertions in the ported suites are named in `distro-omarchy/charly.yml` with what
  unlocks them, not silently dropped. The 10 unrelated submodule moves are a DIFFERENT cutover
  already in flight as opencharly#66, named explicitly.
- **R3 — No duplication.** Gitlink moves only.
- **R4 — No workarounds.** These are the values `task sync` produced; committing a subset of a
  sync's output is not a hand-pin, and Policy B is verified against charly's own gitlinks
  independently of the script.
- **R5 — Delete legacy completely.** N/A — pin moves.
- **R6 — Git safety.** `git status` inspected before and after restoring the 10 unrelated
  moves; branched from a fresh `main`; no force-push, no hook bypass, no direct push to `main`;
  the pre-commit gate ran and passed rather than being bypassed.
- **R7 — Prove the gate, not the plan.** `task verify` output pasted verbatim on the final tree.
- **R10 — Fresh disposable proof.** The bed this wires in is **`check-omarchy-desktop-vm`**,
  `disposable: true` in `distro-omarchy/charly.yml` at the pinned commit. Its full R10 run is in
  opencharly/distro-omarchy#12 — `PASS (steps=9)`, `99 steps: 97 passed, 0 failed, 2 skipped` on
  BOTH the live and the post-rebuild pass, zero non-allowlisted warnings, after `[update]`
  destroyed the guest and rebuilt it from nothing. That run was made WITHOUT a repo override, so
  it exercises the pinned tags rather than a local checkout.

## Change classification

| | |
|---|---|
| **Change class** | umbrella gitlink sync — four submodule pins, no file content changes |
| **Verification gate** | `task verify` (Policy B across all 356 submodules) plus the umbrella's own pre-commit gate, both pasted |
| **Attribution tier** | `fully tested and validated` — the pinning gate passed on the final tree, and every repo pinned here was gated in its own PR (R10 bed for omarchy; `go test` + negative controls for plugin-wl; `charly box validate` at zero multi-version warnings for the repin chain) |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*