# 2026.250.0500 — chore(umbrella): sync 14 submodule gitlinks to the Cutover C tags

## Summary

Umbrella sync for the **Cutover C** wave (the `group:`-kind removal + the uniform member-tree IR): bump every stale submodule gitlink to its landed, tag-on-merge CalVer ref. 14 submodules move; no other file changes.

| submodule | new pin | source |
|---|---|---|
| charly | v2026.250.0450 (387c804c) | charly #552 |
| distro-arch | v2026.250.0229 (429a2d7) | distro-arch #34 |
| distro-cachyos | v2026.250.0307 (5bb0eb5) | distro-cachyos #70 |
| distro-fedora | v2026.250.0253 (93dee42) | distro-fedora #49 |
| distro-omarchy | v2026.250.0359 (3427cc4) | distro-omarchy #47 |
| plugin-build | v2026.249.2311 | merged tag |
| plugin-check | v2026.249.2119 | merged tag (nested candy/plugin-check/v0.2026250.0150 carried by charly's go.mod) |
| plugin-deploy-pod | v2026.249.2212 | merged tag |
| plugin-deploy-vm | v2026.249.2224 | merged tag |
| plugin-example-structkind | v2026.249.2023 | merged tag |
| plugin-fleet | v2026.249.2126 | merged tag |
| plugin-loader | v2026.249.2006 | merged tag |
| plugin-preempt | v2026.249.2116 | merged tag |
| plugin-status | v2026.249.2007 | merged tag |

All new pins are merged refs (CalVer tags written by tag-on-merge at each repo's merge) — no PR-branch pins anywhere (rule 5).

## How tested

1. Fetched origin main on the umbrella + all touched submodules; verified all 14 tags resolve and every pin on origin/main was stale (all 14 confirmed stale pre-bump).
2. Bumped each submodule: `git -C <submodule> checkout <tag>` — post-checkout `git -C <sub> rev-parse HEAD` == `git -C <sub> rev-parse <tag>^{commit}` for all 14 (verified programmatically, zero mismatches).
3. `git status --short` after the bump lists EXACTLY the 14 expected submodules and nothing else (sdk / spec / plugin-group / plugin-substrate leftover local checkouts from the authoring session were restored to their committed pins via `git submodule update`; their committed pins were already current).
4. Diff shape: `14 files changed, 14 insertions(+), 14 deletions(-)` — gitlink lines only.
5. R10 gate on the final committed tree (`task verify`, redirect + bounded read):

```
$ task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 400 submodules, policy B holds
```

(A first verify run pre-commit failed as expected — the gate verifies committed gitlinks; it was re-run on the final committed tree per R10 and is green with zero warnings.)

## Rulebook compliance

- **R0**: git-workflow skill loaded before acting; subrepo rulebooks (umbrella AGENTS.md + charly/AGENTS.md) read first.
- **R1**: two anomalies RCA'd inline — (a) pre-commit verify FAIL was the gate reading committed gitlinks while the bumps were unstaged (remedy: commit then verify per R10); (b) a 120s tool timeout killed the first detached verify (remedy: nohup-detached rerun; log retained at /tmp/umbrella-verify-cutover-c.log).
- **R2**: n/a — no repro script involved.
- **R3**: one canonical sync path used (`git -C <sub> checkout <tag>` + the umbrella's own verify-pins gate); no policy re-implemented.
- **R4**: no sleeps/retries/workarounds; the timeout was handled by detaching, not blind-retrying.
- **R5**: n/a — no legacy path removed here (the wave's cutovers live in the pinned repos' own PRs).
- **R6**: `git status` clean before commit; no force-push, no hook bypass (pre-commit gate ran: "pre-commit: OK"), no direct push to main.
- **R7**: `task verify` pasted above — full pinning gate green on the final committed tree.
- **R8**: attribution footer present (fully tested and validated tier — the gate ran on the executed tree).
- **R9**: scope held to pointer sync only; no submodule content edited.
- **R10**: gate executed on the final committed tree (bb59b3b), not an edited working state.
- Umbrella rules 1–8: no submodule content edits (gitlinks only, recorded via the umbrella); submodule git run through `git -C <absolute-path>`; no nested go.work created; no worktrees; only merged refs pinned; Policy B holds (verified: 6 distro pins equal charly's gitlinks); charly's own rulebook untouched by this change; no harness-config files touched.

## Change classification

- **Change class**: submodule pointer sync (gitlink-only, mechanical)
- **Verification gate**: `task verify` (the umbrella's full pinning gate — local, on demand, pasted above)
- **Attribution tier**: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*