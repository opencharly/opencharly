# 2026.256.1257 — feat(agents): sweep the omarchy PR-eval agents onto the generated skill corpus + retire the archived pi-omarchy-eval package

## Summary

Sweeps the four committed omarchy PR-eval agents onto the current eval-omarchy layout.

The lane corpus moved to `skill:` entities in **opencharly/eval-omarchy** (`candy/eval-lane/charly.yml`, generated to `marketplace/distros/skills/`), already live on origin/main (tag `v2026.256.1245`). The entry entity is `omarchy-eval` (renamed from `omarchy-eval-lane` in that repo). The pristine-golden twin is `check-omarchy-pr-<N>-control` (the SAME checks NEGATED, which must PASS to prove the originals known-red) — not the retired `-vm-probe`. The per-PR plans are `pr-beds/pr-<N>/charly.yml` (no `pr-plans/eval-plan-*.json`).

- `.pi/agents/omarchy-{config-oracle,eval-runner,cold-reader,eval-supervisor}.md` — repoint skill paths to the `candy/eval-lane/charly.yml` entities; RED-PROBE state/step → CONTROL (must PASS; an orphan VM now arises on a control FAIL, not by design); `pr-beds/pr-<N>-control/charly.yml` for the twin; the current `media/<calver>/pr-<N>/` + committed `metrics/<calver>-pr-<N>.md` evidence layout, with the raw per-run artifacts named as `.check/<bed>/<calver>/`; drop the JSON plan.
- `.pi/settings.json` — **unchanged**. The archived `pi-omarchy-eval` pin is deliberately NOT removed here: the marketplace `charly-distros` corpus that replaces it is not yet regenerated (that is the named follow-up marketplace step), and removing a loaded dependency before its successor ships is exactly the non-atomic cutover to avoid.

## How tested

Change class: harness project configuration → the repository harness gates + a load-bearing-rename liveness check, on the final tree at head `c86881d`.

**1. Harness gates:**

```
$ bash scripts/check-harness-parity.sh
check-harness-parity: OK — 5 shared harness files identical to charly/

$ bash scripts/check-omarchy-eval-agents.sh
check-omarchy-eval-agents: OK — 5 unique agents tracked + parseable, staged agents ignored
```

**2. The entry entity the R0 step names is live on the owning repo's `origin/main`:**

```
$ git -C <eval-omarchy> show origin/main:candy/eval-lane/charly.yml | grep -n 'name: omarchy-eval$'
13:        name: omarchy-eval
```

**3. R5 sweep — every retired identifier is gone (real output, not a chained echo):**

```
$ grep -rn '\.agents/skills\|-vm-probe\|pr-plans\|probe\|eval/evidence\|mdverdict\|by-design' .pi/agents/ || echo "no matches"
no matches
```

**Head diff (verbatim, at head `c86881d`):**

```
$ git diff origin/main...HEAD --stat
 .pi/agents/omarchy-cold-reader.md     |  8 ++++----
 .pi/agents/omarchy-config-oracle.md   |  8 ++++----
 .pi/agents/omarchy-eval-runner.md     | 20 ++++++++++----------
 .pi/agents/omarchy-eval-supervisor.md | 10 +++++-----
 4 files changed, 23 insertions(+), 23 deletions(-)
```

## Rulebook compliance

- **R1** — the review's malformed `…mdverdict.yml` path, the metrics/raw-artifact conflation, the `by-design-failing` residue, and the entry-name question are all fixed/settled; the sweep output is the real executed output.
- **R2 (atomic cutover)** — the pin removal is deferred until its marketplace successor ships (no premature dependency drop); the agent prose now matches the landed eval-omarchy entities.
- **R3** — the agents point at the one owning entity instead of restating.
- **R4** — no workaround; the sweep is corrected at the source.
- **R5** — retired identifiers swept; the corrected sweep is pasted.
- **R6** — PR-only landing; no force-push.
- **R7** — both repository harness gates run on the final tree and pasted.
- **R8** — N/A — no OCI labels / Containerfile generation.
- **R9** — N/A — no binary/plugin rebuild; agent prose + a config file.
- **R10** — the harness-config class gate is the repository harness gate (parity + agent gate), both run here; no bed can fail on prose.

## Change classification

- **Change class:** harness project configuration (agent definitions).
- **Verification gate:** `scripts/check-harness-parity.sh` + `scripts/check-omarchy-eval-agents.sh` + the load-bearing entry-name liveness check + the R5 sweep, on the final tree at head `c86881d`.
- **Attribution tier:** fully tested and validated.

*Assisted-by: opencode opencode-go/deepseek-v4.1-flash (fully tested and validated)*