# 2026.251.1909 — sync(gitlinks): charly 2e319c96 -> b5ea83a5 + plugin-box d01ad775 -> 18d3831c (F-wave close)

## Summary

F-wave close umbrella sync: move exactly two gitlinks to their repos' merged state.

- `charly`: `2e319c96` → `b5ea83a5` — main advanced through the F-wave merge train
  (charly #563/#565/#570/#571) plus the plugin-box pin-bump PR #572
  (go.mod require `v0.2026250.1030` → `v0.2026251.1834`).
- `plugin-box`: `d01ad775` → `18d3831c` — the F4.1/F4.2/F4.3 cross-cutting dedup
  merge (plugin-box#12). The repo tags `v2026.251.1834` and
  `candy/plugin-box/v0.2026251.1834` both point at exactly `18d3831c`, so this
  gitlink records the very commit charly's pinned go.mod require resolves to
  (charly-box pin reconciliation).

No other gitlinks are moved; the other dirty worktrees in this checkout belong to
other sessions and are left untouched.

## How tested

Policy-B verification (staged gitlink == the recorded commit, per sub repo):

```
$ git -C charly rev-parse origin/main
b5ea83a5c495c22fc17c12ca177c3e257d92363f
$ git ls-tree origin/main charly plugin-box   # (umbrella tree, pre-sync)
160000 commit 2e319c96cc716d1d1a2e3cfea5731d6012d2c6c1	charly
160000 commit d01ad7751fe420e948fd9be650a5f953d046ae07	plugin-box
```

plugin-box tag-to-commit verification (the pin reconciliation):

```
$ git -C plugin-box rev-parse v2026.251.1834^{commit} candy/plugin-box/v0.2026251.1834^{commit}
18d3831cc5e395c74a3f700bac518239c56a001b
18d3831cc5e395c74a3f700bac518239c56a001b
$ git -C charly grep -n "plugin-box/candy/plugin-box" charly/go.mod
15:	github.com/opencharly/plugin-box/candy/plugin-box v0.2026251.1834
```

Staged diff (exactly these two):

```
$ git diff --cached --submodule=log charly plugin-box
Submodule charly 2e319c9...b5ea83a5 (commits not present)
Submodule plugin-box d01ad77..18d3831:
  > refactor(plugin-box): F4.1/F4.2/F4.3 — cross-cutting dedup (Cutover F) (#12)
```

Commit is ONE commit, 2 files changed, +2/−2, on a fresh branch off origin/main.

## Rulebook compliance

- **Policy B**: umbrella gitlinks record each sub repo's merged state; distro-*
  equality with charly's own gitlinks is unaffected (no distro-* moved here), and
  the plugin-box pin reconciliation ensures the record matches what charly compiles.
- **R6**: branch-per-change off origin/main, PR-only, no force-push; only the two
  intended gitlinks staged — the 14 other dirty worktrees are untouched.
- **R1/doc-divergence note**: the go.mod stale pin (the F6 close note) is resolved
  by charly#572 before this sync, so the umbrella record matches reality.

## Change classification

- Change class: umbrella gitlink sync (pointer-only change, no content).
- Verification gate: policy-B verification pasted above; `task verify` (the full
  pinning gate) requires a fresh full 389-submodule clone — the known environment
  limit recorded for this goal.
- Attribution tier: `documentation reviewed` is not applicable (pins/scripts are
  forbidden there); this is a gitlink-pointer change verified by remote SHA
  comparison — `analysed on a live system` (live remotes queried; full gate needs
  the fresh clone).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*