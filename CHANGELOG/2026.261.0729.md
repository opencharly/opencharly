# 2026.261.0729 — chore: sync gitlinks — the jetkvm on-device chain

## Summary

Advances the two umbrella gitlinks that moved after the on-device JetKVM work merged: their `tag-on-merge` CHANGELOG commits landed after the content PRs.

- `layer-charly-check`: `7bce3631a` → `694223f32` (`docs: add CHANGELOG 2026.261.0617 for PR #14`).
- `marketplace`: `c7c9298c8` → `5fea95ca1` (`docs: add CHANGELOG 2026.261.0629 for PR #312`).

Both are the standard every-other-repo default-branch-HEAD pins; no charly-pinned (`distro-*`) pin moves. Produced by `task sync` (`scripts/sync-gitlinks.sh`), not hand-edited.

## How tested

The umbrella's pinning gate, on the committed tree with all submodules initialized:

```console
$ bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 403 submodules, policy B holds
```

The per-commit hook ran on the commit:

```console
pre-commit: OK
```

Both new pins equal their repos' current default-branch HEAD:

```console
$ for p in layer-charly-check marketplace; do
    echo "$p gitlink=$(git ls-files -s "$p" | awk '{print $2}') main=$(gh api repos/opencharly/$p/commits/main --jq .sha)"; done
layer-charly-check gitlink=694223f32 main=694223f32...
marketplace        gitlink=5fea95ca1 main=5fea95ca1...
```

## Rulebook compliance

- **R0 skills:** `/charly-internals:git-workflow` (pinning / `task sync` + `task verify`).
- **R1 RCA + zero warnings:** the drift was root-caused to the merged content PRs' post-merge CHANGELOG commits (the tag-on-merge workflow writes them after the merge, so the umbrella's pin lags by exactly that one commit) — the documented reason a sync follows a chain of merges. No warnings.
- **R2 no out-of-scope:** the two advanced gitlinks are the whole change.
- **R3 no duplication:** produced by the canonical `scripts/sync-gitlinks.sh`; no hand-pin.
- **R4 no workaround:** the pins move to merged refs (default-branch HEADs); no PR-branch or dangling pin.
- **R5 hard cutover + grep:** only the two drifted gitlinks changed (`git status` after `task sync` shows exactly those two).
- **R6 git safety:** a dedicated linked worktree off fresh `origin/main`; the pre-commit hook ran and passed; no force push, no hooks bypass, no direct push to `main`.
- **R7 prove the gate:** `verify-pins.sh` (the `task verify` body) green on the committed tree, pasted above.
- **R8/R9:** N/A — gitlinks only.
- **R10 fresh disposable proof:** the gate ran on the committed worktree with all submodules initialized; no check bed applies (`N/A — pinning change`).
- **Policy B:** unchanged — `6 distro pins equal charly's gitlinks`; only the every-other-repo pins moved.
- **CHANGELOG:** written by `tag-on-merge` from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*