# 2026.242.1253 — feat: add the plugin-mise submodule (builder:mise + verb:mise)

## Summary

Adds the **plugin-mise** submodule (opencharly/plugin-mise, merged + tagged v2026.242.1251) — the charly plugin serving `builder:mise` + `verb:mise` (full mise support per PLAN-mise.md). The umbrella records the gitlink; the plugin is consumed by the charly project closure (charly#487) and the spec externalized-builder maps (spec#81).

## How tested

```
$ git submodule add https://github.com/opencharly/plugin-mise.git plugin-mise
Adding existing repo at 'plugin-mise' to the index
$ git show --stat HEAD
 .gitmodules | 3 +++
 plugin-mise | 1 +
 2 files changed, 4 insertions(+)
```

The plugin repo's own R10 bed (check-mise-pod, disposable: true) is green; the plugin PR merged with validator PASS.

## Rulebook compliance

- R6: git safety — feat/ branch from origin/main, no force-push, PR-only landing.
- R7: gate proven — the submodule points at the merged main of opencharly/plugin-mise (tag v2026.242.1251); `task verify` runs on the final tree.

## Change classification

- Change class: submodule gitlink addition
- Verification gate: gitlink points at a merged, tagged repo
- Attribution tier: analysed on a live system (the plugin repo it records is merged with a green R10 bed)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*