# 2026.250.1744 — feat(agents): commit the omarchy PR-eval pi agents + split .pi/agents gitignore + agent gate

## Summary

Commits the five hand-authored omarchy PR-eval pi agents so the lane pipeline survives on any system (they previously lived only in the gitignored `.pi/agents/`):

- `.pi/agents/omarchy-config-oracle.md`, `omarchy-eval-runner.md`, `omarchy-eval-supervisor.md`, `omarchy-cold-reader.md`, `worker.md` (project override) — now tracked.
- **Frontmatter repair (I1):** `omarchy-eval-runner.md` had a corrupted `tools:` line (truncated list, no closing `---`) that made pi reject the agent entirely — runner lanes silently fell back to the generic `worker`. Restored `tools: fabric_exec, charly_status, gh_pr_status, subagent, subagent_wait` + the standard frontmatter block; the orphaned mission sentence became the body's opening line.
- **`.gitignore` split:** the blanket `.pi/agents/` ignore discarded ALL agent files; now only `.pi/agents/pi-claude-marketplace-*` (regenerated staging from the charly marketplaces by /claude:plugin import) stays ignored.
- **`scripts/check-omarchy-eval-agents.sh` (I6):** the harness-parity script only diffs files identical-by-design with `charly/`; `.pi` is an umbrella-specific fork with no charly twin, so the agent layer gets its own deterministic gate: unique agents tracked, staged agents ignored, frontmatter parseable with no body leakage.

## How tested

```
$ ./scripts/check-omarchy-eval-agents.sh
check-omarchy-eval-agents: OK — 5 unique agents tracked + parseable, staged agents ignored
```

`git diff --cached --stat`: 7 files, 210 insertions(+), 1 deletion(-) — only the intended agent/gitignore/script files.

Fresh-session agent-registry probe (the runner appearing in a session's agent list) lands in the plan's R10 step, together with the `pi -p` smoke ladder.

## Rulebook compliance

- R1: every anomaly dispositioned — the runner frontmatter corruption is a live bug with a deterministic fix + gate; the gitignore over-coverage and the parity-script blind spot got their own fixes instead of workarounds.
- R3: no duplication — the agents stay in one home (this repo); the pi package (plan PR2) deliberately ships skills/prompts only.
- R6: no force-push, no hook bypass; branch `feat/omarchy-eval-agents` from current origin/main.
- R7: the new gate script pasted above; harness config is umbrella-local (no charly twin to diff).

## Change classification

- Change class: harness config + docs (agents are markdown definitions; a shell gate script).
- Verification gate: R7 local gate (script output pasted); full gate = fresh pi session registry probe + `task verify` (both in plan close-out).
- Attribution tier: analysed on a live system (changed paths executed live on this checkout; the fresh-session delivery audit is the plan's R10 step).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*