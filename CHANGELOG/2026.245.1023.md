# 2026.245.1023 — feat(harness): tool-primacy routing in the pi system-prompt append

## Summary

Adds a **Tool primacy** section to `.pi/APPEND_SYSTEM.md` — the pi system-prompt append (85 → 111 lines). One rule: when a tool exists, **always** use it instead of a hand-rolled command (R4). The section is a 12-row routing table — PR/validator status → `gh_pr_status`; bed/image/VM status → `charly_status`; files/search/listing → `pi.read`/`pi.grep`/`pi.find`/`pi.ls`; edits/writes → `pi.edit`/`pi.write`; task state → `todo`; persist/recall → `memory_write`/`memory_search`/`scratchpad`; web → `web_search`/`fetch_content`/`source_check`; heavy work → `subagent`; images → `vision_ask`; symbols → `lsp_*`; unknown tools → `tools.describe`; MCP → only via `mcp.*` after `mcp.$servers` — plus two closers: bash only where no tool covers the job (R1 RCA still applies), and fabric internals (`schema.*`/`state.*`/`mesh.*`/`components.*`/`compact.*`) left alone unless schema mode.

The default pi prompt already carries tool descriptions; this append resolves the umbrella-specific ambiguous choices and forbids shell equivalents — the gap that pushed the agent to `ps`, `gh api`, and `grep -R` one-liners.

## How tested

- **Catalog resolution:** every tool name in the new section checked against the live `tools.list` catalog — 28 refs resolve (catalog/meta/wildcard); the table's *never hand-roll* column intentionally names forbidden shell commands, not tools.
- **Startup smoke (changed path, live):** fresh `pi -p` process in the umbrella root — clean startup, no error signatures:
  `[vision] config: enabled=true visionModel=ollama-cloud/qwen3.5:397b autoHandoff=true cacheMax=50` / `SMOKE_OK`
- **Pre-commit gate on the commit:** `check-policy-b: OK — 6 distro pins equal charly's gitlinks`; `check-harness-parity: OK — 5 shared harness files identical to charly/`.
- **`task verify` on the final committed tree:** `verify-pins: OK — 391 submodules, policy B holds`; `check-policy-b: OK`.
- **`scripts/check-harness-parity.sh`:** `OK — 5 shared harness files identical to charly/`.
- **Tree state:** `git status` clean apart from the single intended file. A pre-existing generated artifact (`charly/candy/docs-corpus/charly.yml`, untracked, from the docs-corpus wiring pipeline) that dirtied the `charly` submodule and blocked the local gate was removed during verification — it was regenerable, untracked content; the gate is green without it.

## Rulebook compliance

- **R3 / R4**: one canonical path per job — the section mandates tool use over shell equivalents (no workarounds, no re-implementation).
- **R6**: fresh `feat/` branch from current `origin/main`; single-file commit; no force-push; no edits inside submodules.
- **AGENTS.md rule 8 (harness parity)**: `.pi/APPEND_SYSTEM.md` is an umbrella-specific fork (hand-reviewed, not in the parity SHARED diff list); shared hook files untouched — parity gate green.
- **R10**: verified from the final committed tree; the changed runtime path (pi system-prompt load) exercised live via the `pi -p` smoke.

## Change classification

- **Class**: harness configuration / runtime guidance (pi system-prompt append)
- **Verification gate**: `task verify` passed on the final tree (391 submodules, policy B holds) + changed path executed live (`pi -p` smoke)
- **Attribution tier**: fully tested and validated

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*