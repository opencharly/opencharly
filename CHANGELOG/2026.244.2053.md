# 2026.244.2053 — feat: add eval-omarchy + omarchy-eval-artifacts as umbrella submodules

## Summary

Two new org repos land as umbrella submodules:

- **`eval-omarchy/`** (renamed from `omarchy-eval`) — the omarchy PR evaluation repo: per-PR check beds (`check-omarchy-suite-pod-pr9332` and friends), the PR-apply candies, and the eval results (`eval/`). Pinned to its default-branch HEAD `63fbea6`.
- **`omarchy-eval-artifacts/`** — the omarchy PR evaluation run artifacts (screenshots, GIFs, run logs). Pinned to its default-branch HEAD `071ed32` (default branch `runs`).

README: new **"Omarchy evaluation"** category in the org map (2 repos), inserted between "Omarchy layers" and "Check & fixture layers". `.gitmodules` carries `branch = main` / `branch = runs` (each repo's default branch), per the every-entry rule.

## How tested

```
$ task verify
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 391 submodules, policy B holds
```

Pre-commit gate (hooks) passed on the commit:

```
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

## Rulebook compliance

- **Rule 1 (no edits inside submodules):** the two repos are added as gitlinks only; nothing inside them is edited from the umbrella.
- **Rule 2 (git ops via `git -C`):** all submodule pinning used `git -C eval-omarchy` / `git -C omarchy-eval-artifacts` from the umbrella root.
- **Rule 5 (pin only merged refs):** both pins are default-branch HEADs (merged refs), never PR branches.
- **Policy B:** unchanged — 6 distro pins still equal charly's gitlinks; the new repos follow the every-other-repo default-branch rule.

## Change classification

- **Change class:** docs + config (submodule registration + README org map)
- **Verification gate:** `task verify` passed on the final tree; pre-commit gate passed
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*