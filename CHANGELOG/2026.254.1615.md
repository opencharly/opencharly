# 2026.254.1615 — chore(umbrella): sync the .github gitlink after the V4.1 docs PR merged

## Summary

Umbrella gitlink sync (policy B): `opencharly/.github` merged the V4.1 gate
docs PR (#90), so the umbrella pins it at its new default-branch HEAD.

```
$ git diff origin/main...HEAD --stat
 dotgithub | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

Only the `.github` gitlink moves (pinned b9173b3f → e7b2d49, the merged
default-branch HEAD); no other pins touched.

## How tested

1. **Pin == remote default HEAD (verbatim, validator finding 1):**

```
$ git -C dotgithub ls-remote origin refs/heads/main
e7b2d490b38f469d2e1f8b3bf5bca64e76da86ba	refs/heads/main
```

The staged gitlink is exactly this SHA (the umbrella tree entry after `git
add dotgithub`).

2. **Gate machinery — the bumped range is PROVEN docs-only (validator finding
2, T4/A2):** the two commits between the pins are the org docs PR #90 and its
tag-on-merge CHANGELOG — no workflow change, the gate mechanism is preserved
byte-for-byte:

```
$ git -C dotgithub log --oneline b9173b3f624c0ab39a31c21cf302e5e66fe41eeb..e7b2d490b38f469d2e1f8b3bf5bca64e76da86ba
e7b2d49 docs: add CHANGELOG 2026.254.1459 for PR #90
002dd17 docs: align the AI_REVIEW_* variable documentation with the live gate (#90)
$ git -C dotgithub diff --stat b9173b3f624c0ab39a31c21cf302e5e66fe41eeb..e7b2d490b38f469d2e1f8b3bf5bca64e76da86ba
 CHANGELOG/2026.254.1459.md | 82 ++++++++++++++++++++++++++++++++++++++++++++++
 README.md                  | 37 ++++++++++++---------
 2 files changed, 103 insertions(+), 16 deletions(-)
```

No `.github/workflows/*` file appears in the range — the validator, dispatcher
and tag-on-merge workflows are untouched by this bump.

3. **Pre-commit gate, run on the commit (verbatim, validator finding 1/4):**

```
$ git commit …
pre-commit: running the umbrella gate…
check-policy-b:   OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

4. **The `verify-pins` worktree line — not a gate divergence on MAIN (validator
finding 4).** The full `scripts/verify-pins.sh` on this dev machine reports its
WORKTREE check for the charly checkout only:

```
$ bash scripts/verify-pins.sh
FAIL: charly: checked-out HEAD e19198ca80… != gitlink d507ca5a27…
```

That worktree check is NOT part of the umbrella pre-commit gate (which is
policy-b + harness-parity, both green above) and reflects the charly dev
checkout sitting on a worker branch. The umbrella MAIN tree carries the
correct pin (no divergence on main):

```
$ git ls-tree origin/main charly dotgithub
160000 commit d507ca5a27eeb35f4001b8af89fd94eb5cfdcccd	charly
160000 commit b9173b3f624c0ab39a31c21cf302e5e66fe41eeb	dotgithub
```

The worktree exception is documented in `scripts/sync-gitlinks.sh` itself
("Never pins a PR branch — only merged refs") and the README pinning policy;
the charly gitlink d507ca5a on main is the merged wave HEAD. The exit this PR
advances: the sync itself — this PR brings the `.github` pin to its merged
default-branch HEAD, the same mechanism that keeps every pin (charly
included) correct at each wave.

## Rulebook compliance

- **R0** — `git-workflow` skill; PR-only landing, no direct push.
- **R1** — no anomaly surfaced by this change; the worktree/gitlink state is
explained above with evidence (not dismissed): main pins are correct, the
worktree difference is the documented dev-checkout exception, and this PR is
the sync exit.
- **R2** — no parking: this PR advances its own named batch (the pin sync)
and names the mechanism for every other pin.
- **R3** — single pin source: the default-branch HEAD, per `scripts/sync-gitlinks.sh`.
- **R4** — no workaround: the pin is machine-derived (`ls-remote`), not a
hand value.
- **R5** — no residue: the old pin is replaced in the same commit (diff above).
- **R6** — `git status` inspected; only `dotgithub` staged (the other submodule
worktree rows are the dev checkouts, unstaged).
- **R7** — the umbrella gate (policy-b + harness-parity) ran green on the commit
(pasted); the range is docs-only (pasted).
- **R8/R9/R10** — N/A (gitlink only; no generation/artifact/runtime path).

## Change classification

- **Change class:** config (gitlink pin sync) — the whole bumped range is
proven documentation (README + CHANGELOG; range diffstat pasted).
- **Verification gate:** pin == `ls-remote` default HEAD (pasted); range
proven docs-only (pasted); gate scripts green (pasted).
- **Attribution tier:** `documentation reviewed` — the entire committed range
of the bumped submodule is documentation, the pin value is machine-derived,
consistent with the footer.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (documentation reviewed)*