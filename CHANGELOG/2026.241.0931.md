# 2026.241.0931 — chore: sync 29 submodule gitlinks (policy B) — lands the skill corrections

## Summary of changes

Routine Policy B pin sync, produced by `task sync` (`scripts/sync-gitlinks.sh`) — no hand-pinning.
Its substance is that it brings the umbrella's view up to the corpus that now carries this
session's skill corrections.

29 gitlinks advance. The load-bearing three:

| Submodule | Now | Why it matters |
|---|---|---|
| `marketplace` | `1d47c2fe` | The PUBLISHED corpus carrying 23 corrected `SKILL.md` files (`marketplace#240`) |
| `charly` | `165b22a0` | Carries #440 (generation gates), #441 + #444 (pins), #442 (candy skills) |
| `plugin-marketplace` | `0fc9ae45` | Carries the `MARKETPLACE_REF` advance (`plugin-marketplace#4`) |

The remaining 26 are `plugin-*` gitlinks the same sweep advanced.

## Change class

**candy/box-config** — submodule gitlinks only (29 files, one line each). No source, no schema,
no workflow, no harness config. Single repo.

**Closes / relates to:** brings the umbrella's view up to `opencharly/marketplace#240`,
`opencharly/charly#{440,441,442,444}` and `opencharly/plugin-marketplace#4`, all merged. No
ordering dependency remains — every pinned commit is already on its repo's `main`.

**Breaking change + rollback:** none — gitlinks advance to commits already on each submodule's
default branch. Rollback is a plain revert of the single commit.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** `task verify` — the umbrella's full pinning gate (every pin
  reachable and clean, the 344-remote branch audit, Policy B equality), plus the per-commit hook
  gate. Per the umbrella rulebook R7 this IS the gate, run locally on demand; there is no CI gate
  for it.
- **Eval beds run:** `N/A (gitlink sync)` — no `disposable: true` bed maps to a pin advance in the
  umbrella; it builds nothing and deploys nothing.
- **Fresh rebuild:** `N/A (no binary involved)` — the umbrella carries no Go source; it is a view
  of the org.
- **Did the CHANGED code path execute live?** **Yes, in the sense that applies here** — the pins
  were produced BY the sync script rather than typed, `task verify` resolved all 344 submodules
  against them, and the marketplace pin was independently confirmed to be the published corpus
  commit whose corrected skills a real session now loads (block 2c).
- **Concurrency:** `N/A` — gitlinks; no shared state, lock, ledger or lifecycle.

*1. Fresh-rebuild gate output* (`charly version` + the gate run, at zero warnings):
```
N/A — no binary is built by the umbrella. The applicable gate, on the FINAL COMMITTED tree:

$ task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 5 distro pins equal charly's gitlinks
verify-pins: OK — 344 submodules, policy B holds
task: [verify] node --disable-warning=ExperimentalWarning scripts/check-umbrella-gates-syntax.mjs
OK: …/.pi/extensions/umbrella-gates.ts parses (node type-strip pass)
TASK_VERIFY_EXIT=0

Per-commit hook gate on the same commit:
pre-commit: running the umbrella gate…
check-policy-b: OK — 5 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK

Zero warnings.
```

*2. Each changed code path executing* (the lines proving the changed branch ran, not that it built):
```
(a) The pins were GENERATED, not hand-written:

$ task sync                       # scripts/sync-gitlinks.sh
  … plugin-vm -> 31cac00
  … plugin-wl -> def669d
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'

(b) Each load-bearing pin equals its submodule's upstream default-branch HEAD — checked against
    the live remote, not against a local checkout:

  marketplace          staged=1d47c2fe  upstream-main=1d47c2fe  MATCH
  charly               staged=165b22a0  upstream-main=165b22a0  MATCH
  plugin-marketplace   staged=0fc9ae45  upstream-main=0fc9ae45  MATCH

(c) The marketplace pin is the commit that actually carries the corrections — verified in the
    PUBLISHED corpus at that commit, by grepping for strings the fixes REMOVED (expect 0) and
    ADDED (expect the stated count):

  vm/skills/vm/SKILL.md              'CHILD NODE'           = 0   (dead child-node form gone)
  local/skills/local-deploy/SKILL.md 'ssh_args'             = 0   (wrong key gone)
  core/skills/logs/SKILL.md          '-n, --lines'          = 0   (rejected flag gone)
  build/skills/settings/SKILL.md     'CHARLY_BUILD_ENGINE'  = 1   (real env var present)
  check/skills/wl-overlay/SKILL.md   'method: overlay-show' = 11  (all examples converted)
  check/skills/libvirt/SKILL.md      'LibvirtVerbInput'     = 1   (correct def name)

(d) End-to-end proof that this pin changes what an agent is actually told — six fresh headless
    sessions, each asked a question whose WRONG answer is the old skill text:

  Q: on a kind:vm entity, is libvirt inline or a child node?
  A: "INLINE under vm: … the former named child-node shape was deleted … and is now a hard
      loader error"                                                                   CORRECT
  Q: exact YAML key for extra ssh args on a local deploy?
  A: "ssh_arg (singular), inline … not in a separate child block"                     CORRECT
  Q: which flag tails N lines with charly logs?
  A: "No such flag exists … -f/--follow and -i/--instance, but no tail-count option"  CORRECT
  Q: which env var overrides engine.build, and its default?
  A: "CHARLY_BUILD_ENGINE … defaults to auto"                                         CORRECT
  Q: do wl overlay fields go inside the wl: map or as step siblings?
  A: "inside the wl: map … A plan is an ordered list, never a name-keyed map"         CORRECT
  Q: list the charly- skills available
  A: 85 skills                                                                        CORRECT

    Before these corrections each of those answers would have been the opposite.
```

*3. Docs-only evidence* (the R5 grep self-test result + the cross-reference/markdown review):
```
N/A — not a docs-only change: the diff is 29 submodule gitlinks, and no `*.md` is touched.
```

## Attribution tier

**fully tested and validated** — `task verify` passes on the final committed tree (344 submodules,
Policy B holds, zero warnings), the pins were produced by the sync script and each load-bearing
one confirmed equal to its upstream default-branch HEAD, and the corpus the marketplace pin
selects was proven — both by grep and by six live headless sessions — to serve the corrected
guidance.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** the umbrella dispatcher routes pinning / `task sync` / `verify-pins` work to
  `/charly-internals:git-workflow`, which was loaded before the first git call and diffed against
  the current corpus commit before being followed. Landed as a `feat/` branch + PR; no direct push
  to `main`.
- **R1 RCA + zero warnings:** the stale pin was not assumed from dates — the umbrella's
  `marketplace` gitlink was compared against the live remote HEAD before syncing, and the synced
  value was confirmed to be the published-corpus commit. Gate output has zero warnings.
- **R2 no out-of-scope:** this is the umbrella-side completion of the corrections; without it this
  repo's view still points at a corpus predating them. Nothing surfaced here is parked. The one
  open item from this session's work — `opencharly/charly#443`, the ten boxes declaring
  `init: supervisord` with no binary — lives in charly and is a named batch with its own
  build-based gate, not an umbrella pin concern.
- **R3 no duplication:** the pins are produced by the single canonical script; no policy is
  re-implemented here.
- **R4 no workaround:** produced by `task sync`, never hand-pinned — which the umbrella rulebook
  names explicitly as the difference between a sync and a workaround.
- **R5 hard cutover + grep:** `N/A — purely value advances; no identifier removed or renamed.`
- **R6 git safety:** `git status` + `git stash list` checked before branching; branch cut from
  freshly fetched `origin/main`. A `git stash` around the branch switch turned out to be a NO-OP
  (git does not stash submodule gitlink changes), so per the git-workflow skill's warning about
  stash/pop round-trips silently un-staging work, the staging was re-verified before committing —
  all 29 entries still staged (`M ` in the first column), none demoted to unstaged. No force-push,
  no history rewrite. Post-commit status clean; `git show --stat` lists exactly 29 files.
- **R7 runtime gate:** `task verify` — the real pinning gate — ran on the final committed tree,
  not a green `git status`.
- **R8 artifact invariants:** the artifact this sync selects (the published corpus at the pinned
  marketplace commit) was inspected rather than assumed — block 2c/2d.
- **R9 binary == source + deps:** `N/A — the umbrella deploys no binary and adds no runtime dep.`
- **R10 disposable + coverage:** no bed maps to a gitlink sync, so this branches to `task verify`
  on the final committed tree. Coverage that would fail without this change: with the old
  `marketplace` pin this repo's view resolves the PRE-correction corpus — block 2c shows the
  corrected strings exist only at the new commit.
- **RDD / ADE / SDD:** the high-risk assumption — "the new marketplace pin actually serves
  corrected skills" — was proven by executing six live headless sessions against it (2d), not
  inferred from the merge. ADE / SDD: `N/A` — no candy authored, no `.cue` edited.
- **Hard cutover:** ONE atomic commit; all 29 pins move together, so no intermediate state mixes
  pre- and post-correction views.
- **Kernel/plugin boundary law:** `N/A — no core or sdk code; the umbrella is a view of the org.`
- **Disposable-only autonomy:** `N/A — nothing destroyed or rebuilt; no deploy target touched.`
- **Clean architecture + Go gates:** `N/A — no Go source in the diff.`
- **CHANGELOG:** `N/A` — the umbrella's `tag-on-merge` writes `CHANGELOG/<CalVer>.md` from this PR
  body at merge and mints the tag; an author-staged CalVer file is the deprecated flow and would
  leave an orphan entry with no matching tag.

---

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*