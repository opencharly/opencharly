# 2026.251.1126 — feat(agents): FAIL-HARD CONTRACT — stop, preserve evidence, RCA-ready report, fail loudly

## Summary

FAIL-HARD CONTRACT for the eval-omarchy worker agents (docs-only, 2 files, +4/-0):

- **omarchy-eval-runner.md** (+3): every lane MUST stop on ANY failure (runtime, config, infra, lock, build), preserve every artifact, write an RCA-ready block, and fail loudly — never idle, never continue past an unresolved failure, never blind-retry, never fake progress.
- **omarchy-eval-supervisor.md** (+1): the CONFIG AUDIT includes a RUNTIME vm-build check (a config that validates but fails at runtime is a hard FAIL); mirrors the runner contract.

Superseded: the earlier `45968a8` path refs (`.agents/skills/omarchy-eval-*` for cold-reader/config-oracle) are already in main — not part of this PR's net diff (verified: head-vs-base = 2 files, +4/-0). Measured driver: the new-workflow verification run hit the clone-entity vm-build rejection and stopped with evidence — this contract makes that behavior the rule. Mirrored in the lane skills (eval-omarchy#42, merged).

## How tested

```
$ git diff --stat origin/main...HEAD
 .pi/agents/omarchy-eval-runner.md     | 5 +----
 .pi/agents/omarchy-eval-supervisor.md | 4 +--
 2 files changed, 4 insertions(+), 0 deletions(-)
$ grep -c 'FAIL-HARD CONTRACT' .pi/agents/omarchy-eval-runner.md .pi/agents/omarchy-eval-supervisor.md
2
$ ./scripts/check-omarchy-eval-agents.sh
check-omarchy-eval-agents: OK — 5 unique agents tracked + parseable, staged agents ignored
```

Note: the diff-stat above reflects the CURRENT head-vs-base (the path-ref commits are in main).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | The verification failure drove the FAIL-HARD contract. |
| R2 | N/A. |
| R3 | One contract, mirrored in the skills (eval-omarchy#42, merged). |
| R4 | The fix is the remedy — hard failures enable RCA instead of idling. |
| R4a | N/A. |
| R5 | N/A. |
| R6 | feat/ branch; update-branch applied. |
| R7 | Agent gate + greps + diff pasted (matching head). |
| R8 | N/A. |
| R9 | N/A. |
| R10 | N/A for wording; exercised live on the verification runs. |
| RDD | Driven by the measured runtime failure. |
| ADE | N/A. |
| SDD | N/A. |
| B14 | Failures are evidence-preserving + reported; no invented progress. |
| B15 | N/A. |
| B17 | N/A — prompt text. |
| B18 | N/A. |

## Change classification

- Change class: agent-contract wording (2 files).
- Verification gate: agent gate + greps + diff (pasted, matching the head).
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*