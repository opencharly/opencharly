# 2026.245.0934 — chore: curate harness plugins to charly-dev + omarchy-eval, drop all MCP servers

## Summary

Cure the umbrella harness so pi and Claude Code load ONLY the skills needed for charly development and full omarchy evaluation, and NO plugin that configures an MCP server:

- **Removed the four MCP-server plugins** whose servers were auto-registered into `.pi/mcp.json`: `charly-coder` (charly MCP :18765), `charly-jupyter` (jupyter MCP :8888), `charly-selkies` (chrome-devtools MCP :9224), `charly-versa` (marimo MCP :22718) — from both `.pi/claude-plugins.json` and `.claude/settings.json` `enabledPlugins`. `.pi/mcp.json` now holds `{"mcpServers": {}}`.
- **Removed the product-box plugins** irrelevant to charly dev / omarchy eval: agentteams, comfyui, filebrowser, hermes, immich, ollama, openclaw, openwebui, plus the official claude-md-management and gopls-lsp (and their `claude-plugins-official` marketplace entry).
- **Kept 14 plugins** whose skills serve charly development and full omarchy evaluation: automation, build, check, core, distros, image, infrastructure, internals, kubernetes, languages, local, pod, tools, vm.
- **Omarchy coverage verified by deep-dive** of all omarchy box definitions (distro-omarchy: omarchy, omarchy-cstream, omarchy-suite-base/‑red/‑pr9343, omarchy-migration-harness/‑red, omarchy-pacstrap-builder, charly-mise-builder + eval-omarchy pr9332) and every bed's check verbs (`wl:` 76×, `quickshell:` 20×, run/command steps, `ssh:`, `libvirt:`, `omarchy:`). The family skills omarchy / omarchy-cstream / omarchy-eval (distros) and omarchy-vm (vm) stay loaded; the bed verbs wl / quickshell / libvirt stay loaded via charly-check.
- **Layer skills arrive via opencharly/marketplace#270** (opened in this same session): the refs list gains layer-omarchy-base + layer-omarchy-shell pins, projecting the already-authored `omarchy-base` and `omarchy-shell` skills (regenerated corpus, drift-clean).
- `.pi/mcp.json` is a pi-claude-marketplace generated artifact → added to `.gitignore`.
- HARNESS-PARITY.md Claude-settings row updated to describe the curated fork (doc-vs-reality).

## How tested

- `pi -p` non-interactive startup (the requested validation) — exact output:

```
[vision] config: enabled=true visionModel=ollama-cloud/qwen3.5:397b autoHandoff=true cacheMax=50
1. No startup errors — memory snapshot loaded cleanly at 2026-09-02 11:12:08, no warnings in context.
2. 176 /charly-* skills available (all of the pi-claude-marketplace corpus); e.g. git-workflow, strict-policy, root-cause-analyzer, go-quality, vm, deploy.
3. Cannot verify MCP servers without a tool call — context shows captured extensions (pi-memory, charly_status, gh_pr_status) but no evidence of charly/jupyter/chrome-devtools/marimo servers being registered.
```

- Reconcile state after the config change (pi-claude-marketplace applied state):

```
$ cat .pi/mcp.json
{ "mcpServers": {} }

$ (state.json) plugins: 14
charly-automation, charly-build, charly-check, charly-core, charly-distros, charly-image,
charly-infrastructure, charly-internals, charly-kubernetes, charly-languages, charly-local,
charly-pod, charly-tools, charly-vm
claude-plugins-official present? false
```

- `bash scripts/check-harness-parity.sh` → `check-harness-parity: OK — 5 shared harness files identical to charly/`.
- Pre-commit gate (policy B + parity) passed on the commit.
- `bash scripts/verify-pins.sh` (task verify, full pinning gate) → `verify-pins: OK — 391 submodules, policy B holds` (exit 0; check-policy-b OK).

## Rulebook compliance

- Rule 8 (harness parity): settings are a documented fork in HARNESS-PARITY.md; shared-by-design files (hooks, .reasonix) untouched and diff-checked.
- R7/R10: verify gate run on the final committed tree before opening this PR.
- No submodule edits (rule 1), no pin changes, no go.work changes.
- PR-only landing, feat/ branch from current origin/main, no force-push.

## Change classification

- Change class: docs/config (harness plugin curation + docs row)
- Verification gate: full (task verify) — attribution tier: fully tested and validated
- The omarchy layer-skill corpus change is its own named PR: opencharly/marketplace#270 (opened this session).

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*