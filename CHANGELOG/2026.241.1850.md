# 2026.241.1850 — feat(pi): add pi-web-ui@0.50.0 to the project config

## Summary

Adds `npm:pi-web-ui@0.50.0` to the project pi config (`.pi/settings.json`) — the browser cockpit for pi (chat UI, files, terminal, model management) via the `/webui` slash command. Proper pi package: `extensions/webui.ts` registers the command; `dist/server/index.js` is the spawned server. No tool-name collisions — it registers a slash command, not tools.

## How tested

```
$ pi install npm:pi-web-ui@0.50.0 -l
Installed npm:pi-web-ui@0.50.0

$ pi list
  npm:pi-web-ui@0.50.0
    /…/.pi/npm/node_modules/pi-web-ui

$ pi -p --no-session "Reply with exactly: WEBUI_LOAD_OK…"
WEBUI_LOAD_OK          # exit 0, no duplicate-tool errors
```

Command registration + server entry verified directly in the installed package (slash commands are not visible to the model in print mode):
```
$ grep -n "registerCommand" .pi/npm/node_modules/pi-web-ui/extensions/webui.ts
101:  pi.registerCommand("webui", {
$ ls .pi/npm/node_modules/pi-web-ui/dist/server/index.js
.pi/npm/node_modules/pi-web-ui/dist/server/index.js   # server entry OK
```

Pre-commit gate on the final tree:
```
check-policy-b: OK — 5 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

## Rulebook compliance

| Rule | Status |
|---|---|
| R6 (git safety) | `feat/` branch, push only, no force-push |
| R7 (gate proven) | Live `pi -p` load test + direct registration check pasted above |
| R10 (fresh tree) | Tests run from the committed tree |
| Rule 8 (harness parity) | `.pi/settings.json` not in parity `SHARED` list; `check-harness-parity` OK |

## Change classification

- **Change class:** harness config (pi project config + docs)
- **Verification gate:** live `pi -p` load + installed-package source check + pre-commit gate
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*