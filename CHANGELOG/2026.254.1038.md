# 2026.254.1038 — chore: sync docs gitlink to the post-drift-fix main (1a37f2ee)

## Summary

Follow-up sync: the umbrella's docs gitlink to the post-drift-fix docs main. The #191 sync pinned docs at `91ce25f0`; the docs drift-fix landing (docs#116) moved main to `1a37f2ee` (regenerated CI-equivalent tree + tag-on-merge). One targeted gitlink bump: docs `91ce25f0` → `1a37f2ee5e92cf4926103695b6a51404a2a3a3b0` (== docs remote main HEAD, verified via ls-remote --symref before the bump).

## How tested

From the pushed tree (branch feat/sync-docs-driftfix @ 133c24ae):

```
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 400 submodules, policy B holds
```

Diffstat = `docs | 2 +-` (1 file, 1 insertion, 1 deletion); working tree clean after push; `task hooks` pre-commit gate passed (policy B + harness parity).

## Rulebook compliance

- Policy B — verified by `task verify`; manual targeted gitlink bump per the repo's own convention, no hand-pin drift.
- R6/Git — no force-push, no direct-main push; single-purpose commit.

## Change classification

- **Change class:** submodule gitlink sync (view update).
- **Verification gate:** `task verify` (policy B + 400 pins) on the final tree.
- **Attribution tier:** analysed on a live system — the sync + gate ran live with retained output.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*