# 2026.253.1314 — chore(umbrella): sync the eval-omarchy submodule pin to the corpus Wave 4b batch

## Summary

Syncs the umbrella's recorded eval-omarchy submodule gitlink to the corpus Wave 4b batch (eval-omarchy PR #66 MERGED): the battery/bluetooth/clipboard model scans, the tightened cups unit check (installed AND enabled), the default-browser surface, and the honest PARTIAL drop of the sudo-gated security posture.

## How tested (pasted)

```
$ ./scripts/sync-gitlinks.sh
SYNC_EXIT=0 - sync-gitlinks: done
```

Only the eval-omarchy gitlink is bumped; the pins are the merged refs (R9).

## Rulebook compliance

- R0: git-workflow + the umbrella AGENTS.md loaded.
- R3: only the eval-omarchy gitlink is bumped.
- R6: no force-push, no hook bypass, PR-only landing.
- R9: the pin is a merged ref, never a PR branch.
- R10: the fix's live proof pasted in eval-omarchy#66 (referenced).

## Change classification

- Change class: chore (submodule pin sync)
- Verification gate: the sync output
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*