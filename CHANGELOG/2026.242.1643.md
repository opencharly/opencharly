# 2026.242.1643 — chore(pi): slim project config to bare system prompt + plugins

## Summary

Slim the pi project config (`.pi/`) down to its bare system prompt + plugins config, per operator request. Removed every non-pi, non-plugin addition:

- **Extensions** (all four): `umbrella-gates.ts` (incl. `umbrella_load_skills`), `github-pr-status.ts`, `umbrella-verify.ts`, `loop-protection.ts`
- **Custom prompts** (`prompts/*`), **custom subagent** (`agents/validator.md`), `APPEND_SYSTEM.md`, `.pi/README.md`
- `.pi/settings.json` now carries only the package (plugin) pins; `.pi/fabric.json` only the keepVisible list for package tools

The gates-extension removal is a cutover (R5): `scripts/check-umbrella-gates-syntax.mjs` and its wiring in `hooks/pre-commit` + `Taskfile.yml` are deleted, and the `AGENTS.md` / `README.md` / `HARNESS-PARITY.md` claims about pi gate wiring are swept. Git-level gates (`hooks/pre-commit`, `.claude/hooks/*`) are unchanged and still enforced.

## How tested

```
$ git commit -F /tmp/pi-slim-commit-msg.txt
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK

$ git show --stat --oneline HEAD | head -30
b0a67d4 chore(pi): slim project config to bare system prompt + plugins
 21 files changed, 9 insertions(+), 1382 deletions(-)

$ git grep -l "umbrella-gates\|check-umbrella-gates-syntax" -- . ":(exclude)CHANGELOG"
NONE

$ task verify
task: [verify] bash scripts/verify-pins.sh
FAIL: charly: dirty working tree
task: Failed to run task "verify": exit status 1
```

The modified `verify` task executes live (verify-pins.sh runs; the removed syntax-check step is gone from the task). It fails on the submodule-cleanliness assertion because the charly/plugin-mise/sdk/spec working trees carry the in-progress work of the **named thematic batch** `plan/mise-followup-todo.md` — items 2 (plugin-mise builder refactor), 3 (charly vocabulary improvements), and 5 (umbrella gitlink sync: sdk → f476722d, spec → 90c1b7c7). This PR does not advance that batch; the dirty trees are routed to it by name and will be cured by its PRs.

## Rulebook compliance

- R1 — every stale reference to the removed extension swept in the same commit (AGENTS.md, README.md, HARNESS-PARITY.md, hooks/pre-commit, Taskfile.yml, the syntax-check script)
- R2 — the dirty submodules are not parked as "pre-existing": they are the in-progress working trees of the named batch `plan/mise-followup-todo.md` (items 2/3/5), which this PR routes them to
- R5 — dead gate script deleted, not left behind
- R6 — no force-push, no direct push to main; feat/ branch + PR only
- No edits inside submodules; the submodule dirtiness belongs to the named batch above

## Change classification

- **Change class:** config + gate-wiring cutover (not docs-only — scripts/hooks touched)
- **Verification gate:** pre-commit gate green (policy-B + parity); `task verify` ran live and fails only on the parked submodule cleanliness (routed to the named batch)
- **Attribution tier:** analysed on a live system — changed paths ran live (commit + gate + verify task), full gate not green due to the parked batch

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*