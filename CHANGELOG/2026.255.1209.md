# 2026.255.1209 — feat: auto-load marketplace skills in opencode config

## Summary

Wire the umbrella opencode config to auto-load the full marketplace skill corpus. `opencode.json` gains:

```json
"skills": {
  "paths": ["marketplace"]
}
```

so opencode's skill loader recursively discovers all 319 `SKILL.md` files directly from the `marketplace/` submodule checkout — no copies, no symlinks, no edits inside any submodule. Previously the project config declared no skill source, so an opencode session surfaced only the built-in skill and agents had to read `SKILL.md` files by path as a fallback.

## How tested

`task verify` (exit 0, run on the final tree with the change in place):

```
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 400 submodules, policy B holds
```

`task harness` (exit 0):

```
task: [harness] bash scripts/check-harness-parity.sh
check-harness-parity: OK — 5 shared harness files identical to charly/
```

Live changed-path execution — `opencode debug config` (exit 0, no `ConfigInvalidError`; opencode hard-fails on invalid config), merged config contains:

```
"skills": {
  "paths": [
    "marketplace"
  ]
}
```

Live changed-path execution — `opencode debug skill` (exit 0): **320 skills loaded** (319 from `marketplace/...` + 1 built-in `customize-opencode`), all with descriptions, including `git-workflow` and `root-cause-analyzer`.

Skill-corpus pre-scan (same `**/SKILL.md` walk the loader performs): 319 files found, 0 unloadable (every file carries `name` + `description` frontmatter with `name` matching its folder, so none is filtered out).

Post-commit check: `git status --short` clean on the branch, `git show --stat HEAD` lists exactly `opencode.json | 3 +++`.

N/A — harness-config change, no runtime target: no disposable bed and no fresh rebuild apply.

## Rulebook compliance

- Rule 1 (never edit inside a submodule): only umbrella-root `opencode.json` changed; `marketplace/` entered read-only (`git status` inside it clean).
- Rule 2 (git op rule): all git driven from the umbrella root; no worker rooted in a submodule.
- Rule 8 (harness parity): `opencode.json` is a fork by design (not in the shared diff list); `task harness` still green, no silent fork.
- R3 (no duplication): one skill source (the marketplace checkout), no copied/symlinked farm.
- R6 (git safety): status checked before acting; `feat/` branch, no force-push, no direct push to `main`, PR-only landing.
- R7 (prove the gate): `task verify` + `task harness` outputs pasted above, run on the final tree; the changed path executed live via `opencode debug`.
- Cross-PR sweep: only other open PR at the time was #198 (`chore: sync gitlinks`, gitlink-only paths) — zero file overlap, no coordination needed.

## Change classification

- Change class: harness-config only (`opencode.json` at the umbrella root; no code, no pins, no submodule content, no permission-rule changes).
- Verification gate: `task verify` (pinning gate) + `task harness` (parity) + live `opencode debug config` / `opencode debug skill` runs against the changed path.
- Attribution tier: fully tested and validated (`task verify` passed on the final tree; changed path executed live).

*Assisted-by: opencode opencode-go/deepseek-v4.1-flash (fully tested and validated)*