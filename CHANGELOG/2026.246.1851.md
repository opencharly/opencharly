# 2026.246.1851 — chore(gitlinks): sync 11 submodule pins to upstream heads

## Summary

Sync 11 umbrella submodule gitlinks to their upstream default-branch heads per policy B (`task sync` / `scripts/sync-gitlinks.sh`):

- `charly` → its default-branch HEAD (rolling)
- `layer-asciinema`, `layer-charly-check`, `layer-charly-openclaw`, `layer-gogcli`, `layer-openclaw-full`, `marketplace`, `plugin-record`, `plugin-vm`, `pod-charly-automation`, `pod-openclaw` → their own default-branch heads

No distro-* pins changed (they follow charly's gitlinks, which did not move in this sync).

## How tested

- `task sync` → EXIT=0, 11 gitlinks bumped.
- `task verify` → GREEN:
```
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 394 submodules, policy B holds
```
- Initial verify failure was a checkout-state artifact (charly's nested `box/*` submodules uninitialized after top-level init without `--recursive`); fixed with `git -C charly submodule update --init`, then verify passed. Not a pin error.
- Pre-commit gate passed.

## Rulebook compliance

| Rule | Status | How / N/A |
|---|---|---|
| 1. Never edit inside a submodule | HOW | Only umbrella gitlinks changed (11 submodule pointer entries) |
| 2. Git op rule | HOW | All submodule git ops via `git -C <path>` from the umbrella root |
| 3-4. Go/worktrees | HOW | Work done in `.claude/worktrees/gitlink-sync` (per B1/B4) |
| 5. Pin discipline | HOW | Only merged refs pinned (default-branch heads); no PR branches |
| 6. Git safety | HOW | No force-push, no hook bypass; pre-commit gate passed |
| 7. Prove the gate | HOW | `task verify` green on the final tree (pasted above) |
| 8. Harness parity | N/A | No shared harness file touched |
| R10. Fresh proof | HOW | Verify run from the committed worktree tree |

## Change classification

- **Change class:** configuration (submodule gitlink sync)
- **Verification gate:** pr-validator
- **Attribution tier:** fully tested and validated — `task verify` passed on the final tree

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*