# 2026.241.1815 — chore: sync submodule gitlinks (policy B) — catch up dotgithub + marketplace with upstream

## Summary

Policy-B submodule gitlink sync (catch up with upstream main). Advances two pins that landed new upstream work since the last sync (opencharly#38):

- **dotgithub → d0e33ff (v2026.241.1625)** — opencharly/.github#80: `branch-protection.sh` now applies + verifies the repo-level `allow_auto_merge` setting (the org-wide validator auto-merge gap fix).
- **marketplace → 3e8caea (v2026.241.1752)** — marketplace#246–#250: hook-script cleanup, marketplace-verb resolution from this repo, refs list refresh, and the refresh-workflow org-bot-token fix.

## How tested

```
$ charly box validate
exit=0, 0 validation errors
$ pre-commit gate: check-policy-b OK (5 distro pins equal charly's gitlinks), check-harness-parity OK
```

## Rulebook compliance

| Rule | Status |
|---|---|
| R4 (no workarounds) | `task sync`-driven pin advance, not a hand-pin |
| R6 (git safety) | `feat/` branch, push only, no force-push |
| R7 (gate proven) | validate exit 0 on the final tree |
| R10 (fresh tree) | validate run from the committed tree |

## Change classification

- **Change class:** pin sync (2 submodule gitlinks, policy B)
- **Verification gate:** `charly box validate` full pass + policy-B check
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*