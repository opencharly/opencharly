# 2026.246.0749 — chore(pi): bump pi-goal fork pin v0.1.7 -> v0.2.0

## Summary

Bump the pi-goal fork pin in `.pi/settings.json` from `v0.1.7` to `v0.2.0` so the umbrella consumes the org fork's new release.

**opencharly/pi-goal v0.2.0** (PR #1, merged 32fc68a, tag v0.2.0) adds:
- **Subagent-aware continuation** — the `pi-goal` continuation is withheld while background subagent runs are in flight (tracked via the shared extension event bus `subagent:async-started/-complete/process-terminal`), so an agent waiting on subagent results is never pestered with "continue".
- **Richer continue text** — the continuation prompt now opens with: *"Continue with your TODOs, follow the rules in the project's AGENTS.md (and CLAUDE.md where present), and read the relevant skills before acting."*
- **Mandatory TODO completion gate** — `update_goal` refuses `status=complete` while the last observed todo list has open (`[pending]`/`[in_progress]`) items; the completion audit steers the same rule.

## How tested

- Tag exists and is reachable: `git ls-remote --tags https://github.com/opencharly/pi-goal.git v0.2.0` → present.
- Fork PR #1 merged (squash `32fc68a`), source checks: `npm test` **29/29 pass**, `npm run check` loads the extension in a real pi process.
- Pin updated: `python3 -c "import json; ..."` → `['git:github.com/opencharly/pi-goal@v0.2.0']`.
- Pre-commit gate passed (policy B + harness parity):
```
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

## Rulebook compliance

| Rule | Status | How / N/A |
|---|---|---|
| 1. Never edit inside a submodule | HOW | One umbrella-root file changed (`.pi/settings.json`); the pi-goal fork is a pi package repo, changed via its own PR #1 |
| 2. Git op rule | N/A | No submodule git ops |
| 3. No nested go.work | N/A | No Go changes |
| 4. No worktrees inside submodules | N/A | No worktrees |
| 5. Pin discipline | HOW | Pin moves to v0.2.0 — a merged, tagged release on the org fork (verified via ls-remote) |
| 6. Git safety | HOW | No force-push, no hook bypass; pre-commit gate ran and passed |
| 7. Prove the gate | HOW | Pre-commit gate passed; fork-side source gate (`npm test` 29/29 + extension load check) executed and pasted above |
| 8. Harness config parity | N/A | No shared harness file touched |
| R10. Fresh disposable proof | HOW | Pin verified against the live org fork; fork release verified from its committed tree (PR #1 merged before this pin bump) |

## Change classification

- **Change class:** configuration (pi package pin bump)
- **Verification gate:** pr-validator
- **Attribution tier:** analysed on a live system — the pinned tag was verified live (ls-remote) and the fork release's tests ran live (29/29) before the bump

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*