# 2026.252.2254 — chore(umbrella): sync the eval-omarchy submodule pin to the sharp-report + concurrency-fix chain

## Summary

Syncs the umbrella's recorded eval-omarchy submodule gitlink to the merged sharp-report + concurrency-fix chain (eval-omarchy#56, tags v2026.252.1502/1503).

The chain: the user-voice sharp report contract (## Tests / ## Suggestions + the Assisted-by/Tested-by footers, no verdict section), the entity-authored repo (the pr tools' target through the executor boundary), the block-scalar drive, the CHARLY_BIN validate, and the pins to the plugin-pipeline tags carrying the batch-concurrency fix (per-run state), the ghkit rewire (the NEW opencharly/plugin-gh canonical GitHub client), and the gate's designed skip.

## How tested (pasted)

```
$ ./scripts/sync-gitlinks.sh
SYNC_EXIT=0 - sync-gitlinks: done
  eval-omarchy -> 1abe79b

$ git diff --cached --submodule=short eval-omarchy
Submodule eval-omarchy d52ed26..1abe79b (new commits)
```

The consumer R10 (the fully parallel 12-PR eval batch, zero failures) is pasted in eval-omarchy#56 and its reports/beds are committed there.

Only the eval-omarchy gitlink is committed; the other submodules' worktree movements (other sessions' landings) were restored to their recorded shas.

## Rulebook compliance

- R0: git-workflow + the umbrella AGENTS.md loaded.
- R1: the chain's failures (the 404 swap, the env races, the executor boundary, the gate fail-on-unset) each RCA'd from live evidence in the upstream PRs.
- R3: only the eval-omarchy gitlink is bumped.
- R4: no workarounds - the upstream fixes are root fixes.
- R6: no force-push, no hook bypass, PR-only landing.
- R7: the sync output + the consumer R10 pasted.
- R9: the pin is a merged ref, never a PR branch.
- R10: the consumer's 12-PR parallel batch at zero failures (referenced; pasted in #56).

## Change classification

- Change class: chore (submodule pin sync)
- Verification gate: the sync output + the consumer R10
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*