# 2026.250.1751 — chore(pi): pin pi-omarchy-eval@v2026.250.1748

## Summary

Pins the new **opencharly/pi-omarchy-eval** package (v2026.250.1748) in `.pi/settings.json` — the pi package for the omarchy PR-evaluation lane (skills `omarchy-eval-lane` + `omarchy-eval-golden`, prompts `/eval-pr`, `/eval-batch`, `/eval-cold-read`). The four AGENTS live directly in `.pi/agents/` (PR #157) — the package deliberately ships no agents (R3, one home each).

## How tested

```
$ python3 -c "import json; json.load(open('.pi/settings.json')); print('json-ok')"
json-ok
$ git diff .pi/settings.json
+    "git:github.com/opencharly/pi-omarchy-eval@v2026.250.1748",
$ git status --short
M  .pi/settings.json   (single file)
```

Package resolves at the pinned tag: gh api repos/opencharly/pi-omarchy-eval/tags → v2026.250.1748. The plan's close-out (opencharly/opencharly plan/eval-omarchy-pi-plugin.md §4) runs the delivery audit: a fresh pi session lists the package skills/prompts and the four committed agents.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | N/A — no anomaly; the missing pin was the plan's PR2 step. |
| R2 | N/A — no rulebook/API wording changed. |
| R3 | Single home per concern: agents in .pi/agents/ (PR #157), skills/prompts in the package — the pin references, never duplicates. |
| R4 | N/A — no workaround. |
| R4a | N/A. |
| R5 | N/A — additive. |
| R6 | feat/ branch from current origin/main; no force-push. |
| R7 | JSON validity + diff pasted; harness gate scripts ran in the pre-commit (policy-b + parity) when committing. |
| R8 | N/A — no new workflows. |
| R9 | N/A — no new commands. |
| R10 | N/A for config-class pin; delivery audit in the plan close-out step, pasted there. |
| RDD | N/A — no behavior claims. |
| ADE | N/A. |
| SDD | N/A. |
| B14 | Evidence pasted. |
| B15 | N/A. |
| B17 | N/A. |
| B18 | N/A — nothing skipped. |

## Change classification

- Change class: harness config (package pin, one line).
- Verification gate: JSON parse + diff pasted; org pr-validator gates the merge.
- Attribution tier: analysed on a live system (pin resolves against the live tag; delivery audit scheduled in the plan close-out).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*