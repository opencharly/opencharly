# 2026.235.1822 — chore: the ripgrep submodule becomes layer-ripgrep (the kind-prefixed naming)

## Summary

The candy de-submodule cutover's **kind-prefixed naming** (the operator's direction): every candy repo is named `<kind>-<name>` (layer-/box-/pod-/vm-/plugin-/… per the charly kind vocabulary). The pilot repo `opencharly/ripgrep` (a plain layer candy) is renamed **`opencharly/layer-ripgrep`**; this PR renames the umbrella's checkout path accordingly (the old `ripgrep` submodule entry is dropped — the stale `.gitmodules` entry was failing the verify gate).

## How tested

**The umbrella gate on the final tree (pasted):**

```
$ task verify
verify-pins: OK — 24 submodules, policy B holds
OK: /home/atrawog/Sync/Atrapub/coder/pi/opencharly/.pi/extensions/umbrella-gates.ts parses (node type-strip pass)
```

**The rename is live (pasted):**

```
$ git ls-remote https://github.com/opencharly/layer-ripgrep.git
2be28df473c24fe80a3e448a3e8d143e2a61b3e5	HEAD
2be28df473c24fe80a3e448a3e8d143e2a61b3e5	refs/heads/main
2be28df473c24fe80a3e448a3e8d143e2a61b3e5	refs/tags/v2026.235.1653
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface touched |
| R1 | the stale `.gitmodules` entry surfaced as a verify failure — root-caused (the `git rm --cached` left it behind) and fixed in-tree (commit `00839ec`) |
| R2 | N/A — the surfaced failure was fixed, not parked |
| R3 | the sync scripts remain the single pin mechanism |
| R4 | N/A — the submodule follows its own default-branch HEAD |
| R5 | the old `ripgrep` submodule entry is deleted in this PR (the rename is a complete cutover) |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | the verify output pasted above |
| R8 | N/A — no generation changes |
| R9 | N/A — no deploy target change |
| R10 | the verify ran on the final committed tree |

## Change classification

- **Change class**: submodule path rename (ripgrep → layer-ripgrep)
- **Verification gate**: `task verify` (24 submodules, policy B holds)
- **Attribution tier**: analysed on a live system — the verify executed live with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*