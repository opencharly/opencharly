# 2026.259.1225 — chore: sync eval-omarchy + marketplace gitlinks to their merged HEADs

## Summary

Advance the umbrella's `eval-omarchy` and `marketplace` gitlinks to their owning repos' **merged default-branch HEADs** — the two repos this session changed in the schema-first PR-eval work:

| submodule | from | to | what the new commit is |
|---|---|---|---|
| `eval-omarchy` | `f70f758` | `caf4ee7` | merged main HEAD after [eval-omarchy#78](https://github.com/opencharly/eval-omarchy/pull/78) (tag `v2026.259.1014`) |
| `marketplace` | `bd58a38` | `d66f6f4` | merged main HEAD after [marketplace#302](https://github.com/opencharly/marketplace/pull/302) |

Both targets are default-branch HEADs under policy B's every-other-repo rule (these two are not `distro-*`, not charly-pinned, not `sdk`/`spec`). No PR-branch ref is pinned.

**Supersedes** umbrella #204 (the automated daily sync snapshot), which recorded `eval-omarchy 54110f7` / `marketplace 1a12b4a` — **ancestors** of the merged HEADs, so landing it would have regressed this snapshot. #204 was closed for that reason; a fresh sync then skipped (its dedupe sees an open sync PR), so this hand-made PR carries the two current pins. The automated `sync.yml` generator producing a body that never passes the validator is the underlying defect — see "R2" below.

## How tested

**1. Every pinned ref IS its repo's live default-branch HEAD (policy B), pasted from `git ls-remote`:**

```
$ git ls-remote --symref https://github.com/opencharly/eval-omarchy.git HEAD
ref: refs/heads/main	HEAD
caf4ee7ae2e9a54594f1e321206f59b1c9c167a1	HEAD
$ git ls-remote https://github.com/opencharly/eval-omarchy.git refs/heads/main
caf4ee7ae2e9a54594f1e321206f59b1c9c167a1	refs/heads/main

$ git ls-remote --symref https://github.com/opencharly/marketplace.git HEAD
ref: refs/heads/main	HEAD
d66f6f485996a1638ac11a981de09bfeb35d85a6	HEAD
$ git ls-remote https://github.com/opencharly/marketplace.git refs/heads/main
d66f6f485996a1638ac11a981de09bfeb35d85a6	refs/heads/main
```

The `ref:` line proves each repo's default branch is `main`; each recorded gitlink equals that branch's live HEAD exactly.

**2. Both target commits are MERGED, not branch tips** (their PRs merged, tagged by `tag-on-merge`):

```
$ git -C eval-omarchy log --oneline caf4ee7 -2
caf4ee7 docs: add CHANGELOG 2026.259.1014 for PR #78
25b22dc feat(eval): schema-first record emission + org pr-validator grading (#78)
$ git -C marketplace log --oneline d66f6f4 -2
d66f6f4 docs: add CHANGELOG 2026.259.1027 for PR #302
4214d02 chore(refs): bump eval-omarchy to v2026.259.1014 + regenerate its distros skills (#302)
```

**3. The diff is exactly two gitlinks, no file content:**

```
$ git diff --cached --stat HEAD~1
 eval-omarchy | 2 +-
 marketplace  | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```

**4. The content behind each pin was gated by its own repo's `pr-validator` before merge** — the two PRs above (both validate SUCCESS, squash-merged). This PR records pointers to already-validated content; it cannot re-review content it does not carry.

## Rulebook compliance

- **R0** — `git-workflow` + `git-workflow`'s CalVer/pinning references loaded.
- **R1** — the #204 supersession is root-caused (its recorded SHAs are ancestors of the merged HEADs; the automated `sync.yml` body generator has never passed validation, so syncs have been landed by hand) — not dismissed.
- **R2** — no deferral: the two pins land here; the `sync.yml` generator defect is **routed as its own named batch cutover** (below), because it is a separable workflow change with its own test, not part of a two-gitlink pointer bump.
- **R3** — no duplication: `sync-gitlinks.sh` remains the one sync implementation; this PR records its result for the two repos.
- **R4** — no workaround: both pins are merged default-branch HEADs, never branch refs; no hand-force.
- **R5** — the old pointers are replaced in place.
- **R6** — PR-only, add-only; no force-push; `git status` inspected (two gitlinks).
- **R7** — `git ls-remote` (the definitive policy-B check for a gitlink-only diff) run live and pasted above.
- **R8** — N/A — no emitted artifact / OCI label.
- **R9** — N/A — no binary built or deployed.
- **R10** — N/A — gitlink-only; no runtime bed exercises a pointer bump. The gate that applies to this class is the pin-verification in R7, executed and pasted.

### Routed batch (R2, named): fix the `sync.yml` automated body generator

`sync.yml`'s generated PR body has never passed the org validator (the workflow's own comment: *"every landed sync has been hand-made"*). Its three defects — a missing `Assisted-by:` footer, no rule-compliance section, and a `scripts/verify-pins.sh` dismissal instead of a routed fix — are the reason every sync has required a human. That is a **separable workflow change with its own test** (generate the body in a scratch run and assert it against the validator's requirements), so it is its own batch cutover, not folded into this pointer update. Named here so it is not parked in prose.

### Local `verify-pins.sh` note (honest)

`task verify` (`scripts/verify-pins.sh`) is a whole-tree gate for a *fully-initialized local* checkout: it asserts every submodule is clean and checked out at its gitlink, plus charly's recursive nested state. It cannot run green in this shared working tree (a peer session's `charly` changes make one submodule dirty) nor in CI's non-recursive checkout. For a two-gitlink diff the applicable, runnable check is R7's `git ls-remote` comparison above — executed, pasted, and sufficient to prove each pin is its repo's merged default-branch HEAD.

## Change classification

- Change class: gitlink-only (two submodule pointers, no file content).
- Verification gate: `git ls-remote` policy-B comparison + the merged-PR provenance, pasted.
- Attribution tier: analysed on a live system — the pins were resolved against the live default-branch HEADs; no runtime artifact to exercise.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*