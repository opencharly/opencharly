# 2026.243.2139 — feat(prompt): context-economy doctrine, sharper skill dispatcher, dedup

## Summary

Sharpen the umbrella's pi system prompt for efficiency and clarity — **zero capability reduction** (all 85 marketplace skills stay discoverable and loadable; every discipline section preserved verbatim).

- **`.pi/APPEND_SYSTEM.md`** (6,628 B → 6,211 B): add the first-class **Context economy** doctrine — `todo` / `subagent` / `fabric_exec` as the main agent's context-minimization toolkit, with clear per-tool guidance. Fold the scattered tool-usage bullets into it (R3 dedup: one canonical home). Tighten prose; every discipline section (plan, PR validation, delegation, close-loops, charly status, goal completion) preserved.
- **`AGENTS.md`**: expand the R0 skill dispatcher table with the umbrella's real triggers (R10 beds → `/charly-internals:agents`, docs/marketplace regen → `/charly-build:docs`, skill maintenance → `/charly-internals:skills`) and add the explicit per-need line ("load a skill's SKILL.md by path ONLY when its trigger matches — never pre-load, never load-all"). Reconcile stale/conflicting guidance: submodule count 344→389 (matches the actual tree), drop the non-existent `umbrella_load_skills` tool reference, and fix the R0 "load ALL their skills" wording to match the per-need contract.

## How tested

**`pi -p` probe on the new config (2 runs, both green):**
- Context economy doctrine present verbatim (heading + todo bullet quoted)
- Dispatcher rows for R10 beds + docs/marketplace regeneration present
- Per-need line present verbatim: "Load a skill's SKILL.md by path ONLY when its trigger matches — never pre-load, never load-all."
- R0 wording: "load every skill those rows select before doing anything — never the whole index"
- Submodule count: 389 (correct)
- No `umbrella_load_skills` reference anywhere in context
- `todo` / `subagent` / `fabric_exec` all available; `git-workflow` skill loads by path; memory + extensions (`gh_pr_status`, `charly_status`) functional; no startup warnings

**`task verify` on the committed tree (delegated run, full output):**
```
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 389 submodules, policy B holds
```
EXIT=0, tree clean after the run.

**pre-commit gate:** OK (check-harness-parity: OK — 5 shared harness files identical to charly/).

## Rulebook compliance

- **R1** — fixed the 344→389 submodule divergence and the stale `umbrella_load_skills` reference (doc-vs-reality).
- **R3** — no duplication: the tool-usage bullets now live in ONE place (the doctrine); the subagent-exploration bullet removed from AGENTS.md is owned by the doctrine.
- **R7** — `task verify` run on the final committed tree, output pasted above.
- **Rule 8 (harness parity)** — `.pi/settings.json` untouched; check-harness-parity OK.
- No pins, no scripts, no submodule edits, no direct push to main.

## Change classification

- **Change class:** docs-only (AGENTS.md rulebook + `.pi/APPEND_SYSTEM.md` system-prompt prose; no pins/scripts changed)
- **Verification gate:** `task verify` GREEN + `pi -p` live probes
- **Attribution tier:** `documentation reviewed`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*