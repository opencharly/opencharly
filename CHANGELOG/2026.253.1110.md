# 2026.253.1110 — chore(umbrella): sync the gitlinks for the corpus chain

## Summary

Syncs the umbrella's recorded submodule gitlinks for the omarchy-test-corpus chain — THREE merged refs:

- **eval-omarchy** — the corpus Waves 4-5 (the bar QML invariants, the hyprland config, the update machinery, the ocr round-trip, the acceptance session-health surfaces) merged via #62 + #64.
- **plugin-omarchy#4** — the single-lined check description (the remote candy scan fix) + the tag-on-merge dispatcher install (#3).
- **plugin-wl#14 + #16** — the ocr method (the screen text assertion, the authored scale, the native-res default).

Policy B: the pins are the merged refs.

## How tested (pasted)

```
$ ./scripts/sync-gitlinks.sh
SYNC_EXIT=0 - sync-gitlinks: done
$ git show --stat HEAD
 eval-omarchy   | 2 +-
 plugin-omarchy | 2 +-
 plugin-wl      | 2 +-
 3 files changed, 3 insertions(+), 3 deletions(-)
```

Only the three merged refs are committed; the other submodules' worktree states are clean (other sessions' landings were restored to their recorded shas).

## Rulebook compliance

- R0: git-workflow + the umbrella AGENTS.md loaded.
- R3: only the three merged refs are bumped.
- R4: no workarounds — the sync is the canonical path.
- R6: no force-push, no hook bypass, PR-only landing.
- R7: the sync output pasted.
- R9: the pins are merged refs, never PR branches.
- R10: each fix's live proof pasted in its own PR (referenced).

## Change classification

- Change class: chore (submodule pin sync)
- Verification gate: the sync output (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*