# 2026.247.0805 — chore: sync gitlinks

## Summary

Automated gitlink sync: every umbrella submodule pin advanced to what **policy B**
requires — `charly` to its own default-branch HEAD, `distro-*` to exactly the commits
charly's own gitlinks pin, everything else to its own default branch.

**1** gitlink(s) moved.

## Every changed path, named

```
eval-omarchy
```

Each entry is a **submodule pointer**, not file content: the umbrella records which
commit of each repo the snapshot means. The content behind every one of them was
reviewed and gated by that repo's own `pr-validator` before it was tagged. This PR
cannot review it and does not restate it.

## How tested

**What produced this diff**, pasted from the run that opened this PR — every pin
resolved from the repo's own default-branch HEAD (or, for `distro-*`, from charly's
gitlink) over the network, never hand-written:

```
  plugin-bpf -> 4d99ced
  plugin-cardwire -> 216065c
  layer-charly -> e35d843
  layer-charly-build -> 4d95717
  layer-charly-check -> 973960c
  layer-charly-image -> 62629c2
  layer-charly-immich -> 7122419
  layer-charly-internals -> 907772f
  layer-charly-internals-extra -> 3eb5286
  layer-charly-kubernetes -> bb2020a
  layer-check-local-layer -> 131d9bb
  layer-check-stack-layer -> d63cc8d
  layer-examplestep-consumer -> c600046
  layer-examplestep-deploy-consumer -> d4000b0
  layer-omarchy-base -> d66d0cc
  layer-omarchy-boot -> 5adcec2
  layer-omarchy-desktop-apps -> 491cab3
  layer-omarchy-dev -> cae1d30
  layer-omarchy-fonts -> 539faba
  layer-omarchy-gpu-nvidia -> 0e6fcdb
  layer-omarchy-hardware -> 3d3f4e0
  layer-omarchy-hyprland -> 52e0d33
  layer-omarchy-media -> da80c65
  layer-omarchy-network -> a0214cb
  layer-omarchy-shell -> 2a94f0d
  layer-omarchy-terminal -> 03e07cb
  layer-omarchy-themes -> 05e3412
  layer-stepkind-build-consumer -> bff1fc2
  plugin-boot-kind -> f0bcaeb
  plugin-desktop-kind -> 34786df
  plugin-quickshell -> d4cab20
  plugin-unit -> 8af3ddc
  pod-charly-mcp -> b3420c0
  eval-omarchy -> 35b61d2
  omarchy-eval-artifacts -> 071ed32
  plugin-herdr -> 5ad8e7e
  plugin-omarchy -> 93d96a4
  pod-herdr -> a3dca9b
  plugin-openclaw -> 457eb00
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'
```

**On `scripts/verify-pins.sh`, honestly.** An earlier version of this body said that
gate "runs on this PR". It does not — it runs in no workflow, only on demand via
`task verify` in a local checkout — and A1 was right to block the claim. It is also
not worth running here: its last check is
`git -C charly submodule status --recursive | grep '^[+-]'`, where `-` means
UNINITIALISED, and this job checks out submodules non-recursively. It can therefore
only fail; initialising them would check each out AT its gitlink and make it pass by
construction. Tried, measured, and left out rather than faked.

The pins in this diff are still verifiable, by the thing that actually gates them:
each is a real commit on the owning repo's default branch, and `task verify` on a
full local checkout is where policy B is enforced end to end.

## Change classification

- **Change class:** gitlink-only. No file content, no workflow, no script, no Go.
- **Breaking change + rollback:** none — a revert restores the previous snapshot.

---

## Authorship

**No `Assisted-by:` footer, deliberately.** Rule A1 requires that line on an
*AI-authored* body. This body is emitted by a fixed block in
`.github/workflows/sync.yml` with no model in the loop, so there is no harness and no
provider model to name; inventing one to satisfy the pattern would be the false claim
the rule exists to prevent.

Generated by: `.github/workflows/sync.yml` (deterministic; no model).