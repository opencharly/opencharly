# 2026.261.1153 — chore: sync gitlinks — the jetkvm input-fix chain

## Summary

Advances the three umbrella gitlinks that moved after the JetKVM input-fix chain merged.

- `plugin-jetkvm`: `2892333a6` → `92b488725` (`v2026.261.0945` — the move/click/key/coordinate fixes).
- `layer-charly-check`: `694223f32` → `fd9faaf9b` (`v2026.261.1057` — the skill semantics updates).
- `marketplace`: `5fea95ca1` → `9f7a8f8e5` (the regenerated corpus).

All three are the standard every-other-repo default-branch-HEAD pins; no charly-pinned (`distro-*`) pin moves. Produced by `task sync` (`scripts/sync-gitlinks.sh`), not hand-edited.

## How tested

The umbrella's pinning gate, on the committed tree with all submodules initialized:

```console
$ bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 403 submodules, policy B holds
```

The per-commit hook ran on the commit: `pre-commit: OK`.

Both new pins equal their repos' current default-branch HEAD:

```console
$ for p in plugin-jetkvm layer-charly-check marketplace; do
    echo "$p gitlink=$(git ls-files -s "$p" | awk '{print $2}') main=$(gh api repos/opencharly/$p/commits/main --jq .sha)"; done
plugin-jetkvm      gitlink=92b488725 main=92b488725...
layer-charly-check gitlink=fd9faaf9b main=fd9faaf9b...
marketplace        gitlink=9f7a8f8e5 main=9f7a8f8e5...
```

## Rulebook compliance

- **R0 skills:** `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** the drift is the documented post-merge lag — each content PR's `tag-on-merge` CHANGELOG commit lands after the merge, so the umbrella pin trails by that commit. No warnings; the pre-commit hook is clean.
- **R2 no out-of-scope:** the three advanced gitlinks are the whole change.
- **R3 no duplication:** produced by the canonical `scripts/sync-gitlinks.sh`.
- **R4 no workaround:** pins move to merged default-branch HEADs; no PR-branch or dangling pin.
- **R5 hard cutover + grep:** `git status` after `task sync` shows exactly the three expected gitlinks.
- **R6 git safety:** dedicated linked worktree off fresh `origin/main`; hook passed; no force push, no hooks bypass, no direct push to `main`.
- **R7 prove the gate:** `verify-pins.sh` (the `task verify` body) green on the committed tree, pasted above.
- **R8/R9:** N/A — gitlinks only.
- **R10 fresh disposable proof:** the gate ran on the committed worktree with all submodules initialized; no check bed applies (`N/A — pinning change`).
- **Policy B:** unchanged — `6 distro pins equal charly's gitlinks`; only the every-other-repo pins moved.
- **CHANGELOG:** written by `tag-on-merge` from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*