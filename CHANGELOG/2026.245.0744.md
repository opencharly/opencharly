# 2026.245.0744 — chore: enable all charly plugins in umbrella claude + pi harness configs

## Summary

The umbrella's harness plugin configurations had drifted from the org's canonical reference: the umbrella `.claude/settings.json` (`enabledPlugins`) and `.pi/claude-plugins.json` (pi-claude-marketplace desired state) enabled only an 11-plugin subset of the `charly-plugins` marketplace, while `charly/.claude/settings.json` — the canonical harness config — enables the full 26-plugin `charly-*` set.

This PR syncs both umbrella harness configs to charly's canonical plugin set, restoring rule-8 harness-config parity and bringing the skills for ALL charly plugins into the default skill configuration for both harnesses — including everything needed to fully test omarchy and evaluate omarchy PRs (omarchy, omarchy-cstream, omarchy-eval, omarchy-vm, the full charly-check probe suite, plus infrastructure/selkies/tools/coder/languages and the image-family plugins; the omarchy skills themselves were already carried by the enabled distros/vm/check plugins).

Files changed:
- `.claude/settings.json` — `enabledPlugins` expanded 11 → 26 charly plugins (plugin list now byte-identical to `charly/.claude/settings.json`); claude-md-management + gopls-lsp retained.
- `.pi/claude-plugins.json` — `plugins` map expanded 11 → 26 charly plugins; `autoupdate: true` retained on the charly-plugins marketplace entry (from #108).

## How tested

1. Live parse + set equality (`node`, final committed tree):
```
pi: 26 claude: 26 charly-ref: 26
pi==ref: true
claude==ref: true
missing from pi: []
```
2. Harness parity gate:
```
check-harness-parity: OK — 5 shared harness files identical to charly/
```
3. Pre-commit gate (no bypass flags):
```
GATE PASS
pre-commit: OK
```
4. `task verify` on the final committed tree (R7/R10):
```
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 391 submodules, policy B holds
EXIT:0
```

## Rulebook compliance

- R1: root-caused — umbrella harness plugin configs had silently drifted to an 11-plugin subset while charly's canonical reference enables the full 26; fixed at the source, not papered over.
- Rule 8 (harness config parity): umbrella `.claude/settings.json` + `.pi/claude-plugins.json` now mirror charly's canonical enabled-plugin set; parity gate green.
- R6 (git safety): feature branch, no force-push, no direct push to main, PR-only landing.
- Rule 1: no submodule edits — both changed files are umbrella-root files in this repo, tracked and committed here.

## Change classification

- Change class: harness configuration (Claude plugin enablement + pi marketplace desired state)
- Verification gate: `task verify` green on the final committed tree; changed config paths executed live (node set-equality parse + parity gate)
- Attribution tier: fully tested and validated

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*