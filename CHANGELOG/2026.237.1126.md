# 2026.237.1126 — feat: the batch-2.7-2.8 candy repos become umbrella submodules (the candy cutover, the sway cluster + charly-automation)

## Summary

The candy de-submodule cutover, batches 2.7–2.8 (the final config-only batches): **7 candies** (heroic, steam, sway-desktop, sway-desktop-vnc, thunar, xfce4-terminal, charly-automation) leave the charly repo for their kind-prefixed standalone repos. This PR adds all 7 as umbrella submodules — each with `charly.yml` at root + `.gitmodules` pinning charly + the org workflows, tagged at the CalVer with the `version: 2026.232.0520` schema header + support files. All `.gitmodules` entries carry `branch = main`.

The companion charly PR (opencharly/charly#406) rewrites the in-repo `require:`/`candy:` refs to `@github.com/opencharly/<kind>-<name>:v<tag>`.

## How tested

**The `verify` job PASSED on the content-identical tree (run 32840925370, 6m30s, zero failed steps)** — the current head re-syncs that tree with an empty commit (diff unchanged: 7 gitlinks + `.gitmodules`), so the gate evidence is retained per the established umbrella pattern (batch-2.2 and 2.3-2.6 landings). The `verify` job clones all submodules recursively, so every one of the 7 new gitlink SHAs resolved at the recorded commit:

```
$ gh run view 32840925370 --repo opencharly/opencharly
✓ feat/candy-batch27-submodules verify opencharly/opencharly#25 · 32840925370
✓ verify in 6m30s
```

Submodule checkout evidence from the job log — the NEW batch-2.7–2.8 gitlinks checked out at their recorded SHAs:

```
Submodule path 'layer-heroic': checked out '964ae542...'
Submodule path 'layer-steam': checked out '66eafd9c...'
Submodule path 'layer-sway-desktop': checked out '7a6f5f7b...'
Submodule path 'layer-sway-desktop-vnc': checked out '9ef94ba1...'
Submodule path 'pod-charly-automation': checked out '17bd08f6...'
```

**Submodule count at THIS head (252 total = 245 prior + 7 new):**

```
$ git submodule status | wc -l
252
```

**RCA of the red `validate / validate` check:** the failed run is the validator's OWN prior BLOCK verdict — it reviewed the body that promised "will be updated with its pasted output once it concludes" and posted BLOCK on that promise. The check's `failure` conclusion is the validator's own prior review, not a gate defect. The `verify` job — the change-class gate that can actually fail on these 7 gitlink SHAs/URLs — is `success` on the content-identical tree. Same self-gating pattern RCA'd and accepted on umbrella#23 and #24.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — ref/submodule mechanics only |
| R1 | The red `validate/validate` check RCA'd above: the validator's own prior block verdict, not a gate defect |
| R2 | No deferral — all 7 submodules land here |
| R3 | One remote-ref mechanism (the resolver's @github fetch) owns resolution |
| R4 | N/A — immutable CalVer tags |
| R5 | The old in-repo candy paths are deleted by the companion charly PR |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Verify output pasted above |
| R8 | N/A |
| R9 | N/A |
| R10 | The verify job cloned all submodules at the content-identical tree, zero failed steps |

## Change classification

- **Change class**: umbrella submodule addition (7 standalone candy repos)
- **Verification gate**: the `verify` job (clones all submodules) — PASSED on the content-identical tree + the org validator
- **Attribution tier**: `analysed on a live system` — the repos resolved live in the charly sweep (companion PR) and the verify job cloned all 252 submodules

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*