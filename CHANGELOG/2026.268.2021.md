# 2026.268.2021 — chore: sync gitlinks — adopt charly main (the umbrella session model)

## Summary

Run the umbrella's own `./charly/bin/charly task sync` (policy B) now that the
companion PRs have merged. It bumps the `charly` gitlink from `8992f79a` to
`c70c9ec4` — charly main, which contains:

- opencharly/charly#666 — the rulebook pointer to the umbrella model, the
  `CLAUDE.md` symlink, and the dead `.claude/worktrees/` gitignore cleanup;
- opencharly/charly#665 — the sdk/spec contract-pin repair.

It also advances the other default-branch submodules whose upstream moved in the same
window (`docs`, `dotgithub`, `marketplace`, `pkg-arch`, `plugin-dsh`, `layer-charly-*`).
`distro-*` are unchanged — policy B already held.

This is the sanctioned sync, not a hand-pin: the task computes every pin and the full
gate passes.

## How tested

**The pinned `charly` content was executed live.** The sync pins charly to `c70c9ec4`,
whose content is exactly the merged #665+#666 tree this session built and tested in its
own worktree (`.worktrees/umbrella-dev-model/charly`, merged onto that main):

```
$ go build ./...
build exit=0
$ go vet ./...
vet exit=0
$ go test ./...
ok  	github.com/opencharly/charly/charly	186.141s
ok  	github.com/opencharly/charly/charly/internal/pluginsgen	29.286s
$ bash scripts/bootstrap-charly.sh
bootstrap-charly: built ./bin/charly (2026.268.1950)
$ ./bin/charly version
2026.268.1950
```

**The gate ran on the final tree — the full pinning gate, 3/3 pass:**

```
$ ./charly/bin/charly task sync
task sync: 1 step(s), 0 failed
  [pass] run — bump the pins (charly rolls first, then distro-* from its new gitlinks)

$ ./charly/bin/charly task verify
task verify: 3 step(s), 0 failed
  [pass] check — policy B — every distro-* gitlink equals charly's own box/* gitlink
  [pass] check — every submodule is clean and checked out at its recorded gitlink, and its branch equals the remote default
  [pass] check — the opencode gate plugin contract
verify exit=0
```

The pinned `charly` checkout resolves to the same commit as the recorded gitlink
(`task verify`'s second step asserts exactly this for all 11 bumped pins):

```
$ git -C charly rev-parse HEAD
c70c9ec46123c58f6d8e649f1edfcf0317d45945
$ git ls-files -s charly | awk '{print $2}'
c70c9ec46123c58f6d8e649f1edfcf0317d45945
```

For the ten other bumps (`docs`, `dotgithub`, `marketplace`, `pkg-arch`, `plugin-dsh`,
`layer-charly-*`) the changed path is a submodule gitlink advanced to its own
default-branch HEAD; there is no local build/run path to execute, and `task verify`
validates each pin live (`git -C <sub> status` + `HEAD == gitlink` + branch == remote
default).

Diff:

```
$ git diff --stat origin/main...HEAD
 charly                       | 2 +-
 docs                         | 2 +-
 dotgithub                    | 2 +-
 layer-charly-build           | 2 +-
 layer-charly-check           | 2 +-
 layer-charly-image           | 2 +-
 layer-charly-internals       | 2 +-
 layer-charly-internals-extra | 2 +-
 marketplace                  | 2 +-
 pkg-arch                     | 2 +-
 plugin-dsh                   | 2 +-
 11 files changed, 11 insertions(+), 11 deletions(-)
```

## Rulebook compliance

- **Rule 1 (never edit inside a submodule):** upheld — this PR edits only the umbrella's
  recorded gitlinks; no submodule content is changed here.
- **Rule 2 (submodule git via `git -C`):** upheld.
- **Rule 3 (no nested `go.work`):** N/A.
- **Rule 5 (pin only merged refs):** every bump is to a MERGED default-branch HEAD
  (`charly` = the merge of #665/#666, `c70c9ec4`); no PR branch is pinned.
- **Rule 6 (git safety):** on `sync/charly-gitlink` from fresh `origin/main`; no
  force-push, no direct push to `main`.
- **Rule 7 (prove the gate):** `task verify` ran on the final tree — 3/3 steps pass —
  with output pasted above.
- **R2/R3 (finish / no duplication):** the bump is produced by the one canonical sync
  mechanism, not a hand-edit.
- **R5 (delete legacy completely):** the pinned `charly` main carries the
  `.claude/worktrees/` removal; the umbrella side already adopted `.worktrees/` in #256.
- **R10 (fresh disposable proof):** the pinned charly content is the tree this session
  built and tested (`go build/vet/test` + binary build above); a pin sync has no live
  disposable target beyond the gate, which ran.

## Change classification

- Change class: **gitlink/pin sync** (manifest only; no code).
- Verification gate: `task verify` (3/3) on the final tree, plus the pinned content's
  `go build/vet/test` + binary build (executed live).
- Attribution tier: **fully tested and validated**.

## Authoring-runtime identity (attribution evidence)

The authoring runtime is OpenCode on the `ollama-cloud` provider, model ID
`ollama-cloud/deepseek-v4.1-flash`, resolved from the provider's own registry:

```
$ opencode models --provider ollama-cloud | grep -i deepseek
ollama-cloud/deepseek-v4.1-flash    DeepSeek V4.1 Flash    (active)
ollama-cloud/deepseek-v4-pro:0813   DeepSeek V4 Pro 0813   (active)
```

The attribution footer uses that exact provider + full model name.

*Assisted-by: OpenCode DeepSeek V4.1 Flash (fully tested and validated)*