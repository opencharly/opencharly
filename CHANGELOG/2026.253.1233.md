# 2026.253.1233 — chore(umbrella): sync the eval-omarchy submodule pin to the corpus registry removal

## Summary

Syncs the umbrella's recorded eval-omarchy submodule gitlink to the corpus registry removal (eval-omarchy PR #65 MERGED + its tag-on-merge CHANGELOG ref 9096b548): the corpus mapping now lives in the CUE-validated charly.yml single source; the side registry.yaml is deleted; the corpus-gate probe validates the oracle's selection.

## How tested (pasted)

```
$ ./scripts/sync-gitlinks.sh
SYNC_EXIT=0 - sync-gitlinks: done
$ git diff --cached eval-omarchy | grep Subproject
-Subproject commit 025dbd0337602c60aeeefad782776e91604795b8
+Subproject commit 9096b5484da96a26a953c10981294eace53ac3ba
```

## Rulebook compliance

- R0: git-workflow + the umbrella AGENTS.md loaded.
- R3: only the eval-omarchy gitlink is bumped.
- R6: no force-push, no hook bypass, PR-only landing.
- R9: the pin is a merged ref, never a PR branch.
- R10: the fix's live proof pasted in eval-omarchy#65 (referenced).

## Change classification

- Change class: chore (submodule pin sync)
- Verification gate: the sync output
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*