# 2026.268.0825 — chore: bump gitlinks for the /verify-beds retirement

## Summary

Bump three gitlinks to record the merged `/verify-beds` retirement across the org:

- `charly` → `v2026.268.0744` (`4f9c868b`) — charly#660 retires the agent-layer `/verify-beds` workflow and repoints its two sibling workflows at the native `kind:check-roster` engine.
- `layer-charly-check` → `50c410c` — layer-charly-check#23/#24 repoint the check-family skill prose at the native roster (and revert an accidental schema bump so the repo's CI passes).
- `layer-charly-internals` → `45d5409` — layer-charly-internals#20/#21 repoint the agents skill prose at the native roster similarly.

All three are merged `main` HEADs; this umbrella PR only records the gitlinks (umbrella rule 1: never edit inside a submodule). The marketplace/docs generated corpora regenerate automatically from the pinned layer tags via the marketplace repo's own refresh workflow (never hand-edited).

## How tested

The pinned `charly` commit is `4f9c868b` (the merged `main` HEAD tagged `v2026.268.0744`). The build binary reports CalVer `2026.268.0745`: the build stamp is derived from the HEAD commit's **UTC timestamp** (`scripts/calver.sh`), whereas `v2026.268.0744` is the tag-on-merge CalVer minted at merge time — the two differ by the minute the CHANGELOG commit landed. Both name the SAME pinned tree (`git rev-parse HEAD` = `4f9c868b`).

```
$ git -C charly rev-parse HEAD
4f9c868bdb873bc088eb2ecefe6b050b9882602e        # == the pinned gitlink
$ ./scripts/bootstrap-charly.sh
bootstrap-charly: built ./bin/charly (2026.268.0745)
$ ./bin/charly version
2026.268.0745                                   # the pinned commit's UTC-date stamp
$ charly/bin/charly -C . box validate
charly box validate: OK — checked 628 candies, 129 boxes, 7 distros, 7 builders; 0 warnings, 0 errors
$ task verify
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-opencode-plugin: OK
verify-pins: OK — 408 submodules, policy B holds
```

## Rulebook compliance

- Rule 1 (no submodule edits): only gitlinks recorded; every change landed by PR to its owning repo.
- Rule 5 (pin merged refs): all three SHAs are merged `main` HEADs (charly `v2026.268.0744`, layer-* `v2026.268.0756`).
- Rule 6 (Policy B): only these three gitlinks move; the other ~405 submodules are untouched.
- R1/R6/R10 of charly's own rulebook: the retirement is docs/harness-config (no runtime path); the roster engine's R10 is proven in charly#656.

## Change classification

- **Change class:** umbrella gitlink sync (submodule pointer bump to merged producer HEADs).
- **Verification gate:** `task verify` (policy B) + the root `box validate`, both pasted above.
- **Attribution tier:** `fully tested and validated`.

---

*Assisted-by: opencode ollama-cloud deepseek-v4.1-flash (fully tested and validated)*