# 2026.243.1623 — chore: sync gitlinks — docs head (plugin-bpf + plugin-cardwire reference pages)

## Summary

Umbrella gitlink sync following the docs publish of the two new plugins:

- `docs` → its current default-branch head `1573a12` (carries docs#107: the plugin-bpf + plugin-cardwire reference pages, drift-gate-verified regeneration at charly a31f76d7).
- This PR's diff is exactly ONE pointer change: `docs` (24614b2 → 1573a12). The other repos the sync script examined (pod-kde-desktop, pod-kde-selkies, layer-language-runtimes, layer-ollama-cuda) ended at the same commits as the recorded gitlinks — no pointer changes resulted, so the diff contains only `docs`.

## How tested

```
$ task verify
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 389 submodules, policy B holds
```

Pre-commit gate green (check-policy-b OK, check-harness-parity OK), run on the final committed tree with every submodule initialized.

## Rulebook compliance

- Policy B: 6 distro pins equal charly's gitlinks (verified above).
- Pin discipline: default-branch heads only (merged refs); no PR branches.
- R6: clean status before commit; no force-push.

## Change classification

- Change class: infra/chore (single gitlink sync: docs pointer).
- Verification gate: `task verify` GREEN on the final committed tree, pasted above.
- Attribution tier: `fully tested and validated` — the full pinning gate (verify-pins + policy B + pre-commit parity) ran live on the committed tree containing this exact diff.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*