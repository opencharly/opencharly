# 2026.254.0349 — chore(umbrella): sync the charly submodule pin after #581 (v2026.254.0335)

## Summary

Sync the umbrella's charly submodule gitlink after charly PR #581 merged (tag v2026.254.0335). The umbrella recorded charly at 993f105 (pre-#580/#581); the merged tree's tip is ad842518. Single-purpose PR — only the charly gitlink changes; local working-tree checkouts of unrelated submodules (sdk/spec/plugin-*) are left untouched and uncommitted.

eval-omarchy's gitlink was already synced by umbrella PR #187 (064f435 == eval-omarchy origin/main), so no eval-omarchy change is needed in this PR.

## How tested

$ cd /home/atrawog/Sync/Atrapub/coder/pi/opencharly && task verify
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 400 submodules, policy B holds
EXIT=0

Pre-commit gate also passed on the commit itself:
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK

## Rulebook compliance

- Rule 2 (git ops): all submodule git operations run via git -C <abs-path> from the umbrella root; no cross-boundary commands
- Rule 5 (pin discipline): pins the merged ref (charly default-branch HEAD ad842518), never a PR branch
- Rule 6 (Policy B): charly is pinned to its own default-branch HEAD per sync-gitlinks.sh; the fix is a sync, not a hand-pin
- R6 (git safety): feat/ branch, no force-push, no direct push to main; PR-only landing
- R7 (prove the gate): full task verify passed on the final committed tree (pasted above)

## Change classification

- Change class: chore (gitlink sync after a merged PR)
- Verification gate: task verify on the final committed tree (400 submodules, policy B holds)
- Attribution tier: analysed on a live system (full gate ran on the committed tree with pasted exit; the pinned charly commit's own validation lives in charly #581)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*