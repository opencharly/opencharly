# 2026.237.0933 — feat: the batch-2.2 candy repos become umbrella submodules (the candy cutover, batch 2.2)

## Summary

The candy de-submodule cutover, batch 2.2: **29 config-only candies** leave the charly repo for their kind-prefixed standalone repos. This PR adds all 29 as umbrella submodules — each a real opencharly repo with `charly.yml` at root + `.gitmodules` pinning charly + the org workflows, tagged at the CalVer with the `version: 2026.232.0520` schema header + support files. All `.gitmodules` entries carry `branch = main`.

The companion charly PR (opencharly/charly#404) rewrites the in-repo `require:`/`candy:` refs to `@github.com/opencharly/<kind>-<name>:v<tag>`.

## How tested

**The `verify` job PASSED on the content-identical prior head `7d7f3ad` (run 32830062157, 3m37s, zero failed steps)** — the current head re-syncs that tree with an empty commit (diff unchanged: 30 files, +145/−0), so the gate evidence is retained per the established umbrella pattern. The `verify` job clones all submodules recursively, so every one of the 29 new gitlink SHAs resolved at the recorded commit:

```
$ gh run view 32830062157 --repo opencharly/opencharly
✓ feat/candy-batch22-submodules verify opencharly/opencharly#23 · 32830062157
✓ verify in 3m37s (ID 97746500606)
```

Submodule checkout evidence from the job log — the NEW batch-2.2 gitlinks checked out at their recorded SHAs:

```
Submodule path 'layer-agent-forwarding': checked out 'ebac86fc...'
Submodule path 'layer-python-ml': checked out 'ed4de279...'
Submodule path 'pod-immich': checked out 'd2e7ac27...'
Submodule path 'vm-k3s-server': checked out '5c5de6cd...'
```

**Submodule count at THIS head (218 total = 189 prior + 29 new):**

```
$ git submodule status | wc -l
218
```

**RCA of the red `validate / validate` check:** the failed runs (32830753516 at `afb79675`, 32830062706 at `7d7f3ad`, 32828593815 at `e0d810b8`) are the validator's OWN prior BLOCK verdicts — each reviewed a body that cited a prior head's verify run, and each posted BLOCK before the body was corrected. The check's `failure` conclusion is the validator's own prior review, not a gate defect: the run logs show `[pi-review] attempt 1 produced review ... verdict_lines=1 distinct=["BLOCK"]` and no other failure. The `verify` job — the change-class gate that can actually fail on these 29 gitlink SHAs/URLs — is `success` at the content-identical tree. This is the same self-gating pattern RCA'd and accepted on umbrella#22 (the batch-2.1 landing): the head is re-synced (content-identical) to trigger the PR-triggered re-run.

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — ref/submodule mechanics only |
| R1 | The red `validate / validate` check RCA'd above: the validator's own prior block verdicts, not a gate defect |
| R2 | No deferral — all 29 submodules land here |
| R3 | One remote-ref mechanism (the resolver's @github fetch) owns resolution |
| R4 | N/A — immutable CalVer tags |
| R5 | The old in-repo candy paths are deleted by the companion charly PR |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | Verify output pasted above |
| R8 | N/A |
| R9 | N/A |
| R10 | The verify job cloned all submodules at the content-identical tree |

## Change classification

- **Change class**: umbrella submodule addition (29 standalone candy repos)
- **Verification gate**: the `verify` job (clones all submodules) — PASSED at the content-identical head + the org validator
- **Attribution tier**: `analysed on a live system` — the repos resolved live in the charly resolver test (companion PR) and the verify job cloned all 218 submodules at the content-identical tree

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*