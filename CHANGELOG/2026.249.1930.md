# 2026.249.1930 — chore(umbrella): sync gitlinks - charly + distro-omarchy + eval-omarchy (the Phase 3 from:name:tag wave)

## Summary

Umbrella gitlink sync for **the Phase 3 `from:name:tag` wave** — three gitlinks move, nothing else:

| gitlink | from | to | why |
|---|---|---|---|
| `charly` | `192096b` | `2734f43e` | charly's NEW merged main (opencharly/charly #549 — the `box/omarchy` re-vendor to 9805d53) |
| `distro-omarchy` | `962110d` | `9805d53` | the unified `from:name:tag` clone (distro-omarchy #46, merged default-branch HEAD) |
| `eval-omarchy` | `8349d9b` | `9c217d1` | merged default-branch HEAD (eval-omarchy #27 changelog) |

All three targets are merged refs (verified via `git branch -r --contains` against each submodule's `origin/main`) — pin-discipline clean. Every other previously-dirty gitlink (distro-cachyos, plugin-check, plugin-deploy-vm, plugin-vm, sdk, spec) was reset to its recorded pin via `git submodule update --checkout`; `git status` before commit showed ONLY the three gitlinks above.

## How tested

`task verify` on the final tree (`feat/phase3-umbrella-sync` @ 153b58f, only the 3 gitlinks changed):

```
$ task verify   # VERIFY_EXIT=0
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 400 submodules, policy B holds
```

## Rulebook compliance

- **R0** — `/charly-internals:git-workflow` loaded before any tool action.
- **R6** — PR-only landing; `feat/` branch off origin/main; no force-push, no hook bypass, no direct push to `main`.
- **R7** — the pinning gate (`task verify`) run on the final tree; output pasted above.
- **R10** — gate executed against the committed tree state (gitlinks staged exactly as committed).
- **Pin discipline / Policy B** — only merged refs pinned; policy B holds (6 distro pins equal charly's gitlinks, 400 submodules verified).

## Change classification

- **Class:** chore / gitlink sync (3 submodule pins)
- **Verification gate:** pasted (`task verify` GREEN on final tree)
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*