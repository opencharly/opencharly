# 2026.253.0119 — chore(umbrella): sync the eval-omarchy submodule pin to the charly-native eval loop

## Summary

Syncs the umbrella's recorded eval-omarchy submodule gitlink to the charly-native self-improving eval loop (eval-omarchy#57, merged).

The chain: the negative-control bed (every knownRed claim proven on the pristine golden — a check that passes without the PR is a FAKE assertion, caught at render), the verdict gates (a can't-test lane renders a setup-defect record, never a worthless eval), the typed agent contracts (the informed redo), the oracle-decided venue (the EVAL_CHANNEL env var is gone), the 9-skill corpus, and the pr-apply test-context closure. The R10 three-lane proof ran at zero failures (pasted in #57).

## How tested (pasted)

```
$ ./scripts/sync-gitlinks.sh
SYNC_EXIT=0 - sync-gitlinks: done
  eval-omarchy -> 64c29f6

$ git diff --cached --submodule=short eval-omarchy
Submodule eval-omarchy 1abe79b..64c29f6 (new commits)
```

Only the eval-omarchy gitlink is committed; the other submodules' worktree movements (other sessions' landings) were restored to their recorded shas.

## Rulebook compliance

- R0: git-workflow + the umbrella AGENTS.md loaded.
- R1: the chain's failures (the fake-assertion catch, the harness-failure classification) each RCA'd from live evidence in the upstream PR.
- R3: only the eval-omarchy gitlink is bumped.
- R4: no workarounds — the upstream fixes are root fixes.
- R6: no force-push, no hook bypass, PR-only landing.
- R7: the sync output + the consumer R10 pasted.
- R9: the pin is a merged ref, never a PR branch.
- R10: the consumer's three-lane proof at zero failures (referenced; pasted in #57).

## Change classification

- Change class: chore (submodule pin sync)
- Verification gate: the sync output + the consumer R10
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*