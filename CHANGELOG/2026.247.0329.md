# 2026.247.0329 — chore(gitlink): advance distro-omarchy to main (962110d — channel bootstrap fix #43)

## Summary

Advance the umbrella's `distro-omarchy` submodule gitlink to its default-branch HEAD (`962110d`), which includes the channel bootstrap fix (#43) — the rc/edge/dev golden bases are now provisioned, completing the four-channel golden-base provisioning (plan/omarchy-pr-validation-disk-link-gif.md, Phase 1).

Policy B: the umbrella's `distro-omarchy` now equals charly's own `box/omarchy` gitlink (962110d, advanced by charly #536) — the pre-commit gate passed.

## How tested

```
$ git ls-files -s distro-omarchy
160000 962110d 0  distro-omarchy
$ git -C distro-omarchy rev-parse origin/main
962110d
```

The gitlink names distro-omarchy's default-branch HEAD (verified). The pre-commit gate (policy B) passed: `pre-commit: OK`.

## Rulebook compliance

- **R1** — RCA: the gitlink was stale (48ce50cb, missing distro-omarchy #43); the umbrella's policy-B gate required charly's box/omarchy to advance first (charly #536). Fixed by this sync.
- **R2** — N/A: no cutover; a gitlink advance.
- **R3** — No duplication: the gitlink is the single pointer; no content copied.
- **R4** — No workarounds: sanctioned gitlink sync (the rulebook's "sync, not a hand-pin").
- **R5** — N/A: no legacy path removed.
- **R6** — Git safety: feat/ branch, no force-push, no direct push to main.
- **R7** — Gate: the pre-commit gate (policy B) passed; the gitlink resolves to the default-branch HEAD.
- **R8** — The gitlink is the submodule's default-branch HEAD (sync, not pin).
- **R9** — N/A: no R10 bed change.
- **R10** — The validated tree is the committed branch state.

## Change classification

- **Change class:** gitlink sync (chore).
- **Verification gate:** the pre-commit gate (policy B) + gitlink resolution to the default-branch HEAD.
- **Attribution tier:** analysed on a live system — the gitlink resolution ran live with retained output.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*