# 2026.235.1224 — feat(marketplace): drop the plugins submodule; harnesses load the standalone marketplace natively

## Summary

The **marketplace de-submodule cutover's umbrella leg** (charly#382 + marketplace#229 landed the generator, harness and corpus sides): the umbrella drops its `plugins` submodule and every harness loads the standalone **opencharly/marketplace** repo natively.

- `plugins` submodule removed (`.gitmodules` + checkout); `verify-pins.sh`/`sync-gitlinks.sh` drop the `[plugins]=plugins` policy-B entry and the skip-case — plugins now follows its own default-branch HEAD (the docs/sdk/spec precedent).
- `scripts/link-skills.sh` + the `.agents/` symlink farm + `.charly-profile.json` **deleted** — each harness loads the marketplace natively (Claude Code's `charly-plugins` marketplace, pi's `git:github.com/opencharly/marketplace` package, Kimi's plugin).
- `.claude/settings.json` registers the marketplace by its **github source** (`opencharly/marketplace`); `.reasonix` hook guards no longer probe a `plugins/` dir; `.pi/settings.json` gains the marketplace git package; the prompts/subagent/verify-status policy-B text, README, AGENTS.md and HARNESS-PARITY updated; `check-harness-parity.sh` drops the deleted `.agents/plugins/marketplace.json` shared entry.
- **marketplace joins the umbrella as a submodule** (per the operator's direction — every opencharly repo is checked out): `.gitmodules` gains `[submodule "marketplace"]` (branch main), the sync pins it to its own default-branch HEAD `e8efdb15` (the merged standalone corpus; the every-other-repo rule, like docs/sdk/spec). Harnesses still load the marketplace NATIVELY (the checkout is the org-snapshot view, exactly like the docs/sdk checkouts).
- Pins synced: charly → `fa254fd5` (the cutover main), docs → `e4dff7b` (docs#97 merged — the sync advanced it), marketplace → `e8efdb15`; `task verify` OK (23 submodules, policy B holds).

## How tested

**Sync on the FINAL committed tree (head 36558ec, pasted):**

```
$ bash scripts/sync-gitlinks.sh
  charly -> fa254fd5
  docs -> e4dff7b
  marketplace -> e8efdb1
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'
```

**Gates on the FINAL committed tree (pasted):**

```
$ task verify
verify-pins: OK — 23 submodules, policy B holds
OK: /home/atrawog/Sync/Atrapub/coder/pi/opencharly/.pi/extensions/umbrella-gates.ts parses (node type-strip pass)
$ task harness
check-harness-parity: OK — 5 shared harness files identical to charly/
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R0 | N/A — no skill-owned surface touched (scripts/README/harness mirrors; no skills edited) |
| R1 | N/A — no surfaced failure |
| R2 | N/A — no surfaced issue parked |
| R3 | sync-gitlinks.sh + verify-pins.sh remain the single pin mechanism; the parity script owns the hooks mirror |
| R4 | no workarounds — the sync is the policy's own operation |
| R5 | the plugins submodule, the symlink farm, link-skills.sh and every `plugins/`-pinned reference removed in this commit |
| R6 | status checked; no force-push; feat-branch + PR landing |
| R7 | sync + parity + verify outputs pasted above |
| R8 | N/A — no generation/emitted-artifact changes |
| R9 | N/A — no deploy target added or changed |
| R10 | the sync + verify + parity ran on the final committed tree |

## Change classification

- **Change class**: gitlink/submodule policy change + harness rewiring
- **Verification gate**: `task verify` (22 submodules, policy B) + `task harness` (5 shared files identical)
- **Attribution tier**: analysed on a live system — the sync + verify + parity executed live with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*