# 2026.253.1528 — chore: sync eval-omarchy gitlink (dca4079 — the Wave 6 lane integration)

## Summary

Syncs the eval-omarchy gitlink to dca4079 — the Wave 6 lane integration: the bed-corpus + the ledger-gate corpus facts + the selection vocabulary + the regenerated report. This is the umbrella-side record of the merged eval-omarchy main HEAD (PR #67, squash dca4079).

## How tested

- `git -C eval-omarchy rev-parse HEAD` = dca4079549e42116052405d2aa72b159544fc2be (the merged main HEAD)
- `git ls-files -s eval-omarchy` = 160000 dca4079... — the gitlink record matches the merged default-branch HEAD exactly
- pre-commit gate on the sync commit: check-policy-b OK (6 distro pins equal charly's gitlinks), check-harness-parity OK (5 shared harness files identical to charly/)
- The bumped submodule was exercised live on the disposable check-omarchy-eval-base-inst bed (eval-omarchy #67's three-lane R10, pasted below); the validator PASS on #67's head (dca4079) confirms the changed path executed live.

**Pasted R10 output (eval-omarchy #67, the bumped submodule's own PR):**

```
[stage corpus-gate] ok   ← the gate PASSED on the first pass (no redo)
[stage control] ok — check-run exit 0 (the negative control passed)
[stage eval] ok — check-run exit 2 (the PR's own test fails on the applied system — a REAL finding)
lane 10115: done
Upstream's own corpus surfaces ran: 25 (passed 25) — the charly-native equivalents of omarchy's own tests, per the omarchy-corpus plan in charly.yml.
```

## Rulebook compliance

- Rule 1: no edits inside a submodule — only the gitlink record changes
- Rule 2: git ops via `git -C` from the umbrella root
- Rule 5: pin discipline — pinned to the merged default-branch HEAD (dca4079), never a PR branch
- Rule 6: PR-only landing, no direct push to main (branch protection enforced)

## Change classification

- Change class: sync (gitlink record)
- Verification gate: pre-commit gate + validator
- Attribution tier: analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*