# 2026.243.0827 — chore: sync gitlinks — the desktop bed, a red main, and a completed pin batch

## Summary
Produced by task sync, not hand-pinned. Four gitlinks:

    charly           0e4d026d -> 8b194ed8   (its default branch)
    distro-omarchy   b82ed69e -> 2081960d   (policy B: exactly what charly pins)
    distro-cachyos   db2199a9 -> 677dbbe0   (policy B)
    distro-fedora    47cac269 -> 53bd54c0   (policy B)

Policy B verified independently of the script: each distro pin equals THE COMMIT CHARLY PINS,
which for cachyos and fedora is NOT their latest main - tag-on-merge added a CHANGELOG commit
past the tree charly composes, and pinning that would break the invariant.

## What it brings in

    charly          #508  the parse-time prescan never read an imported project's plugin pins,
                          plus box/omarchy, box/cachyos, box/fedora and plugin-check
    distro-omarchy  #11   check-omarchy-desktop-vm - the first bed proving the DESKTOP comes up
    distro-cachyos  #55   pin the cstream verb plugin so the bed parses when composed
                    #56   complete the pin advance charly#506 started
    distro-fedora   #47   the second copy of the same pin list

Two are worth naming. charly's main was RED: a bed that validated at exit 0 from its own repo
root failed at exit 1 the moment charly composed it, because the prescan walked the root
manifest and discover: paths but never import:. And charly#506 advanced 11 candy pins in one
copy of a list that exists as three independent copies, so three candies resolved at two
versions until every copy moved.

## Rebased onto a main that moved underneath

While this was open, main took eight commits including two AUTOMATED `chore: sync gitlinks` PRs
(#80, #81) and #69, which made seven cstream repos submodules. `origin/main` was merged in and
the two gitlink conflicts (`charly`, `distro-cachyos`) were resolved by RE-DERIVING from charly
rather than picking a side: `charly` to its current default-branch HEAD, `distro-cachyos` to the
commit that HEAD's `box/cachyos` records. The automated syncs ran before charly#508 merged, so
their values are strictly older; taking "theirs" would have silently reverted the fix.

`task verify` was re-run after the merge — 356 submodules now, up from 349, because of #69.

## Why only four gitlinks and not the 89 task sync moved

task sync advances EVERY submodule to its default-branch HEAD, and this run moved 89. The other
85 are unrelated to this work and opencharly#66 is already in flight covering that ground;
landing both would collide.

The four committed here are the values task sync produced - not hand-picked - and they are
exactly what Policy B requires: charly moved, so its distro pins must follow. The remaining 85
are left to #66 rather than raced.

## How tested

    task verify
    check-policy-b: OK - 6 distro pins equal charly's gitlinks
    verify-pins: OK - 356 submodules, policy B holds
    exit 0

The umbrella's own pre-commit gate ran on this commit:

```
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

The commit is exactly four files:

```
$ git show --stat HEAD
 charly         | 2 +-
 distro-cachyos | 2 +-
 distro-fedora  | 2 +-
 distro-omarchy | 2 +-
 4 files changed, 4 insertions(+), 4 deletions(-)
```

## Rulebook compliance

- **R1 — RCA every anomaly.** Every pin here exists because a failure was root-caused: a red
  `charly box validate` on main (prescan never read `import:`), a bed whose setup could never run
  (mutating steps on the wrong timeline), a debug hint naming the wrong identity, and two
  partial-repin version splits. None was allowlisted or deferred.
- **R2 — Nothing parked.** No "pre-existing" or "out of scope" classification anywhere in the
  chain — including charly#506's incomplete pin batch, which surfaced mid-cutover and was
  finished rather than attributed elsewhere. The 85 unrelated submodule moves are not parked
  work: they are a DIFFERENT cutover already in flight as opencharly#66, named explicitly.
- **R3 — No duplication.** Gitlink moves only. The one duplication found — `charly-marketplace`
  as three independent copies of one pin list — is named as a finding in
  opencharly/distro-fedora#47, not silently worked around.
- **R4 — No workarounds.** These are the values `task sync` produced; committing a subset of a
  sync's output is not a hand-pin, and Policy B is verified against charly's own gitlinks
  independently of the script.
- **R5 — Delete legacy completely.** N/A — pin moves.
- **R6 — Git safety.** `git status` inspected before and after the restore of the 85 unrelated
  moves; branched from a fresh `main`; no force-push, no hook bypass, no direct push to `main`;
  the pre-commit gate ran and passed rather than being bypassed.
- **R7 — Prove the gate, not the plan.** `task verify` output pasted verbatim on the final tree.
- **R10 — Fresh disposable proof.** The bed this wires in is **`check-omarchy-desktop-vm`**,
  marked `disposable: true` in `distro-omarchy/charly.yml` at the pinned commit. Its full R10 run
  is in opencharly/distro-omarchy#11 — `PASS (steps=9)`, with `check-live` and
  `check-live-rebuild` both `16 steps: 14 passed, 0 failed, 2 skipped` and zero non-allowlisted
  warnings, after `[update]` destroyed the guest and rebuilt it from nothing.

## Change classification

| | |
|---|---|
| **Change class** | umbrella gitlink sync — four submodule pins, no file content changes |
| **Verification gate** | `task verify` (Policy B across all 356 submodules) plus the umbrella's own pre-commit gate, both pasted |
| **Attribution tier** | `fully tested and validated` — the pinning gate passed on the final tree, and every repo pinned here was gated in its own PR (R10 bed for omarchy; `charly box validate` at zero multi-version warnings plus `go test ./charly/...` for charly) |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*