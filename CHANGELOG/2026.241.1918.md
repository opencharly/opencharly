# 2026.241.1918 — feat(pi): replace the injected rules copy with an operating-principles complement

## Summary

Replaces the pi system prompt's injected rules block in `.pi/extensions/umbrella-gates.ts` from a hand-written condensed **copy** of AGENTS.md into an **operating-principles + tool-usage complement**.

**Why:** the old block was an R3 violation (policy duplicated in prose outside its owner), had drifted (added "Validator Verdict Discipline" not in AGENTS.md; missed the 8-rule Rulebook and Command hygiene), and duplicated what pi already auto-loads (the full AGENTS.md is in `project_context`).

**The new block** references AGENTS.md by name/section/skill instead of re-stating rules — it cannot drift because it holds no rule text:
- **AGENTS.md is the authoritative rulebook** — always follow it; prioritize clean architecture, fully tested, deduplicated code
- **All decisions come from AGENTS.md** — ambiguous choices resolve from it or surface as blockers
- **Goal-driven autonomous completion** — `create_goal` at task start, loop until the goal is met
- **Completion is defined, not felt** — only when all todos/tasks are done and verified, or a blocker not fixable solely from AGENTS.md
- **RCA every anomaly (R1) — enforced, no exceptions**
- **Spike on a check bed — never guess** — settle uncertainty on a disposable `charly check` bed, verify live
- **Tool usage section** — `todo`, `create_goal`, `subagent`, `gh_pr_status`, `umbrella_load_skills`, `memory_*`, `web_search`/`fabric_exec`/`mcp.*`, `charly check` beds

## How tested

All tests run via `pi -p` (non-interactive CLI) from the committed tree.

**System prompt audit** (live session reporting its own prompt):
```
1. YES — '## Operating principles' with all 10 headings (AGENTS.md authoritative,
   decisions from AGENTS.md, goal-driven, completion defined, skills first,
   RCA enforced, spike on check bed, prove the gate, validator verdicts, command hygiene)
2. YES — '## Tool usage' with all 8 lead-ins (todo, create_goal, subagent,
   gh_pr_status, umbrella_load_skills, memory_*, web_search/fabric_exec/mcp, charly check beds)
3. "Always follow the rules in AGENTS.md when writing or modifying new code, and
   always prioritize a clean architecture and fully tested and deduplicated code over anything else."
4. NO — '## Umbrella Engineering Rules' section gone (no drift)
5. NO — no duplicated rule text (R6/rulebook text not re-stated)
6. create_goal: YES; gh_pr_status: YES; todo: YES; subagent: YES
```

**Functional tool tests** (all four advertised tools executed live):
```
TEST1 — create_goal OK: goal created, status active
TEST2 — umbrella_load_skills OK: charly-internals:git-workflow content returned
TEST3 — gh_pr_status OK: opencharly/opencharly#42 state MERGED, verdict PASS
TEST4 — todo OK: task created (pending)
```

**Static content check** (node strip-types): all 19 assertions pass — every section present, verbatim principle present, old drift section absent, no rule-text duplication.

**Pre-commit gate:**
```
check-policy-b: OK — 5 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

## Rulebook compliance

| Rule | Status |
|---|---|
| R3 (no duplication) | Injected block holds no rule text — references AGENTS.md by name/section/skill; cannot drift |
| R6 (git safety) | `feat/` branch, push only, no force-push |
| R7 (gate proven) | Live `pi -p` audit + functional tests + pre-commit gate pasted above |
| R10 (fresh tree) | All tests run from the committed tree |
| Rule 8 (harness parity) | `.pi/extensions/umbrella-gates.ts` is a fork (not in parity `SHARED` list); `check-harness-parity` OK |

## Change classification

- **Change class:** harness config (pi extension system-prompt injection)
- **Verification gate:** live `pi -p` system-prompt audit + functional tool tests + pre-commit gate
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*