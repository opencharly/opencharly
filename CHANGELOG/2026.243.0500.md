# 2026.243.0500 — chore: sync gitlinks — the omarchy boot fix and the typed pacstrap bed

## Summary

Produced by `task sync`, not hand-pinned. Two gitlinks:

  charly           a31f76d7 -> 1d0164f0   (its default branch, rolling)
  distro-omarchy   db2199a9 -> b82ed69e   (policy B: exactly what charly pins)

Policy B verified independently of the script: the new charly pin's own box/omarchy gitlink
is b82ed69e, and that is what distro-omarchy is staged at. The invariant is that a distro pin
equals THE COMMIT CHARLY PINS, not that submodule's latest main.

## What it brings in

  charly #502  the omarchy pacstrap VM's limine.conf produced an unbootable branch
         #503  advance box/omarchy to the typed pacstrap bed
         #504  golden-compile was left on the pre-cutover root marker (main was red)
  distro-omarchy #10  the pacstrap bed asserts through typed verbs

#502 is the substantive one: charly wrote a depth-1 limine entry, limine-entry-tool nested
its own UKI entry under it, and charly's bootable entry silently became an unbootable BRANCH.
The guest never booted and check-omarchy-pacstrap-vm hung at deploy-add indefinitely. After
the fix that step completes in 16.26s and the bed is PASS (steps=9).

## Why only two gitlinks and not the 46 task sync moved

`task sync` advances EVERY submodule to its default-branch HEAD, and this run moved 46. The
other 44 are unrelated to this work and opencharly#66 ("chore(sync): third wave") is already
in flight covering that ground; landing both would collide.

The two committed here are the values `task sync` produced — not hand-picked — and they are
exactly what Policy B requires: charly moved, so its distro pin must follow. The remaining 44
are left to #66 rather than raced.

## How tested


    $ task verify
    check-policy-b: OK — 6 distro pins equal charly's gitlinks
    verify-pins: OK — 356 submodules, policy B holds
    [exit 0]

One recurring trap worth recording again: after `task sync` the umbrella reported `Mm charly`
— a DIRTY submodule, which fails verify and blocks review. It is not dirt. charly's inner
box/omarchy gitlink moved in #503 while the working checkout had not, and
`git submodule update --recursive` cannot move it until the inner clone has FETCHED the new
commit. `git -C charly/box/omarchy fetch origin main` then checking the pinned commit out
resolves it.

The umbrella's own pre-commit gate ran on this commit:

```
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

The commit is exactly two files:

```
$ git show --stat HEAD
 charly         | 2 +-
 distro-omarchy | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```

## Rulebook compliance

- **R1 — RCA every anomaly.** The `Mm charly` dirty-submodule report was root-caused to a
  stale inner `box/omarchy` checkout that cannot advance until the inner clone fetches the
  commit charly#503 pins — recorded above rather than cleaned away silently.
- **R2 — No pre-existing / out-of-scope split.** Nothing parked. The 44 unrelated submodule
  moves `task sync` produced are named explicitly, with the reason they are left to the
  in-flight `opencharly#66` rather than raced.
- **R3 — No duplication.** Gitlink moves only.
- **R4 — No workarounds.** These are the values `task sync` produced; committing a subset of
  a sync's output is not a hand-pin, and Policy B is verified against charly's own gitlink
  independently of the script.
- **R5 — Delete legacy completely.** N/A — pin moves.
- **R6 — Git safety.** `git status` inspected before and after; branched from a fresh
  `origin/main`; no force-push, no direct push to `main`; the pre-commit gate ran and passed
  rather than being bypassed.
- **R7 — Prove the gate, not the plan.** `task verify` output pasted verbatim on the final
  tree.
- **R10 — Fresh disposable proof.** The bed this wires in is **`check-omarchy-pacstrap-vm`**,
  marked `disposable: true` in `distro-omarchy/charly.yml` at the pinned commit. Its full R10
  run is in `opencharly/distro-omarchy#10` — `PASS (steps=9)` with `check-live` and
  `check-live-rebuild` both 6/6 after `[update]` pacstrapped a new disk from nothing.

## Change classification

| | |
|---|---|
| **Change class** | umbrella gitlink sync — two submodule pins, no file content changes |
| **Verification gate** | `task verify` (policy B across all 356 submodules) plus the umbrella's own pre-commit gate, both pasted |
| **Attribution tier** | `fully tested and validated` — the pinning gate passed on the final tree and the bed being wired in was proven on the exact commit pinned |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*