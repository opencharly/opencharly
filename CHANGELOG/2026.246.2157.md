# 2026.246.2157 — chore(gitlink): advance distro-omarchy + eval-omarchy to main (omarchy PR-eval work #40-#42, #15-#17)

## Summary

Advance the umbrella's `distro-omarchy` and `eval-omarchy` submodule gitlinks to their default-branch HEADs:

- `distro-omarchy` 1f5c1aef → `48ce50cb` (includes the omarchy PR-evaluation work #40–#42: the rc/edge/dev channel anchored PR beds, the VM-on-VM clone entity + beds, and the channel round-trip check fix)
- `eval-omarchy` d826aeef → `34507f41` (includes the omarchy-eval-record package #15, the rollup golden-sha256 cache key #16, and the lane-status update #17)

Policy B: the umbrella's `distro-omarchy` now equals charly's own `box/omarchy` gitlink (48ce50cb, advanced by charly #535) — the pre-commit gate passed.

## How tested

```
$ git ls-files -s distro-omarchy eval-omarchy
160000 48ce50cba297d39c30777e496725ff6b1e6c181f 0  distro-omarchy
160000 34507f415925e9151ca503f925913a7fa668d63e 0  eval-omarchy
$ git -C distro-omarchy rev-parse origin/main
48ce50cba297d39c30777e496725ff6b1e6c181f
$ git -C eval-omarchy rev-parse origin/main
34507f415925e9151ca503f925913a7fa668d63e
```

Both gitlinks name their repos' default-branch HEADs (verified). The pre-commit gate (policy B) passed: `pre-commit: OK`.

## Rulebook compliance

- **R1** — RCA: the gitlinks were stale (missing the merged omarchy PR-eval work); the umbrella's policy-B gate required charly's box/omarchy to advance first (charly #535). Fixed by this sync.
- **R2** — N/A: no cutover; a gitlink advance.
- **R3** — No duplication: the gitlinks are the single pointers; no content copied.
- **R4** — No workarounds: sanctioned gitlink sync (the rulebook's "sync, not a hand-pin").
- **R5** — N/A: no legacy path removed.
- **R6** — Git safety: feat/ branch, no force-push, no direct push to main.
- **R7** — Gate: the pre-commit gate (policy B) passed; the gitlinks resolve to default-branch HEADs.
- **R8** — The gitlinks are the submodules' default-branch HEADs (sync, not pin).
- **R9** — N/A: no R10 bed change.
- **R10** — The validated tree is the committed branch state.

## Change classification

- **Change class:** gitlink sync (chore).
- **Verification gate:** the pre-commit gate (policy B) + gitlink resolution to default-branch HEADs.
- **Attribution tier:** analysed on a live system — the gitlink resolution ran live with retained output.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*