# 2026.254.0945 — chore: sync charly/docs gitlinks to the docs-regeneration landings

## Summary

Syncs the umbrella's gitlinks to the docs-regeneration landings (the org-wide docs rearchitecture shipped across charly/spec/sdk/plugin-docs/plugin-harness-kind/marketplace/docs):

- **charly**: `e19198ca` → `d507ca5a` (the charly main carrying the top-level `docs:` config node + the release/extra corpus feeds and the brand cutovers).
- **docs**: `453a4aa` → `91ce25f0` (the regenerated opencharly.ai site, post-#115).
- **marketplace**: already current (`44fa7db`) — no-op.

All 400 submodules were re-pinned by the repo's own `task sync` / `scripts/sync-gitlinks.sh`; only these 2 gitlinks drifted — the other 398 pins were already current.

## How tested

From the pushed tree (branch feat/sync-docs-wonky @ bd21095806f65c373aab06793a2ce646e38ed265):

```
$ task verify
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 400 submodules, policy B holds
```

GREEN, no warnings. `task hooks` pre-commit gate passed on the commit (policy B + harness parity OK). Gitlink diff = exactly 2 entries (charly, docs).

## Rulebook compliance

- Policy B — distro-* pins equal charly's gitlinks; the sync is charly's own recorded set, no hand-pins (a hand-pin would be a violation).
- R7 — the full pinning gate (`task verify`) ran green on the final tree.
- R6/Git — no force-push, no direct-main push; sync via the repo's own scripts.

## Change classification

- **Change class:** submodule gitlink sync (view update).
- **Verification gate:** `task verify` (policy B + 400 pins) on the final tree.
- **Attribution tier:** analysed on a live system — the sync and gate ran live with retained output.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*