# 2026.242.2315 — chore: sync gitlinks — the typed omarchy beds reach the umbrella

## Summary

`task sync`, not a hand-pin. Three gitlinks advance, and only three:

  charly           a8875f4b -> 0e4d026d   (its default branch, rolling)
  distro-omarchy   2f5e5c1b -> db2199a9   (policy B: exactly what charly pins)
  marketplace      db0593e5 -> 11380419   (its own default branch)

Policy B verified independently of the script rather than taken on trust: the new charly
pin's own box/omarchy gitlink is db2199a9, and that is what distro-omarchy is staged at. The
invariant is that a distro pin equals THE COMMIT CHARLY PINS — not that submodule's latest
main, which is a different and wrong comparison.

## What it brings in

  distro-omarchy #8  the omarchy beds assert through typed verbs, not shell
                 #9  upstream's system and cups acceptance suites, ported
  charly        #497 the plugin-vm pin that made `vm build omarchy-vm` possible at all
                #498 repins the four check verbs that gained fields
                #499 advances box/omarchy to the above
  marketplace   #255 projects the moved wl skills and the new omarchy skills

check-omarchy-iso-vm goes from 28 assertions to 52, and from 43 `command:` shell steps
across three beds to 4 in this one.

## How tested


    $ task verify
    check-policy-b: OK — 6 distro pins equal charly's gitlinks
    verify-pins: OK — 349 submodules, policy B holds
    [exit 0]

One thing worth recording, because it cost time and looks like a defect when it is not:
after `task sync` the umbrella reported `Mm charly` — a DIRTY submodule, which fails verify
and is a review blocker. It was not dirt. charly's own inner `box/omarchy` gitlink had moved
to db2199a9 in #499, while the working checkout was still at 2f5e5c1b, and
`git submodule update --recursive` could not move it until the inner clone had fetched the
new commit. `git -C charly/box/omarchy fetch origin main` then checking the pinned commit out
directly resolves it, after which charly is clean and verify passes.

The bed itself was proven on the exact distro-omarchy commit this pins, in
opencharly/distro-omarchy#9: `check-omarchy-iso-vm` (`disposable: true`) at
`PASS (steps=9)`, with check-live and check-live-rebuild both at
`52 steps: 52 passed, 0 failed, 0 skipped`.

The umbrella's own pre-commit gate also ran on this commit:

```
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

## Rulebook compliance

- **R1 — RCA every anomaly.** The `Mm charly` dirty-submodule report was root-caused to a
  stale inner `box/omarchy` checkout that could not advance until the inner clone fetched
  the commit charly#499 pins — not treated as dirt to be cleaned away, and recorded above so
  the next reader does not re-derive it.
- **R2 — No pre-existing / out-of-scope split.** Nothing parked. Only three gitlinks move and
  each is named with its reason; `marketplace` is named explicitly rather than left to be
  discovered in the diff.
- **R3 — No duplication.** A sync; nothing restated.
- **R4 — No workarounds.** `task sync` produced these pins — this is not a hand-pin, which
  is what the umbrella rulebook forbids for exactly this case.
- **R5 — Delete legacy completely.** N/A — pin moves only.
- **R6 — Git safety.** `git status` inspected before and after; branched from a fresh
  `origin/main`; no force-push, no direct push to `main`, no hook bypass — the pre-commit
  gate ran and passed rather than being skipped.
- **R7 — Prove the gate, not the plan.** `task verify` output pasted verbatim; a green
  `git status` alone would prove nothing.
- **R10 — Fresh disposable proof.** The bed this wires in is `check-omarchy-iso-vm`, marked
  `disposable: true`, proven on the exact `distro-omarchy` commit pinned here in
  `opencharly/distro-omarchy#9` — `PASS (steps=9)` with 52/52 after the destroy-and-reinstall.

## Change classification

| | |
|---|---|
| **Change class** | umbrella gitlink sync — three submodule pins, no file content changes |
| **Verification gate** | `task verify` (the full pinning gate: policy B across all 349 submodules) plus the umbrella's own pre-commit gate, both pasted |
| **Attribution tier** | `fully tested and validated` — the pinning gate passed on the final tree, and the bed being wired in was proven on the exact commit pinned |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (fully tested and validated)*