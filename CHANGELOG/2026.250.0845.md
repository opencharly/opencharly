# 2026.250.0845 — chore(umbrella): sync charly + plugin-fleet gitlinks to the CC-2 tags

## Summary

The **CC-2 umbrella sync** for the fleet-vocabulary retirement wave. The umbrella recorded two stale gitlinks, each one release behind the freshly merged CC-2 tags:

| submodule | before (main) | after (this PR) | CC-2 tag |
|---|---|---|---|
| charly | 37fedada (CHANGELOG for PR #552 / v2026.250.0450) | 2a206c79 (CHANGELOG for PR #554 / tag **v2026.250.0820**) | v2026.250.0820 — the `charly fleet add/del` → `charly deploy add/del` retirement surface (PR #554) |
| plugin-fleet | 3c228451 (CHANGELOG for PR #16 / v0.2026250.558) | daf19c2 (CHANGELOG for PR #17 / tag **v2026.250.0829**) | v2026.250.0829 — the CC-2 contract pins on top of #15/#16 (PR #17) |

**sdk and spec: already current — no bump needed.** Resolved against upstream default-branch HEADs (the umbrella pins default-branch HEADs, the post-tag CHANGELOG commit, per the every-other-repo rule):
- sdk = f92239d (CHANGELOG for v0.2026250.758 / PR #234) == upstream `origin/main`
- spec = 7c05448 (CHANGELOG for v0.2026250.709 / PR #115) == upstream `origin/main`

Touched exactly two gitlinks; nothing else moved (git status shows only `M charly` + `M plugin-fleet`).

## How tested

```
$ git status --porcelain
M  charly
M  plugin-fleet
$ git diff --cached --stat
 charly       | 2 +-
 plugin-fleet | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)
```

Policy B pre-check (charly@2a206c79 box/* gitlinks == umbrella distro-* pins, byte-for-byte):

```
$ bash scripts/check-policy-b.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
$ bash scripts/check-harness-parity.sh
check-harness-parity: OK — 5 shared harness files identical to charly/
```

Full umbrella gate on the final committed tree (HEAD 76b0aca, all 400 submodule worktrees aligned via `git submodule update --recursive` — R10 fresh proof):

```
$ bash scripts/verify-pins.sh   # task verify
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 400 submodules, policy B holds
```

(exit 0)

## Rulebook compliance

- **R0 (skills first):** `git-workflow` skill (branch-per-change, PR-only landing, pin policy, `task verify`) loaded before the first tool call.
- **R1 (RCA every anomaly):** the local checkout's dirty submodules were RCA'd as a stale worktree from an unmerged older wave — not a product anomaly; sdk/spec "already current" is a resolved fact (upstream HEAD comparison), not an assumption.
- **R2 (finish the whole cutover):** the cutover content itself landed upstream (charly #554, plugin-fleet #17); this PR finishes the umbrella's record of the wave — the only two stale CC-2 pointers.
- **R3 (no duplication):** gitlinks bumped with the same semantics as `scripts/sync-gitlinks.sh` (fetch → detach at the default-branch ref → stage), verified with the existing gate scripts — nothing re-implemented.
- **R4 (no workarounds):** no sleeps, retries, suppressions, or hand-pins; both targets are merged default-branch state (never a PR branch / dangling pin). The one-shot detached invocation of the gate was pure local tooling (120s call cap), not a workaround.
- **R5 (delete legacy completely):** N/A — the legacy `fleet` word was deleted inside charly #554; nothing to delete in the umbrella (gitlink-only change).
- **R6 (git safety):** `git status` inspected before every destructive step; no force-push, no direct main push, no hook bypass; committed with `core.hooksPath=hooks` live (pre-commit ran policy B + parity).
- **R7 (prove behavior, not compilation):** the umbrella's gate is `task verify` (pinning/cleanliness/policy-B audit) — run and pasted above on the final tree; no code changed, so no test units apply.
- **R8 (preserve emitted artifacts):** N/A — no labels/plans/configs emitted; the change is two 160000-mode gitlinks.
- **R9 (binary equals source):** N/A — no binary built.
- **R10 (fresh disposable proof):** gate run from the final committed tree with all submodule worktrees aligned to the gitlinks, not from an edited state.

## Change classification

- **Change class:** submodule gitlink sync (chore; two 160000-mode pointer records, no tree-file change)
- **Verification gate:** `task verify` (full umbrella pinning gate: 400-submodule default-branch audit + cleanliness + policy B + charly nested) — passed
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*