# 2026.241.1540 — chore: sync submodule gitlinks (policy B) — advance charly + the distro pins to the #437 fix heads

## Summary

Policy-B submodule gitlink sync (the umbrella records gitlinks; the owning repos land their own changes). Advances 19 submodule pins to their upstream main heads:

- **charly → b9eac81a (v2026.241.1535)** — charly#458 advanced the charly repo's five `import:` distro pins + five `box/*` submodule gitlinks to the FIXED distro tags (distro-arch#31, distro-cachyos#34, distro-debian#21, distro-fedora#44, distro-ubuntu#21), eliminating the 564 `remote candy name conflict` errors in `charly box validate` (opencharly/charly#437). Root cause: the charly repo's distro pins pointed at pre-cutover distro tags that still carried `@github.com/opencharly/charly/candy/<name>` refs, resolving an old charly tag that still ships those candies.
- **distro-arch / distro-cachyos / distro-debian / distro-fedora / distro-ubuntu** → fixed heads (the `charly/candy/` refs are gone org-wide).
- **marketplace + 12 plugins + sdk + spec** → current main heads.

## How tested

```
$ cd /home/atrawog/Sync/Atrapub/coder/pi/opencharly && charly box validate
exit=0
remote candy name conflict: 0
unknown candy: 0
```

The umbrella roots into charly's project; `charly box validate` on this tree: **exit 0, 0 validation errors** (was 564 conflicts before the sync). Also verified per-repo: distro-arch/cachyos/debian/fedora/ubuntu, marketplace, and docs all show **0** `charly/candy/` old-path refs on their main heads.

Also run: `check-policy-b: OK — 5 distro pins equal charly's gitlinks` (in the pre-commit gate).

## Rulebook compliance

| Rule | Status |
|---|---|
| R1 (RCA every anomaly) | RCA in commit: stale distro pins → pre-cutover charly tag still providing candies → named collision |
| R3 (no duplication) | Pure gitlink sync; no re-implementation |
| R4 (no workarounds) | `task sync`-driven advance, not a hand-pin |
| R6 (git safety) | `feat/` branch on the umbrella, push only, no force-push |
| R7 (gate proven) | `charly box validate` exit 0 + pre-commit gates (check-policy-b, harness parity) passed |
| R10 (fresh tree) | Validate from the committed tree |

## Change classification

- **Change class:** pin sync (19 submodule gitlinks, policy B)
- **Verification gate:** `charly box validate` full pass + `check-policy-b`
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*