# 2026.246.2001 — chore(gitlink): advance charly + distro-omarchy to main (omarchy PR-eval work)

## Summary

Advance the umbrella's `charly` and `distro-omarchy` submodule gitlinks to their default-branch HEADs:

- `charly` 110f284e → `b3aa91b6` (includes the plugin-vm v0.2026246.545 clone-wiring pin #532 and the box/omarchy re-vendor #534)
- `distro-omarchy` 1bc53b7 → `1f5c1aef` (includes the omarchy PR-evaluation beds #36–#39: layer-asciinema v2026.246.1622 pin, doc-violation fixes, the snapshot-anchored PR bed, and the record:gif evidence bed)

Policy B: the umbrella's `distro-omarchy` now equals charly's own `box/omarchy` gitlink (1f5c1aef, advanced by charly #534) — the pre-commit gate passed.

## How tested

```
$ git ls-files -s charly distro-omarchy
160000 b3aa91b6fe2fdcc40d411199c738b2728fd68df5 0  charly
160000 1f5c1aef83acab254c4e81ce721d0586b8e7bdb5 0  distro-omarchy
$ git -C charly rev-parse origin/main
b3aa91b6fe2fdcc40d411199c738b2728fd68df5
$ git -C distro-omarchy rev-parse origin/main
1f5c1aef83acab254c4e81ce721d0586b8e7bdb5
```

Both gitlinks name their repos' default-branch HEADs (verified). The pre-commit gate (policy B) passed: `pre-commit: OK`.

## Rulebook compliance

- **R1** — RCA: the gitlinks were stale (missing the merged omarchy PR-eval work); the umbrella's policy-B gate required charly's box/omarchy to advance first (charly #534). Fixed by this sync.
- **R2** — N/A: no cutover; a gitlink advance.
- **R3** — No duplication: the gitlinks are the single pointers; no content copied.
- **R4** — No workarounds: sanctioned gitlink sync (the rulebook's "sync, not a hand-pin").
- **R5** — N/A: no legacy path removed.
- **R6** — Git safety: feat/ branch, no force-push, no direct push to main.
- **R7** — Gate: the pre-commit gate (policy B) passed; the gitlinks resolve to default-branch HEADs.
- **R8** — The gitlinks are the submodules' default-branch HEADs (sync, not pin).
- **R9** — N/A: no R10 bed change.
- **R10** — The validated tree is the committed branch state.

## Change classification

- **Change class:** gitlink sync (chore).
- **Verification gate:** the pre-commit gate (policy B) + gitlink resolution to default-branch HEADs.
- **Attribution tier:** analysed on a live system — the gitlink resolution ran live with retained output.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*