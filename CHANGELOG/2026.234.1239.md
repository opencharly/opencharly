# CHANGELOG placeholder — will be finalized to merge-time CalVer by pr-validator

## Fix

- Fix unescaped backticks in `.pi/extensions/umbrella-gates.ts` that caused
  `ParseError: Missing semicolon` on pi extension load.
- Add `.pi/npm/` to `.gitignore` to match charly's ignore list.
