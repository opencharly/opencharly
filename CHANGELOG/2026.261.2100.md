# 2026.261.2100 — chore: sync gitlinks — the docs-regeneration chain

## Summary

Advances the four umbrella gitlinks that moved after the docs-regeneration chain merged.

- `docs`: `d6a313527` → `0835a7aa9` (the site regeneration; `docs#128`).
- `layer-charly-build`: `0d76aa23e` → `d3d7aaca5` (the pin-agnostic + corrected docs skill; `layer-charly-build#12`/`#13`).
- `marketplace`: `df0242e5a` → `54f778395` (the regenerated corpus; `marketplace#316`/`#317`).
- `plugin-docs`: `b5cfda0ca` → `974439087` (the skill dedupe + lint + candy-gate repair; `plugin-docs#9`).

All four are the standard every-other-repo default-branch-HEAD pins; no charly-pinned (`distro-*`) pin moves. Produced by `task sync` (`scripts/sync-gitlinks.sh`), not hand-edited.

## How tested

The umbrella's pinning gate, on the committed tree with all submodules initialized:

```console
$ bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 403 submodules, policy B holds
```

The per-commit hook ran on the commit: `pre-commit: OK`.

Each new pin equals its repo's current default-branch HEAD:

```console
$ for p in docs layer-charly-build marketplace plugin-docs; do
    echo "$p gitlink=$(git ls-files -s "$p" | awk '{print $2}') main=$(gh api repos/opencharly/$p/commits/main --jq .sha)"; done
docs               gitlink=0835a7aa9 main=0835a7aa9...
layer-charly-build gitlink=d3d7aaca5 main=d3d7aaca5...
marketplace        gitlink=54f778395 main=54f778395...
plugin-docs        gitlink=974439087 main=974439087...
```

## Rulebook compliance

- **R0 skills:** `/charly-internals:git-workflow`.
- **R1 RCA + zero warnings:** the drift is the documented post-merge lag (each content PR's `tag-on-merge` CHANGELOG commit lands after the merge). No warnings; the pre-commit hook is clean.
- **R2 no out-of-scope:** the four advanced gitlinks are the whole change.
- **R3 no duplication:** produced by the canonical `scripts/sync-gitlinks.sh`.
- **R4 no workaround:** pins move to merged default-branch HEADs; no PR-branch or dangling pin.
- **R5 hard cutover + grep:** `git status` after `task sync` shows exactly the four expected gitlinks.
- **R6 git safety:** dedicated linked worktree off fresh `origin/main`; hook passed; no force push, no hooks bypass, no direct push to `main`.
- **R7 prove the gate:** `verify-pins.sh` (the `task verify` body) green on the committed tree, pasted above.
- **R8/R9:** N/A — gitlinks only.
- **R10 fresh disposable proof:** the gate ran on the committed worktree with all submodules initialized; no check bed applies (`N/A — pinning change`).
- **Policy B:** unchanged — `6 distro pins equal charly's gitlinks`; only the every-other-repo pins moved.
- **CHANGELOG:** written by `tag-on-merge` from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*