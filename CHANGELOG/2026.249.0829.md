# 2026.249.0829 — chore: sync plugin-check gitlink per policy B (instrument-architecture wave, #25)

## Summary

Policy B gitlink sync: **plugin-check 1b6274f → 3a24a6f** (the cachyos binutils/libtool local-newer allowlist + gating tests, PR opencharly/plugin-check #25, MERGED). All other umbrella pins were already current on origin/main.

## How tested

```
$ task sync
== everything else (own default-branch HEAD) ==
  plugin-check -> 3a24a6f
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'
$ git status --short   # only the plugin-check gitlink bump
```

Pinned ref is the merged main (gh pr view #25 = MERGED); distro-* untouched (already at charly's box/* pins per policy B).

## Rulebook compliance

- Rule 1: no submodule edits — gitlinks only; the owning repo landed its change via its own PR.
- Rule 2: git ops from the umbrella root via `git -C`.
- Rule 5: only merged refs pinned (plugin-check #25 state=MERGED).
- Rule 6: policy B contract via `sync-gitlinks.sh` + `policy-b.sh`.

## Change classification

Class: gitlink sync (mechanical). Gate: `task sync` ran clean. Attribution: fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*