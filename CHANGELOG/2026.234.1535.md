# 2026.234.1535 — chore: sync dotgithub pin to main

## Summary

Syncs the `dotgithub` submodule pin to `opencharly/.github` main HEAD (44edadd), which moved through #68 (pi-review v1.0.10 + body-as-changelog), #69 (bot-token write), #70 (tag/CHANGELOG decoupling), and #71 (raw-fetch write). The umbrella's gitlink must track the owning repo's default branch (policy B).

### Diff items (complete)

- `dotgithub` submodule pointer: 12ceb38 → 44edadd.

## How tested

```
$ task verify
verify-pins: OK — 23 submodules, policy B holds
```

The pin equals `opencharly/.github` main HEAD (verified via `git ls-remote`); the underlying org-wide workflows (#67–#71) each landed green through the org validator and their CHANGELOG-from-PR-body mechanism is verified live (sweep run 32581903742 created CHANGELOG/2026.234.1251.md, 1110.md, 1005.md from PR bodies).

## Rulebook compliance

| Rule | Status | Evidence |
|---|---|---|
| 1. No edits inside submodules | ✅ | Only the gitlink pointer changed |
| 2. Submodule git via `git -C` | ✅ | dotgithub fetched/checked via `git -C dotgithub` |
| 5. Only merged refs pinned | ✅ | 44edadd = merged #71 on main |
| 6. Policy B equality | ✅ | `task verify` green (sdk/spec/plugins/docs/distro-* == charly) |
| R5 (delete legacy) | ✅ | N/A — pin sync only |
| R6 (git safety) | ✅ | chore/ branch, no force-push |

## Change Classification

- **Change class**: chore (pin sync)
- **Verification gate**: `task verify`
- **Attribution tier**: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*