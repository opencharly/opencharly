# 2026.251.0927 — chore: close-out — eval-wave gitlinks + pi-omarchy-eval pin v2026.251.0738

## Summary

Close-out of the omarchy eval + validator-fix wave — six entries, NO workflow change (the earlier branch revision's dispatcher permission line is reverted out of this PR):

| Entry | To | Merged wave PRs |
|---|---|---|
| eval-omarchy gitlink | 41174c45 | #35–#41 (restore, legacy cut, full loop, skills at .agents/skills/, dispatcher dedupe) |
| marketplace gitlink | 73596755 | #290, #292, #293 (corpus) |
| layer-charly-internals gitlink | 81eb5ccc | #12 (git-workflow mechanics) |
| layer-charly-distros gitlink | 0fca78fd | #6, #7 |
| dotgithub (.github) gitlink | fdced146 | #84, #85 (validator dedupe + caller-compat) |
| pi-omarchy-eval pin | v2026.251.0738 | #2–#5 |

## How tested

```
$ git diff --stat origin/main...HEAD
 eval-omarchy  | 2 +-
 layer-charly-distros | 2 +-
 layer-charly-internals | 2 +-
 marketplace | 2 +-
 dotgithub | 2 +-
 .pi/settings.json | 2 +-
 6 files changed, 6 insertions(+), 6 deletions(-)
$ for s in eval-omarchy marketplace layer-charly-internals layer-charly-distros dotgithub; do git ls-tree HEAD $s ... ; done
(each: recorded == checked-out HEAD, clean — PIN+CLEAN)
$ python3 -c "import json; json.load(open('.pi/settings.json'))"   # pin json ok
$ grep -c 'actions: write' .github/workflows/pr-validator.yml
0
$ git diff origin/main...HEAD -- .github/workflows/   # (no output — no workflow change)
```

`task verify` shows ONE failure: `charly: checked-out HEAD bcfadc6674 != gitlink 81f3cf23`. Evidence + routing: `git ls-files --stage charly` → 81f3cf23 (the recorded gitlink, unchanged by this PR); `git -C charly rev-parse HEAD` → bcfadc667 (a sibling session's build checkout, with its own dirty box/cachyos); this PR does not touch the charly gitlink. NAMED BATCH: "charly-pin sync — owned by the umbrella sync workflow; the sibling charly/cachyos wave advances the pin and the sync records it" (the sibling's own sync PR was dispatched-dead pre-#85 and resumes now that the reusable is caller-compatible).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | The verify failure is RCA'd with pasted evidence and routed to the named charly-pin-sync batch; no in-tree fix possible (the pin is sibling-owned) and it is not advanced by this PR's entries. |
| R2 | N/A — the external charly state is routed to a named batch, not a silent park. |
| R3 | One pin per submodule; all five verified PIN+CLEAN. |
| R4 | N/A — nothing worked around; the external state is documented, not force-aligned (no-interference). |
| R4a | N/A. |
| R5 | N/A — gitlink advances + pin bump. |
| R6 | feat/ branch from current main; no force-push to main. |
| R7 | Pins verified from the final committed tree; full verify run pasted (single external failure, RCA'd). |
| R8 | N/A. |
| R9 | N/A. |
| R10 | The changed pins verified PIN+CLEAN from the final tree. |
| T4 | N/A — no workflow change in this PR (the permission line was reverted out; grep 0 pasted). |
| B14 | Every pasted output matches the current head; the failure is shown in full, not trimmed. |
| B15 | N/A. |
| B17 | N/A — no code. |
| B18 | N/A. |

## Change classification

- Change class: submodule gitlink sync + package pin (close-out).
- Verification gate: targeted pin checks + task verify (external charly-pin failure RCA'd + named-batch routed).
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*