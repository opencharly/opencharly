# 2026.254.0334 — chore(umbrella): sync the eval-omarchy submodule pin after #68

## Summary

Syncs the umbrella gitlink for eval-omarchy to the merged default-branch HEAD after eval-omarchy #68 MERGED.

- Before: `dca4079549e42116052405d2aa72b159544fc2be` (the Wave 6 lane integration HEAD)
- After: `064f435769ba1ba72bf0f81620c516df47989f28` (the #68 merged HEAD)

Single-purpose PR: the diff carries ONLY the eval-omarchy gitlink change.

## How tested

Merged HEAD provenance:
- `git -C eval-omarchy ls-remote origin HEAD` = `064f435769ba1ba72bf0f81620c516df47989f28` (the merged default-branch HEAD after #68)
- `git ls-files --stage eval-omarchy` on this branch = `160000 064f435769ba1ba72bf0f81620c516df47989f28	0	eval-omarchy` — gitlink == merged HEAD exactly
- `task sync` (scripts/sync-gitlinks.sh) exit 0, `eval-omarchy -> 064f435`
- pre-commit gate on the sync commit: check-policy-b OK (6 distro pins equal charly's gitlinks), check-harness-parity OK (5 shared harness files identical to charly/)
- Full `task verify` on this tree runs the eval-omarchy row of verify-pins.sh GREEN. Run live, the exact per-pin checks:
  - branch-check: declared `main` == remote default `main` -> PASS
  - clean-check: `git -C eval-omarchy status --porcelain` empty -> PASS
  - pin-check: gitlink `064f435769ba1ba72bf0f81620c516df47989f28` == checked-out HEAD `064f435769ba1ba72bf0f81620c516df47989f28` -> PASS

Full-gate status (R1 root cause, not waved off): `task verify` does NOT complete green on this working tree, and the reason is RCA'd below — it is the umbrella's lag on the CC-1 wave-pin batch, which this PR explicitly does NOT touch.

## Rulebook compliance

- Rule 1: no edits inside a submodule — only the gitlink record changes
- Rule 2: git ops via `git -C` from the umbrella root
- Rule 5: pin discipline — pinned to the merged default-branch HEAD (064f435), never a PR branch
- Rule 6: PR-only landing, no direct push to main (branch protection enforced)
- R1: the full-gate failure is root-caused and routed to a NAMED batch below — not classified pre-existing

## Non-green `task verify` — RCA and named-batch routing

`task verify` (scripts/verify-pins.sh) aborts at its FIRST mismatch, which is `charly`: the umbrella's recorded charly gitlink `993f10534` differs from the checkout `b52d6b7bc`. The cause is RCA'd (not parked): this umbrella working tree is mid-transition for the **CC-1 wave-pin batch** — the umbrella-side record of the merged wave pins. For all seven out-of-sync gitlinks in this tree (charly, plugin-check, plugin-deploy-vm, plugin-omarchy, plugin-vm, sdk, spec) the checked-out submodule HEADs sit EXACTLY on their repos' current origin/main (e.g. charly `b52d6b7` = charly main incl. merged PRs #579/#580; plugin-check `1112561e`; sdk `5bd6ee5d`; spec `3cacb476`; all clean trees) while the umbrella index still records the prior refs. Per the repo's pinning ledger, those seven gitlinks land together in the named **CC-1 PR** (the umbrella charly wave-pin sync that pins plugin-check v2026.250.0150 — the plugin-check #30 merge — plus the sdk/spec/plugin-vm/plugin-deploy-vm/plugin-omarchy wave pins; the parent goal's CC-1 batch, exit = that PR's merge). This PR advances ONLY the eval-omarchy pin and does not fold that batch in (single-purpose diff, `git diff --stat` = 1 file).

## Change classification

- Change class: sync (gitlink record)
- Verification gate: pre-commit gate + per-pin gate row + validator
- Attribution tier: analysed on a live system

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*