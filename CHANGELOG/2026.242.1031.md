# 2026.242.1031 — docs(pi): correct root cause to provider-verified 65536 output cap

## Summary

PR #53 merged with the pre-spike root-cause framing (which suggested lowering thinking and raising maxTokens to 131072). A live `pi -p` spike then proved both wrong: the provider rejects `max_tokens` above **65536** for `deepseek-v4-flash:0731` via ollama-cloud (`max_tokens (131072) exceeds model's maximum output tokens (65536)`). This correction replaces that section in `.pi/APPEND_SYSTEM.md` with the provider-verified truth:

- Real maximum output = **65536** (provider-verified; catalog's 32768 is understated).
- `maxTokens` must be 65536 — never higher (rejected), never lower (wastes capability).
- Truncation = a single response (high thinking + long tool call) exceeding 65536 → `finish_reason="length"` → harness fails the tool calls → loop.
- Downgrading thinking is NOT a root-cause fix; the model is used at FULL capability (maxTokens 65536, thinking high). The only truncation lever that preserves capability: short tool calls + subagent delegation.

## How tested

- **Spike (pi -p, live):** `maxTokens: 131072` → provider rejection `max_tokens (131072) exceeds model's maximum output tokens (65536)`. This empirically established the real cap.
- **Config corrected** to `maxTokens: 65536` in `~/.pi/agent/models.json` (verified); rejection gone.
- `git commit` pre-commit gate: `pre-commit: OK`.

## Rulebook compliance

- **R1:** root cause verified empirically against the live provider (spike), not guessed.
- **R4:** config aligned with the provider-verified real maximum; no workaround.
- **R5:** the incorrect pre-spike guidance is replaced in this commit; no stale claims remain.

## Change classification

- **Change class:** documentation (harness guidance doc).
- **Verification gate:** pre-commit gate passed; root cause proven by live provider rejection.
- **Attribution tier:** `analysed on a live system`.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*