# 2026.261.0657 — feat: add the charly-jetkvm submodule

## Summary

Adds the newly created `opencharly/charly-jetkvm` repo to the umbrella as a submodule.

- `.gitmodules`: new `charly-jetkvm` entry (`branch = main`, HTTPS url), placed in **alphabetical position among the other `charly-*` entries** (between `charly-fedora` and `charly-openwrt`), matching that contiguous sorted run.
- `charly-jetkvm` gitlink pinned at its **merged default-branch HEAD** `e80319e` (the repo's genesis commit — it has no tag yet; the every-other-repo rule pins default-branch HEAD, not a tag).
- `README.md`: a "Product & tooling" org-map row for the repo, that section's count `5 → 6`, and the header count `402 → 403`.

`charly-jetkvm` is the host-side tool that provisions the appliance-resident `charly` on a JetKVM (armv7) — it installs the published release binary + welded plugin set over ssh and verifies it runs project-less. It is a plain non-charly-pinned repo (like every repo not in policy B's contract), so it follows the every-other-repo rule.

## How tested

The umbrella's gate is `task verify` → `scripts/verify-pins.sh`, which asserts: every `.gitmodules` branch equals the remote default, every submodule is clean and at its recorded gitlink, policy B (distro pins == charly's gitlinks), and charly's nested submodules are initialized. Run directly on the committed tree (the worktree has every submodule initialized):

```console
$ bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 403 submodules, policy B holds
```

The per-commit gate (the umbrella hook) also ran on the commit:

```console
$ git config core.hooksPath hooks   # task hooks
$ git commit …                      # hooks/pre-commit fired
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
sync-dispatcher: self-test OK (splice writes, steady-state passes, stale fails)
pre-commit: OK
```

The README's own reproducible sweep (`scripts/check-org-map.sh`, the check the previous umbrella PR shipped) reconciles every section against `.gitmodules`:

```console
$ bash scripts/check-org-map.sh
### Product & tooling            6     6     yes
...
submodules in .gitmodules : 403
submodules with no row    : none
header '(N today)'        : 403  matches=True
OK: all sections reconcile and are sorted; every submodule has a row
```

The pinned gitlink is the repo's merged HEAD:

```console
$ git ls-files -s charly-jetkvm
160000 e80319ed9e706680d54eee41aff3178bc5009d0d 0	charly-jetkvm
$ gh api repos/opencharly/charly-jetkvm/commits/main --jq '.sha'
e80319ed9e706680d54eee41aff3178bc5009d0d
```

## Rulebook compliance

- **R0 skills:** `/charly-internals:git-workflow` (pinning/gitlink policy, `task sync`/`verify`).
- **R1 RCA + zero warnings:** two fixes, each root-caused: (a) the gitlink needed `branch = main` (the repo's default branch) — a `git submodule add` against a fresh repo omits it; (b) that same `submodule add` appends the block at the end of `.gitmodules`, but the file keeps a contiguous **sorted `charly-*` run**, so the entry was moved into its alphabetical position (between `charly-fedora` and `charly-openwrt`). Both were caught by the review/gate and fixed at the source. No warnings.
- **R2 no out-of-scope:** the submodule, its gitlink, and its README row/counts are the whole change.
- **R3 no duplication:** the README row is the single org-map entry; the pin is the gitlink itself, no second list.
- **R4 no workaround:** the submodule carries `branch = main` explicitly; no hand-edited gitlink, no stale pin.
- **R5 hard cutover + grep:** the README header and the section count both move 402→403 / 5→6 in the same commit; `check-org-map.sh` reconciles.
- **R6 git safety:** a dedicated linked worktree off fresh `origin/main`; `git status` inspected; no force push, no hooks bypass, no direct push to `main`; the pre-commit hook ran and passed.
- **R7 prove the gate:** `verify-pins.sh` (the `task verify` body) and the commit hook both run green on the committed tree, pasted above.
- **R8 artifacts:** N/A — no generated artifact.
- **R9 binary == source:** N/A — a gitlink, not a binary.
- **R10 fresh disposable proof:** the gate was run on the committed worktree with **all** submodules initialized (`git submodule update --init`), so the tree is the real fresh checkout, not a half-initialized one. No check bed applies (`N/A — pinning change`).
- **Policy B:** the new submodule is deliberately **not** charly-pinned (`verify-pins` reports `6 distro pins equal charly's gitlinks`, unchanged) — it follows the every-other-repo default-branch rule.
- **Harness config parity:** `check-harness-parity.sh` is green; no harness config touched.
- **CHANGELOG:** written by `tag-on-merge` from this body.

---
*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*