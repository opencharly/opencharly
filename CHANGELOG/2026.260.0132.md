# 2026.260.0132 — chore: sync gitlinks — the Phase-2 vision chain (eval-omarchy, marketplace, spec, sdk)

## Summary

Advances the umbrella pins to the merged default-branch HEADs of the Phase-2
`vision:` chain:

| submodule | pin | carries |
|---|---|---|
| `spec` | `6eb5d41` | PR #136 — the shared `#LLMSpec`/`#LLMParams` CUE vocabulary (tag `v0.2026259.2033`) |
| `sdk` | `eb9f055` | PR #261 — `llmkit` + the deploykit repair (tag `v0.2026259.2136`; this HEAD is the merge's CHANGELOG commit, one above the tagged squash) |
| `eval-omarchy` | `3bc8887` | PR #80 (the `vision:` verb wired into the PR-eval bed) + PR #81 (pin plugin-pipeline v2026.259.2340 — the consumer of the shared llmkit), tag `v2026.260.0048` |
| `marketplace` | `7fb85a8` | PR #304 — the eval-omarchy ref + the regenerated corpus (tag `v2026.260.0021`) |

Every staged pin equals its repo's `origin/main` (rule 5). Raw per-line output:

```
$ for p in eval-omarchy marketplace sdk spec; do t=$(git ls-files -s --stage "$p"|awk '{print $2}'); o=$(git -C "$p" rev-parse origin/main); [ "$t" = "$o" ] && echo "OK $p" || echo "DIFF $p"; done
OK eval-omarchy
OK marketplace
OK sdk
OK spec
```

## How tested

### The pinning gate (R7), pasted — on the SHIPPED tree

```
$ bash scripts/check-policy-b.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks

$ bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 399 submodules, policy B holds
```

### The umbrella composes the synced tree

```
$ charly box validate
charly box validate: OK — checked 7 distros, 7 builders; 0 warnings, 0 errors
```

### The lane RUNS on the shipped eval-omarchy pin, with the LLM path EXECUTED

Run from the `eval-omarchy` checkout at the staged `3bc8887`. `12138` is the
omacom/omarchy PR under evaluation (the lane's SUBJECT); its own plugin ref —
now `plugin-pipeline v2026.259.2340`, the shared-llmkit consumer — is what the
lane resolves from this tree. Full stage stream, unedited:

```
$ EVAL_UMBRELLA=<umbrella> charly pipeline run eval-pr-plan --pr 12138 --lanes 1 --workdir <eval-omarchy-checkout@3bc8887>
plugin github.com/opencharly/plugin-pipeline/candy/plugin-pipeline: served from /home/atrawog/.cache/charly/plugins/github.com_opencharly_plugin-pipeline_candy_plugin-pipeline-1c9cf9c742bafddb (build stamp b2602e55e306f2cf); source /home/atrawog/.cache/charly/repos/github.com/opencharly/plugin-pipeline@v2026.259.2340/candy/plugin-pipeline
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=2 content= tool_calls=5 reasoning=130
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=8 content={"class":"system","golden":"check-omarchy-eval-base-inst:golden","sha":"be0917f988fb6f9a7f2bb6590f35be888b91aef2","files tool_calls=0 reasoning=27088
[stage oracle] ok
plugin github.com/opencharly/plugin-pipeline/candy/plugin-pipeline: served from /home/atrawog/.cache/charly/plugins/github.com_opencharly_plugin-pipeline_candy_plugin-pipeline-1c9cf9c742bafddb (build stamp b2602e55e306f2cf); source /home/atrawog/.cache/charly/repos/github.com/opencharly/plugin-pipeline@v2026.259.2340/candy/plugin-pipeline
[stage render] ok
[stage control] ok — check-run exit 0: … check-omarchy-pr-12138-control: [check-live] PASS after 14.056s [allowlisted=1] (log: .check/check-omarchy-pr-12138-control/2026.260.0107/check-live.log)
[stage eval] ok — check-run exit 2: …y: charly vm destroy check-omarchy-eval-base-inst --domain check-omarchy-pr-12138-vm
[stage media] ok
[stage gate] ok
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=2 content=I'll gather the PR facts and the ledger evidence before grading. tool_calls=8 reasoning=143
[tool] stage_output({"stage":"eval"}) -> {"summary":"check-run exit 2: …y: charly vm destroy check-omarchy-eval-base-inst --domain check-omarchy-pr-12138-vm\ncharly check run check-omarchy-pr-12138-vm: FAIL (steps=5) — repo override active (
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=11 content=Let me pull the remaining stage outputs for the full ledger detail. tool_calls=5 reasoning=4433
[tool] stage_output({"stage":"control"}) -> {"summary":"check-run exit 0: … check-omarchy-pr-12138-control: [check-live] PASS after 14.056s [allowlisted=1] (log: .check/check-omarchy-pr-12138-control/2026.260.0107/check-live.log)\ncharly check 
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=17 content={
[stage validate] ok
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=2 content= tool_calls=4 reasoning=182
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=7 content= tool_calls=2 reasoning=97
[tool] stage_output({"stage":"eval"}) -> {"summary":"check-run exit 2: …y: charly vm destroy check-omarchy-eval-base-inst --domain check-omarchy-pr-12138-vm\ncharly check run check-omarchy-pr-12138-vm: FAIL (steps=5) — repo override active (
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=10 content={"tests":"I pulled PR 12138 (sha be0917f9) onto a disposable golden clone of omarchy (check-omarchy-eval-base-inst:golde tool_calls=0 reasoning=2170
[stage report] ok
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=2 content= tool_calls=6 reasoning=1463
[chat] model=deepseek-v4.1-flash:cloud base_url=http://localhost:11434/v1 turns-so-far=9 content={"verdict":"PASS","suggestions":["Record the root cause of the failing 'pr-tests' step (the PR's own /usr/share/omarchy/ tool_calls=0 reasoning=8810
[stage cold-read] ok
[stage record] ok
[stage not-testable] skipped — skipped: @oracle.class != skip
[stage publish] skipped — skipped: $env.EVAL_PUBLISH != approve || @cold-read.verdict == NO_VALIDATION
lane 12138: done
pipeline eval-pr-plan: OK (lanes=1, pool=1)
```

The `source ...@v2026.259.2340/...` line in that output is the resolution proof —
not a cached predecessor. So: the staged `eval-omarchy` pin RUNS, and through it the shipped
`plugin-pipeline v2026.259.2340` (which consumes the synced `sdk`/`spec` via
`llmkit`) resolves and executes — the LLM-bearing stages (`validate`, `report`,
`cold-read`) all produced real `[chat]` turns and completed. The
`spec`/`sdk` gitlinks themselves are proven by the `verify-pins` gate above
(every pointer checked against its repo's default branch); a wrong umbrella
gitlink would red that gate, not this lane.

### The working tree, resolved (no parking)

The umbrella tree had one untracked entry, `plugin-group/` — an orphaned local
checkout whose gitlink was REMOVED by #203 (not in `.gitmodules`; no commit's tree
references it). Its content was verified reproducible from its remote — its
detached HEAD IS the remote's HEAD — before removal:

```
$ git ls-remote https://github.com/opencharly/plugin-group.git
fb749a6111aa9e1bcade3e421f5eff79c66d58da	HEAD
```

so no local-only commit existed. It was removed in this tree, and the resulting
status is empty:

```
$ git status --short
(no output)
```

## Rulebook compliance

- **R0 (skills first)** — `/charly-internals:git-workflow` loaded for the sync.
- **R1 (RCA every anomaly)** — the `plugin-group/` orphan was root-caused and
  resolved; the fold-commit's pin was re-verified.
- **R2 (no deferral)** — the orphan is resolved in-tree; the second pin advance is
  folded in THIS PR (an add-only commit, since the branch is pushed).
- **R3 (no duplication)** — pins come from `task sync`.
- **R4 / R4a (no workarounds)** — no hand-pin; the script is the one path.
- **R5 (delete legacy completely)** — superseded pins replaced; the orphan removed.
- **R6 (git safety)** — `git status` before each step; no force-push (the fold is an
  add-only commit); no direct push to `main`.
- **R7 (prove the gate)** — `verify-pins.sh` + `check-policy-b.sh` pasted above from
  the shipped tree.
- **R8 (artifact invariants)** — a gitlink sync writes no artifacts.
- **R9 (deployed binary matches source)** — N/A — no image/binary is built or
  deployed by this change; the composition is proven by `box validate`.
- **R10 (fresh disposable proof)** — the lane ran on the disposable golden clone.

## Change classification

- **Change class:** pinning/gitlinks (policy B), 4 submodule pointers.
- **Verification gate:** `task verify` (`verify-pins.sh` + `check-policy-b.sh`) +
  `charly box validate` + a full `eval-pr-plan` lane on a disposable target — all on
  the shipped pins, all pasted above.
- **Attribution tier:** `fully tested and validated` — the full pinning gate passed
  on the shipped tree, and the staged eval-omarchy pin ran end to end with its
  LLM-bearing stages executing.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*