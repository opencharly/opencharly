# 2026.265.0539 — docs(agents): add the live-or-skip mandate to the engineering rules

## Summary

Add the **live-or-skip mandate** to the umbrella rulebook's Engineering rules (`AGENTS.md`), beside R7 (the proof gate):

> **Live or skip — never fake a live service.** Any test, harness, or gate that
> crosses a live-service boundary (a `gh` / GitHub API call, an LLM or provider
> endpoint, a network or `charly` call) MUST run against the **REAL** service, or
> **SKIP cleanly** when its credential/endpoint is absent — never a mock, stub,
> or fake of that boundary. A fake asserts the behaviour the author IMAGINED, not
> what the service actually does, so it certifies a contract that may not exist
> and hides a real integration break behind green. Gate the skip on the real
> credential (`LIVE_*` unset → skip, visibly reported — never a silent pass), and
> keep pure/deterministic in-repo logic unit-testable normally. Mandate here; the
> per-surface how-to lives in the owning skill.

**Why.** A faked live boundary certifies the author's mental model, not the service's real behaviour: it goes green on a contract that may not exist and hides a genuine integration break. The mandate is the *what*; the owning skill carries the per-surface *how* (the skip gate, the credential name, the retained live output).

## How tested

**Change-class gate** — the umbrella pre-commit gate on the final tree:

```console
$ bash hooks/pre-commit
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
sync-dispatcher: self-test OK (splice writes, steady-state passes, stale fails)
sync-pin-evidence: self-test OK (per-pin classification both ways; coverage both directions; staged-gitlink reads the index + falls back to HEAD)
pre-commit: OK
exit=0
```

**The change is documentation-only** — a rulebook prose addition; no code, script, pin, or generated artifact changes. The diff is one hunk in `AGENTS.md`.

## Rulebook compliance

- **R0 skills:** `/charly-internals:skills` + `/charly-internals:git-workflow` loaded.
- **R1 RCA:** the mandate operationalizes the failure class observed this session — a workflow harness that faked `gh`/`charly` certified a decision chain the real services need not honour.
- **R2 complete:** the mandate lands here; the per-surface procedure belongs to the owning skill (stated in the text).
- **R3 no duplication:** the rulebook states the mandate and points at the owning skill; it does not restate the how-to.
- **R4 no workaround:** this *forbids* fakes; it adds no shim.
- **R5:** additive; no deleted identifier.
- **R6 git safety:** new `docs/` branch off `origin/main`; no force-push; no direct push to `main`.
- **R7 gate:** `hooks/pre-commit` run and pasted above.
- **R8/R9/R10:** `N/A — documentation-only change class` (no artifact, no binary, no runtime bed).

## Change classification

- **Change class:** documentation-only (umbrella rulebook).
- **Verification gate:** the umbrella pre-commit gate.
- **Attribution tier:** `documentation reviewed` — the rulebook edit is prose; no runtime path exists.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*