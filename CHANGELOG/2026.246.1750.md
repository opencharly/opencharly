# 2026.246.1750 — chore(pi): move theme/compaction/pi-web-ui into umbrella settings

## Summary

Move the pi agent configuration that is NOT model/provider selection from the local `~/.pi/agent/settings.json` into the umbrella's `.pi/settings.json` (the single source of truth for project config):

- `npm:pi-web-ui@0.54.0` package (was in local config)
- `theme: "dark"` (was in local config)
- `compaction` block (was in local config)

The local `~/.pi/agent/settings.json` now holds ONLY model/provider selection (`defaultProvider`, `defaultModel`, `defaultThinkingLevel`) per the operator's instruction: everything except model and provider selection lives in the umbrella repo.

## How tested

- `pi -p` (no `--model`) in the umbrella cwd resolves the default to `ollama-cloud/deepseek-v4-flash:0731` and replies (session `01a0684f-70ae` model_change: `provider: ollama-cloud, modelId: deepseek-v4-flash:0731`).
- `pi -p` subagent dispatch (worker) runs on `ollama-cloud/deepseek-v4-flash:0731` with `error: null` and produces the expected output (run `eaa01237` meta: `model: ollama-cloud/deepseek-v4-flash:0731`, `attemptedModels: [ollama-cloud/deepseek-v4-flash:0731]`).
- `pi list` shows the project packages load from the umbrella `.pi/settings.json` (incl. the new `npm:pi-web-ui@0.54.0`).
- Pre-commit gate passed: `check-policy-b: OK`, `check-harness-parity: OK`.

## Rulebook compliance

| Rule | Status | How / N/A |
|---|---|---|
| 1. Never edit inside a submodule | HOW | One umbrella-root file changed (`.pi/settings.json`) |
| 2. Git op rule | N/A | No submodule git ops |
| 3-4. Go/worktrees | N/A | No Go, no worktrees |
| 5. Pin discipline | HOW | Package pins unchanged; `npm:pi-web-ui@0.54.0` moved from local to umbrella (same version) |
| 6. Git safety | HOW | No force-push, no hook bypass; pre-commit gate passed |
| 7. Prove the gate | HOW | Pre-commit gate passed; `pi -p` behavioral tests run live |
| 8. Harness parity | N/A | `.pi/settings.json` is an umbrella-specific fork (not in the parity SHARED list) |
| R10. Fresh proof | HOW | `pi -p` runs executed live from the committed tree |

## Change classification

- **Change class:** configuration (pi settings relocation)
- **Verification gate:** pr-validator
- **Attribution tier:** analysed on a live system — `pi -p` default + subagent dispatch ran live with retained output

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*