# 2026.247.1349 — sync: crabbox follow-up gitlinks (pod-crabbox, pod-postgresql, eval-omarchy)

## Summary

Follow-up gitlink sync for the Crabbox wave (umbrella #137 advanced the
initial set; these mains moved since):

- `pod-crabbox` -> 4191ee6 (postgres root-capable pin bump #2)
- `pod-postgresql` -> 201cab4 (root-capable entrypoint #2, v2026.247.1304)
- `eval-omarchy` -> b56f1c5 (sibling-session advances #21-#23 - the sanctioned `task sync` includes them)

All via `scripts/sync-gitlinks.sh` (no hand-pins).

## How tested

```
$ bash scripts/sync-gitlinks.sh
sync-gitlinks: done - review with git status and git diff --submodule (exit 0)
$ git diff --submodule=log  # each gitlink advances exactly to its repo main
  pod-crabbox 8c45267..4191ee6 (CHANGELOG 2026.247.1311 + #2)
  pod-postgresql ab3899d..201cab4 (CHANGELOG 2026.247.1304 + #2)
  eval-omarchy 369c5f0..b56f1c5 (#21-#23)
$ task verify
verify-pins: OK - 398 submodules, policy B holds
```

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 - skills first | git-workflow + marketplace skills loaded before the sync. |
| R1 - RCA every anomaly | The earlier sync child timed out (no changes made) - re-run via the sanctioned script, verified equal to main. |
| R2 - finish the whole cutover | All moved gitlinks land in ONE sync PR. |
| R3 - no duplication | `scripts/sync-gitlinks.sh` is the single sanctioned mechanism. |
| R4 - no workarounds | No hand-pins; the script owns the advance. |
| R5 - delete legacy | N/A. |
| R6 - git safety | No force-push; dedicated worktree from origin/main; branch-per-change PR. |
| R7 - prove behaviour | verify-pins OK (398 submodules, policy B) + per-gitlink main equality shown above. |
| R8 - preserve emitted artifacts | The gitlink set validated by verify-pins. |
| R9 - binary equals source | Fresh worktree-built bin/charly consistent with origin/main. |
| R10 - fresh disposable proof | N/A - gitlink metadata change (verified by task verify). |
| RDD/ADE/SDD | N/A. |
| Concurrency / Disposable / Hooks | N/A (sibling worktree discipline observed). |

## Change classification

- **Class:** gitlink sync (metadata).
- **Verification gate:** `task verify` (verify-pins OK, policy B).
- **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*