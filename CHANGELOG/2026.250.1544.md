# 2026.250.1544 — chore: advance the marketplace gitlink to the #289 corpus merge

## Summary

Advances the umbrella's marketplace gitlink from 22b83161 (pre-#289) to **38bc022c** — the #289 merge (the MCP/systemd corpus wave's regeneration: the mcp: VM venues + addr VM vars, the serve loopback-default/opt-in bind + packaged units/config, the service overrides -> drop-in docs). The prior sync (PR #154) staged the marketplace before #289 merged; this one-pin follow-up records the corpus.

## How tested

```
$ git -C marketplace rev-parse HEAD        # the checkout == the pin
38bc022c
$ git ls-tree HEAD: marketplace            # the recorded gitlink
38bc022c...
$ git ls-remote origin HEAD                # the marketplace's merged main
38bc022c
```

The pin equals the marketplace's merged default-branch HEAD (the #289 squash merge). `bash scripts/verify-pins.sh` stays green (the pin matches the checkout; no tree changes).

## Rulebook compliance

- R0: git-workflow + the sync pattern.
- R1: the #154 sync's marketplace pin was one behind the just-merged #289 — recorded here rather than deferred.
- R3: sync-gitlinks.sh is the single pinning implementation.
- R6: worktree, PR-only.

## Change classification

- **Change class:** config (single gitlink pin).
- **Verification gate:** the pin == the repo's merged origin/HEAD (pasted above).
- **Attribution tier:** analysed on a live system — the pin was verified against the live upstream (pasted).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*