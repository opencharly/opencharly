# 2026.246.2005 — chore(gitlink): advance charly to main (1ed8552b — gh_pr_status extension #533)

## Summary

Advance the umbrella's `charly` submodule gitlink to `1ed8552b` (charly main, post-#533) — the follow-up to #125: charly #533 (the gh_pr_status mergeable-state extension) merged after #125's sync, so the umbrella's charly pointer lagged one commit.

## How tested

```
$ git ls-files -s charly
160000 1ed8552b478a67e7cad9b85c904ee7a7fd73a88d 0  charly
$ git -C charly rev-parse origin/main
1ed8552b478a67e7cad9b85c904ee7a7fd73a88d
$ git -C charly show origin/main:.pi/extensions/github-pr-status.ts | grep -c mergeableInterpretation
2
```

The gitlink names charly's default-branch HEAD, which contains the extension change (verified: the file in main has the new `mergeableInterpretation` helper).

## Rulebook compliance

- **R1** — RCA: #533 merged after #125's sync, leaving the umbrella's charly pointer one commit behind. Fixed by this follow-up sync.
- **R2** — N/A: no cutover; a gitlink advance.
- **R3** — No duplication: the gitlink is the single pointer.
- **R4** — No workarounds: sanctioned gitlink sync.
- **R5** — N/A: no legacy path removed.
- **R6** — Git safety: feat/ branch, no force-push, no direct push to main.
- **R7** — Gate: the gitlink resolves to charly's default-branch HEAD; the pre-commit gate passed.
- **R8** — The gitlink is the submodule's default-branch HEAD (sync, not pin).
- **R9** — N/A: no R10 bed change.
- **R10** — The validated tree is the committed branch state.

## Change classification

- **Change class:** gitlink sync (chore).
- **Verification gate:** the gitlink resolution to the default-branch HEAD.
- **Attribution tier:** analysed on a live system — the gitlink resolution ran live with retained output.

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*