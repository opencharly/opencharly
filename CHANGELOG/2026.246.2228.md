# 2026.246.2228 — chore(gitlink): sync openclaw-2.0 batch to main

## Summary

Gitlink sync for the openclaw 2.0 update batch (all merged upstream):
- **pod-openclaw** → openclaw 2026.9.1 (v2026.246.2119)
- **plugin-openclaw** → new repo added as submodule (openclaw: check verb, v2026.246.1626), with `branch = main` in .gitmodules per the established pattern
- **layer-openclaw-full** → metalayer pins (v2026.246.1954)
- **layer-himalaya** → chacha20 fix (v2026.246.1954)
- **pod-selkies** → GPU-check fixes (v2026.246.2210)

distro-cachyos is NOT advanced (policy B: umbrella must record charly's box/cachyos gitlink, which is 677dbbe on the sibling's branch).

## How tested

```
$ git diff origin/main..HEAD --stat
 .gitmodules         | 4 ++++
 layer-himalaya      | 2 +-
 layer-openclaw-full | 2 +-
 plugin-openclaw     | 1 +
 pod-openclaw        | 2 +-
 pod-selkies         | 2 +-
 6 files changed, 9 insertions(+), 4 deletions(-)
```

```
$ git status --short | grep -E "^[MA]"
M  .gitmodules
M  layer-himalaya
M  layer-openclaw-full
A  plugin-openclaw
M  pod-openclaw
M  pod-selkies
```

```
$ task verify
(389 submodules, policy B holds — distro-cachyos matches charly's box/cachyos)
```

## Rulebook compliance

- R1: RCA — the pre-commit gate refused the distro-cachyos advance (policy B: umbrella distro-* must equal charly's box gitlinks); reverted to 677dbbe. The validator flagged the missing `branch = main`; added it.
- R3: no duplication — sync script semantics, one commit.
- R4: no workaround — no hand-pins; only merged refs.
- R6: git safety — feat/ branch, no force-push.
- R7: gate — pre-commit gate passed; task verify GREEN.

## Change classification

- Change class: config (gitlink sync)
- Verification gate: task verify (local)
- Attribution tier: fully tested and validated

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*