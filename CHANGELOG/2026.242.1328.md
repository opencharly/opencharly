# 2026.242.1328 — sync: record charly#487 + spec#81 merged gitlinks; add PLAN-mise.md

## Summary

Two commits on this branch (squash collapses them):

1. **feat: add the plugin-mise submodule** (4c4a5477) — the new `plugin-mise` gitlink (opencharly/plugin-mise, merged + tagged v2026.242.1251) + its `.gitmodules` entry. The charly plugin serving `builder:mise` + `verb:mise` (full mise support per PLAN-mise.md).
2. **sync: record charly#487 + spec#81 merged gitlinks; add PLAN-mise.md** (865c87e) — `charly` → 65e05362 (embedded `builder: mise:` vocabulary + plugin-mise pin + TestEmbeddedMiseBuilderVocabulary), `spec` → 185e86f (ExternalizedBuilders + externalBuilderPlugins maps), and the validated PLAN-mise.md.

## How tested

```
$ git -C charly log --oneline -1
65e05362 docs: add CHANGELOG 2026.242.1313 for PR #487
$ git -C spec log --oneline -1
185e86f docs: add CHANGELOG 0.2026242.1251 for PR #81
$ git -C plugin-mise log --oneline -1
17943e9 docs: document the check-mise-pod R10 bed
$ git show --stat HEAD
 PLAN-mise.md | 286 +++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
 charly       |   2 +-
 spec         |   2 +-
 3 files changed, 288 insertions(+), 2 deletions(-)
$ git show --stat HEAD~1
 .gitmodules | 3 +++
 plugin-mise | 1 +
 2 files changed, 4 insertions(+)
```

All three gitlinks point at merged main heads (validator PASS on all PRs).

## Rulebook compliance

- R6: git safety — feat/ branch, no force-push, PR-only landing.
- R7: gate proven — the gitlinks point at merged, validator-PASSed heads; `task verify` runs on the final tree.

## Change classification

- Change class: gitlink sync + new submodule + plan doc
- Verification gate: gitlinks at merged heads
- Attribution tier: analysed on a live system (all referenced PRs merged with validator PASS)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*