# 2026.243.0524 — feat(config): gitignore plan/ contents, drop pi-plan-mode, append plan-save system prompt

## Summary

Config-only change to the umbrella harness (no submodule edits, no pins touched):

1. **`plan/` contents gitignored, directory kept in git** — `.gitignore` gains `plan/*` + `!plan/.gitkeep`; the two formerly-tracked plan docs (`plan/candy_to_repo.md`, `plan/mise-followup-todo.md`) are untracked (files stay on disk as local working docs) and `plan/.gitkeep` keeps the directory in the repo.
2. **Plan plugin removed from the project pi agent** — `npm:@narumitw/pi-plan-mode@0.56.0` (the Codex-like `/plan` collaboration extension) dropped from `.pi/settings.json` packages. The only config reference was this entry — the sweep evidence is pasted under ## How tested (item 6, B14(d)).
3. **System-prompt replacement adopted** — new `.pi/APPEND_SYSTEM.md` (pi's project-level append-without-replace mechanism): whenever a user asks for a plan, the plan must be saved to a file under `plan/` before any execution starts and kept updated as the plan is executed.

Machine-local follow-up (not part of this diff): the same package was also registered in the global pi agent config (`~/.pi/agent/settings.json`). It was removed there and its generated npm-store dirs pruned so the extension no longer loads; a fresh `pi -p` session confirms plan-mode tools are gone.

## How tested

(1) `pi list` — project package set now 11 entries, plan-mode absent:

```
Project packages:
  npm:pi-mcp-adapter@2.31.0, npm:pi-subagents@0.60.0, npm:@juicesharp/rpiv-todo@2.8.0,
  npm:pi-memory@0.4.2, npm:pi-ollama-cloud@0.9.0, npm:pi-web-access@0.27.0,
  npm:pi-fabric@0.70.0, npm:pi-claude-marketplace@0.18.1, npm:pi-web-ui@0.54.0,
  git:github.com/Michaelliv/pi-goal@v0.1.7, git:github.com/giladbarnea/pi-simple-team@v1.0.9
  (npm:@narumitw/pi-plan-mode no longer listed)
```

(2) Fresh `pi -p` behavioral probe (boots clean with the new config; model answer):

```
1. The plan must be saved to a file under plan/ (e.g. plan/<topic>.md) before any execution starts,
   and kept updated as the plan is executed. plan/ is gitignored — plans are working documents, never committed.
2. No — no /plan collaboration-mode extension or its tools (plan_mode_complete/plan_mode_question)
   are exposed or active in this session.
```

(3) `git check-ignore -v plan/mise-followup-todo.md` → `.gitignore:27:plan/*	plan/mise-followup-todo.md`

(4) Live pre-commit gate on the commit:

```
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

(5) `task verify` on the final committed tree:

```
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 356 submodules, policy B holds
```

(6) B14(d) sweep evidence — no harness config references the removed package (commands + real output):

```
$ git grep -n -i "narumitw\|pi-plan-mode" -- ':!.pi/npm' ':!.pi/git' ':!.pi/fabric' ':!.pi/pi-claude-marketplace' ':!.pi/agents'
CHANGELOG/2026.242.0950.md:13:5. **Package updates to latest.** pi-subagents 0.51.0→0.60.0, @narumitw/pi-plan-mode 0.49.3→0.56.0, ... (historical changelog entry only)

$ grep -n -i "narumitw\|plan-mode" opencode.json reasonix.toml .claude/settings.json hooks/pre-commit 2>/dev/null
(no output; exit 1 — zero hits in live harness configs)
```

The only reference in the tracked tree is a historical CHANGELOG entry documenting the past package update itself; the gitignored `.pi/fabric/` runtime logs retain old mentions but are not part of the repository.

## Rulebook compliance

| Rule | Status |
|---|---|
| 1 Never edit inside a submodule | N/A — no submodule touched |
| 2 Git op rule | OK — all git driven from the umbrella root |
| 3 No nested go.work | N/A — Go untouched |
| 5 Pin discipline | N/A — no pins/gitlinks touched |
| 7 Subrepo rulebooks | N/A — no subrepo edits |
| 8 Harness parity | OK — check-harness-parity: 5 shared files identical to charly/; `.pi/settings.json` is an umbrella-specific fork by design (HARNESS-PARITY.md) and not in the shared diff set |
| R0 Skills first | OK — git-workflow skill loaded before any git/gh action |
| R1 RCA every anomaly | OK — stale `.git/index.lock` (0 bytes, no git processes) RCA'd as a crashed-op artifact and removed after verification; `pi -q` identified as an unknown option (no such flag) and the verification method switched to `pi -p` |
| R3 No duplication | OK — no behavior re-implemented; config removed/adjusted only |
| R4 No workarounds | OK — no sleeps/retries/hand-pins |
| R5 Delete legacy completely | OK — plan plugin removed from project config, and global registration + npm-store dirs removed on this machine so no stale path loads; sweep evidence in (6) proves no other harness config references it |
| R6 Git safety | OK — `git status` before destructive actions; no force-push |
| R7 Prove the gate | OK — `task verify` green on final tree (pasted above) |

## Change classification

- Change class: configuration change (harness config, gitignore, appended system prompt)
- Verification gate: R7 (`task verify`) + pre-commit gate (policy-b + parity) + live `pi` CLI boot
- Attribution tier: fully tested and validated

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*