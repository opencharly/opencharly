# 2026.256.1242 — docs: add the paragraph-level documentation verification audit

## Summary

Adds `DOC-VERIFICATION.md` to the umbrella root: the paragraph-level audit of charly's hand-authored documentation against the actual `charly` binary, the CUE schema, and the full opencharly corpus (188 `layer-*`, 74 `pod-*`, 108 `plugin-*`, 6 `distro-*` repos + `spec`/`sdk` + docs + marketplace).

Contents: scope + generation boundary (1384 generated vs 19 hand-authored of 1403 content files), the claim-class method + corpus-index recipe (901 entity rows), the README ledger, divergence codes D1–D36, the mirror/duplication register, the escalation register (E1 RCA'd), and the landing log of the nine merged PRs it produced. Root-level documentation only — no submodule content, no gitlinks, no pins.

## How tested (real output)

```
$ # generation boundary (full-file DO-NOT-EDIT scan)
$ total=$(find docs/src/content -type f \( -name '*.md' -o -name '*.mdx' \) | wc -l)
$ gen=0; hand=0; while IFS= read -r f; do if grep -qm1 'DO.NOT.EDIT' "$f"; then gen=$((gen+1)); else hand=$((hand+1)); fi; done < <(find docs/src/content -type f \( -name '*.md' -o -name '*.mdx' \))
$ echo "total=$total generated=$gen hand=$hand"
total=1403 generated=1384 hand=19
$ # corpus entity index
$ wc -l /tmp/corpus-index.tsv
901 /tmp/corpus-index.tsv
$ # provider census
$ head -1 <docs>/reference/providers.md
**166 words across 175 plugin candies, 121 compiled into the binary.**
$ # README:89-101 (previously timed out; now warm-cache verified)
$ charly --repo opencharly/charly box list boxes | head -5
agentteams [testing]
agentteams-manager [testing]
agentteams-worker [testing]
alpine-repo-box [testing]
arch.arch [testing]
$ # 129 boxes total
$ # live divergences
$ charly fleet add x
charly: error: the "fleet" command is retired in the CC-2 vocabulary cutover — use `charly deploy add` / `charly deploy del` instead (fleet → deploy).
$ charly --repo opencharly/charly mcp serve
charly: error: unexpected argument mcp
$ charly --repo opencharly/plugin-mcp mcp serve --help
Usage: mcp serve [flags]
```

The nine landed PRs (charly#604, docs#119, distro-fedora#51/#52, distro-arch#36, distro-cachyos#88, distro-debian#22, distro-omarchy#50, distro-ubuntu#22) each passed their `charly/pr-validator` gate; charly#604 additionally ran the R10 bed (`check-tutorial-shell`, 14/14 PASS).

## Rulebook compliance

- **R0** — every worker that produced the audit's findings was R0-gated (each loaded the owning skills before its first tool call; §7 records it).
- **R1** — every divergence RCA'd; E1 (the tutorial-shell init contradiction) resolved by a live `charly box generate` probe.
- **R2** — no blanket parking: each item is fixed in the merged PRs or routed to a **named** batch (`feat/retire-group-kind` for the `group` provider; the docs deploy drift was fixed in docs#119). The report explicitly avoids the "doc-stale, no fix" dismissal.
- **R3** — duplication register included (generated projections vs hand copies).
- **R4** — no workaround; all CLI facts verified through the `charly` binary.
- **R4a** — §10's reproduction now uses checkout-independent `charly` invocations, not `./bin/charly`.
- **R5** — claim-keyed sweeps recorded; no live stale reference remains in the fixed surfaces.
- **R6** — lands via a `feat/` branch + PR; no direct push to `main`.
- **R7** — N/A — root documentation, no pins/gitlinks changed; `task verify` (the pin gate) is unaffected.
- **R8** — N/A — no emitted artifact generated by this change.
- **R9** — N/A — no binary/source change.
- **R10** — N/A — no runtime path; documentation-only, no bed.

## Change classification

- Class: documentation-only (new root `.md`).
- Gate: non-runtime standards.
- Confidence: **documentation reviewed**.

*Assisted-by: opencode deepseek-v4.1-flash (documentation reviewed)*