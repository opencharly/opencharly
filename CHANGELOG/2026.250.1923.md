# 2026.250.1923 — chore: advance eval-wave gitlinks (eval-omarchy #28-#34, marketplace #290, layer-charly-distros #6, docs #112)

## Summary

Advances the umbrella's submodule gitlinks to record the omarchy PR-eval wave (plan `plan/eval-omarchy-pi-plugin.md`, all PRs merged upstream):

| Submodule | From | To | Merged wave PRs |
|---|---|---|---|
| `eval-omarchy` | 9c217d173a | 4cf3160953 | #28 (VISION/CONTRIBUTING/PR tpl), #29 (docs map+golden-vm+evidence), #30 (work-lane schema), #31 (lane split), #32 (schema migration), #33 (revalidation report+evidence), #34 (cold-read verdict) |
| `marketplace` | 38bc022cae | d0096ec24c | #290 (corpus: omarchy-eval rewritten + omarchy-eval-golden published; layer-charly-distros pin bump) |
| `layer-charly-distros` | 0add419d61 | b034d17d47 | #6 (omarchy-eval skill entity updated to the oracle-generated per-PR reality + omarchy-eval-golden) |
| `docs` | aeae4674f2 | de51317561 | #112 (regenerated corpus pages + gitlink bump) |

No other files changed (`git show --stat`: 4 files, 4 insertions/4 deletions — the four gitlinks).

## How tested

Targeted pin verification on the FINAL committed tree (each of the four: recorded gitlink == checked-out HEAD AND clean working tree):

```
$ git rev-parse HEAD:eval-omarchy eval-omarchy HEAD:marketplace marketplace ...  # per-submodule
eval-omarchy: PIN+CLEAN OK
marketplace: PIN+CLEAN OK
layer-charly-distros: PIN+CLEAN OK
docs: PIN+CLEAN OK
```

```
$ task verify
task: [verify] bash scripts/verify-pins.sh
FAIL: charly: dirty working tree
task: Failed to run task "verify": exit status 1
```

The gate fails on `charly` — an EXTERNAL dirty tree, NOT part of this change (classified, not touched):
- `charly`: checked out at f9844d99 ≠ recorded 81f3cf23, with an uncommitted `go.work.sum` edit — a sibling session/build's state (evidence: `git -C charly status --short` → ` M go.work.sum`).
- `distro-cachyos`: checked out at bd16005 — a non-upstream commit (upstream main = 59dac79) — another session's in-flight branch.
- `layer-android-apidemos` / `layer-android-sdk`: checked out at upstream main but unrecorded (sibling sync advanced them).
Per AGENTS.md and the prior precedent (umbrella #155), these are reported with evidence and operator sign-off requested; they are NOT fixed in this PR because they are not this session's state.

`git diff --cached --check` clean; pre-commit gates green (check-policy-b OK, check-harness-parity OK — pasted above at commit time).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | The verify FAIL was RCA'd: it is the external `charly` dirty tree (sibling session), not this PR's pins — evidenced above, not papered over. |
| R2 | N/A — no rulebook/API wording changed. |
| R3 | One pin per submodule; no restated copies. |
| R4 | N/A — no workaround; the external dirt is reported, not force-resolved (never clobber another session's state). |
| R4a | N/A. |
| R5 | N/A — gitlink advances are additive. |
| R6 | feat/ branch; no force-push; no hook bypass; only this session's four submodules staged. |
| R7 | Targeted verification pasted; full gate run + external-failure classification pasted; operator sign-off requested for the external tree (prior precedent). |
| R8 | N/A. |
| R9 | N/A. |
| R10 | The four changed pins verified from the final committed tree (PIN+CLEAN), gate executed live. |
| RDD | N/A — no behavior claims. |
| ADE | N/A. |
| SDD | N/A. |
| B14 | Every statement above is a pasted output; the gate failure is shown in full, never trimmed. |
| B15 | N/A — no secrets. |
| B17 | N/A. |
| B18 | N/A — nothing skipped; the failure is disclosed, not hidden. |

## Change classification

- Change class: submodule gitlink sync (config/pins).
- Verification gate: targeted pin verification + `task verify` (external-dirt failure documented).
- Attribution tier: analysed on a live system (changed pins verified live; full-gate green blocked by the external `charly` tree with operator sign-off requested).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*