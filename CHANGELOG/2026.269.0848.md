# 2026.269.0848 — feat: register layer-kind as a submodule

## Summary

Registers the merged `opencharly/layer-kind` repo as an umbrella submodule, pinned
to its default-branch HEAD.

- **`.gitmodules`** — adds `layer-kind` (url + `branch = main`), beside `layer-kimi`.
- **gitlink** — `layer-kind` @ `b7b976a` (main; tagged `v2026.269.0833`).
- **`README.md`** — the org-map gains one row in Other layers (`layer-kind` →
  Other layers); the section count bumps 140→141 and the header's "(N today)"
  bumps 408→409.

`layer-kind` is the `kind` (Kubernetes-in-Docker) candy — the host-tooling leg of
the `deploy:kindcluster` substrate. That substrate's `kind:kindcluster` template
kind (spec), its `deploy:kindcluster` provider (plugin-kube), its `kind_cluster`
egress schema (plugin-egress), its `kind` skill, and the `check-kindcluster` R10
bed (charly) have already landed; those repos are already submodules or reached by
`@github` pin, so only `layer-kind` itself was missing from the umbrella.

## Change class

`cross-repo` (umbrella submodule registration). **Breaking change + rollback:**
none; rollback = revert this commit (removes the submodule + the README row).

## How tested — the umbrella's own gates

A pin/view change has no `charly check` bed; the umbrella's own gates are the
change-class gate, and they pass on the final committed tree (head `f174cd7`).

*1. `task verify` — the full pinning gate (policy-B + the 409-submodule audit + the
opencode plugin contract), run from the umbrella worktree:*

```
$ ./charly/bin/charly task verify
task verify: 3 step(s), 0 failed
  [pass] check — policy B — every distro-* gitlink equals charly's own box/* gitlink
      ran 1 task(s), 0 step(s) failed
  [pass] check — every submodule is clean and checked out at its recorded gitlink, and its branch equals the remote default
      exit=0
  [pass] check — the opencode gate plugin contract (the check-opencode-plugin gate the former verify-pins.sh ran)
      exit=0
```

*2. `task org-map` — README ↔ `.gitmodules` reconciliation:*

```
$ python3  # the org-map check body
section                    claimed  rows  sorted
### Other layers               141   141     yes
...
submodules in .gitmodules : 409
submodules with no row    : none
header '(N today)'        : 409  matches=True

OK: all sections reconcile and are sorted; every submodule has a row
```

*3. The gitlink pins a merged default-branch HEAD, never a PR branch:*

```
$ git ls-remote git@github.com:opencharly/layer-kind.git main
b7b976a7b376e1727594617ce39b4100400e321a	refs/heads/main
$ git ls-files -s layer-kind
160000 b7b976a7b376e1727594617ce39b4100400e321a 0	layer-kind
```

**Did the changed path execute live?** Yes — the changed paths are the umbrella's
gate scripts (`task verify`, `task org-map`), and both ran green above.

**R10 beds:** `N/A — a pin/view change; no charly runtime path changed.` The
runtime proof for the composed `kindcluster` substrate is the `check-kindcluster`
R10 bed in the charly repo (`charly check run check-kindcluster`, PASS).

## Rulebook compliance

- **R0 (skills first):** loaded `/charly-internals:git-workflow`,
  `/charly-internals:strict-policy`.
- **R1 (RCA, zero warnings):** the first `task verify` run failed on charly's
  *nested* submodules being uninitialized in the fresh worktree — root-caused
  (an unpopulated nested checkout, not the change) and fixed by initializing them;
  the re-run is green with zero warnings.
- **R2 (no out-of-scope):** purely additive registration; nothing deferred.
- **R3 (no duplication):** the README row follows the existing per-section format;
  the org-map gate derives the counts from the tables.
- **R4 (no workaround):** the gitlink pins a merged default-branch HEAD, never a PR
  branch; the gate scripts are the umbrella's own.
- **R4a:** `N/A — no defect being routed around.`
- **R5 (hard cutover + grep):** `N/A — purely additive, no identifier removed or
  renamed`.
- **R6 (git safety):** isolated in a linked worktree (rule 9 — the root checkout is
  on another session's branch with a dirty `distro-cachyos`, left untouched); no
  force-push; landed via a `feat/` branch + PR.
- **R7 (prove behavior):** the changed paths are the umbrella gates; they executed
  live (pasted above), not merely compiled.
- **R8 (artifact invariants):** `N/A — no artifact`.
- **R9 (binary == source + deps):** `N/A — no binary`.
- **R10 (fresh disposable proof):** `N/A — no runtime change`; the umbrella's own
  gates (verify + org-map) are the change-class gate and pass on the final tree.

## Change classification

- **Change class:** `cross-repo` — umbrella submodule registration (a pin/view change).
- **Verification gate:** `./charly/bin/charly task verify` (3/3) + `task org-map`
  (all sections reconcile, 409 submodules each with a row).
- **Attribution tier:** `fully tested and validated` — the umbrella's gates pass on
  the final committed tree, and the registered gitlink points at the merged
  default-branch HEAD of `layer-kind`.

### Attribution evidence (A3)

The authoring runtime's own model registry reports this model as provider
`ollama-cloud`, id `ollama-cloud/deepseek-v4.1-flash`, name **`DeepSeek V4.1
Flash`**, status active. Harness: `OpenCode`.

*Assisted-by: OpenCode DeepSeek V4.1 Flash (fully tested and validated)*