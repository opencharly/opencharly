# 2026.259.1146 — docs(agents): sweep the omarchy PR-eval agents onto the eval-pr layout

## Summary

Docs-only sweep of the four committed omarchy PR-eval agents (`.pi/agents/omarchy-{eval-supervisor,config-oracle,eval-runner,cold-reader}.md`) onto the current eval-omarchy lane layout.

`opencharly/eval-omarchy#78` (merged, tagged **v2026.259.1014**) consolidated the lane's v1 corpus — `candy/eval-lane/charly.yml` plus the 7 skills `omarchy-eval-full-loop`/`-golden`/`-media`/`-sequencing`/`-tests`/`-tiers`/`-work-lane` — into `candy/eval-pr/charly.yml` (the entry `omarchy-eval` + `omarchy-eval-oracle` + `omarchy-eval-cold-reader`). The per-PR record is now emitted **schema-first** by the new `emit` stage and graded by the **org pr-validator** criteria (the new `validate` stage), with the record at `eval/pr-<N>/eval.yml`.

The four agents still described the retired v1 paths, so they were stale siblings of that change (R5). Swept:

| retired | current |
|---|---|
| `candy/eval-lane/charly.yml` | `candy/eval-pr/charly.yml` |
| `pr-beds/pr-<N>/charly.yml` | `eval/pr-<N>/charly.yml` (rendered by the `render` stage) |
| `metrics/<calver>-pr-<N>.md` | the committed `eval/pr-<N>/eval.yml` record |
| `media/<calver>/pr-<N>/` | `eval/pr-<N>/media/` |
| "the omarchy-eval-full-loop entity" | the `eval-pr-plan` pipeline's `redo:` edges + the entry `omarchy-eval` skill |
| — | the supervisor/oracle now name the pr-validator applicability map and the separate `validate.verdict` field |

## How tested

**1. The repo's own agent gate (the deterministic gate for these files), on the final tree:**

```
$ bash scripts/check-omarchy-eval-agents.sh
check-omarchy-eval-agents: OK — 5 unique agents tracked + parseable, staged agents ignored
```

**2. R5 sweep — no retired identifier survives in `.pi/agents`** except this commit's intentional historical note ("the `candy/eval-lane/` v1 corpus was consolidated"):

```
$ grep -rn "eval-lane\|pr-beds\|metrics/<calver>\|-vm-probe" .pi/agents/
.pi/agents/omarchy-eval-supervisor.md:20: ... (the `candy/eval-lane/` v1 corpus was consolidated into `candy/eval-pr/`).
```

**3. Every replacement claim was re-derived from the merged eval-omarchy** (not assumed):

```
$ git -C eval-omarchy ls-tree --name-only caf4ee7:candy
eval-pr
...
$ git -C eval-omarchy show caf4ee7:candy/eval-pr/charly.yml | grep 'name: omarchy'
        name: omarchy-eval
        name: omarchy-eval-oracle
        name: omarchy-eval-cold-reader
```

**4. Diff is docs-only (4 files), gitlinks untouched:**

```
$ git diff --stat HEAD~1
 .pi/agents/omarchy-cold-reader.md     |  8 +++++---
 .pi/agents/omarchy-config-oracle.md   | 24 +++++++++++++++++-------
 .pi/agents/omarchy-eval-runner.md     |  6 +++---
 .pi/agents/omarchy-eval-supervisor.md |  6 +++---
 4 files changed, 28 insertions(+), 16 deletions(-)
```

The `eval-omarchy` / `marketplace` gitlinks are unchanged in this PR; they advance through the automated `task sync` (`scripts/sync-gitlinks.sh`) workflow, which owns policy B pinning.

## Rulebook compliance

- **R1** — the retired-path divergence is root-caused (eval-omarchy#78 consolidated the corpus; these agents were stale siblings), not assumed; every replacement was re-derived from the merged tree.
- **R2/R5** — the whole sweep lands in one docs-only commit; no retired identifier survives except the deliberate historical note.
- **R3** — the agents point at the one owning lane contract (`eval-pr-plan` + the `candy/eval-pr` skills) instead of restating it.
- **R4** — docs-only, no workaround.
- **R6** — PR-only; add-only.
- **R7** — the repo's agent gate run on the final tree and pasted.
- **R10** — docs-only change class: the repo's non-runtime gate (`scripts/check-omarchy-eval-agents.sh`) is the applicable standard; no bed can fail on prose.

## Change classification

- Change class: harness project configuration (the committed `.pi/agents` definitions) — documentation-only.
- Verification gate: `scripts/check-omarchy-eval-agents.sh` + the R5 sweep + the merged-tree re-derivation, on the final tree.
- Attribution tier: documentation reviewed — comment/prose-only edits, every non-runtime standard passed.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (documentation reviewed)*