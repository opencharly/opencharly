# 2026.247.1049 — pi: bump pi-asciinema package to v2026.247.1038 (ships cast-analysis skill)

## Summary

Bumps the umbrella main pi config entry for pi-asciinema to the new tag:

```jsonc
"git:github.com/opencharly/pi-asciinema@v2026.247.0912"  →  "git:github.com/opencharly/pi-asciinema@v2026.247.1038"
```

pi-asciinema v2026.247.1038 (opencharly/pi-asciinema#3, validator PASS, merged 2026-09-04T10:38Z) ships the **`cast-analysis` skill** in addition to the `cast_read` extension — umbrella sessions now load the skill automatically from the package (pi loads package `skills/`, like pi-goal/pi-subagents).

## How tested

1. **Skill loads live**: `pi -p --no-session --skill <repo>/skills/cast-analysis/SKILL.md ...` (provider ollama-cloud, umbrella worktree) — the agent named the loaded skill `cast-analysis` and quoted its recipes-table first row verbatim (`| Full transcript (grep-able, ANSI stripped) | format="text" |`).
2. **R7 gate on the final tree (this branch):** `bash scripts/verify-pins.sh` → `verify-pins: OK — 395 submodules, policy B holds`; `check-policy-b: OK — 6 distro pins equal charly's gitlinks` (submodule sync needed: eval-omarchy checkout refreshed to the current main gitlink).
3. `pi list` shows the tagged package installable from the git ref; the tag exists on the remote (`git ls-remote --tags`).

## Rulebook compliance

- Rule 5 (pin discipline): the entry pins a MERGED tag (`v2026.247.1038`), never a PR branch.
- PR-only, feat/ branch, no force-push; org-wide pr-validator gates.
- Cross-repo sequencing respected: repo #3 merged + tagged before this bump PR was created.
- PR body = changelog (tag-on-merge contract).

## Change classification

- **Change class**: config change — pi package tag bump in `.pi/settings.json`
- **Verification gate**: fully tested and validated — final tree ran the full R7 pinning gate OK and the changed runtime path (skill loading from the package) executed live
- **Attribution tier**: fully tested and validated

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*