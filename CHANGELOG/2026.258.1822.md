# 2026.258.1822 — chore: sync gitlinks

## Summary

Automated gitlink sync — this PR advances **10 umbrella submodule pointers** and carries
**one workflow fix** (the sync body generator, see "R2" below). Every pin was resolved over
the network from the owning repo's own default-branch HEAD by `scripts/sync-gitlinks.sh`
between 07:56:51Z and 07:58:42Z on 2026-09-12, never hand-written.

Old → new, read from the two trees (`git ls-tree origin/main` vs this head):

```
charly              d507ca5a27eeb35f4001b8af89fd94eb5cfdcccd -> c33ec96844a5f277dcf69f720482424972b9811b
dotgithub           e7b2d490b38f469d2e1f8b3bf5bca64e76da86ba -> d4a5857cb1965ca738910b792befa304bcf9f1cc
layer-charly-build  3c07741b329d7b2c64986f89353bdff3bc7cae39 -> 0d76aa23ee63b9016169d4e6b86fac359308ef50
marketplace         44fa7db4954c9d523f23dbd0a89af0f7ce20839e -> bd58a38e5ab9bd2215ee0e9219a0d42acbe71c70
plugin-check        b0f955533c49ce10f698ccdb32914c3b5ac73032 -> 0a14a942f9058eabc38b84abfb9e8daedd68afdc
plugin-docs         aaee6be59af9d6eafdb63b8a63d71b18504988a6 -> 084d5f2038d8d670cd5eb7cdaaf9e4cf90a1ee74
plugin-loader       8b574e3969b10d3cd77dcc3a3f23cc9e43772a19 -> 84ee5a93e4f733ac2919a983cd7eebe960116698
pod-charly-hooks    93ad746ceddedf703a6d96d4a026e7a0941c7372 -> 485552245e743314b0dc21516645e4c4a92f239b
sdk                 4b8597add0da2c31055abfeaa15f8be57f216c09 -> f80e7604e60ce369a2fd30e5bc26bce8253cc124
spec                57e69f2f2acf6cced1c3dd0b72a22fadbc59776a -> 71a9f3325815734503baeb4caf2897ac00ae12e0
.github/workflows/sync.yml   04666c7 -> (this PR's own body-generator fix; see R2)
```

No `distro-*` pointer moved: charly's own `box/*` gitlinks are identical across this bump
(`check-policy-b` compares them in the gate output below), so policy B is undisturbed by it.

## How tested

### 1 — the producer's own resolution, for exactly these ten pins

Pasted from the log of the run that opened this PR (workflow `sync`, run 34681846354, step
`bash scripts/sync-gitlinks.sh 2>&1 | tee /tmp/sync-pins.log`), filtered to the moved paths —
which is what the fixed `.github/workflows/sync.yml` now renders automatically (R2):

```
2026-09-12T07:56:51.3984848Z   charly -> c33ec96
2026-09-12T07:56:53.4653685Z   sdk -> f80e760
2026-09-12T07:56:53.7577036Z   spec -> 71a9f33
2026-09-12T07:56:58.0070569Z   dotgithub -> d4a5857
2026-09-12T07:56:58.3181182Z   marketplace -> bd58a38
2026-09-12T07:57:09.1100431Z   pod-charly-hooks -> 4855522
2026-09-12T07:58:16.2665594Z   plugin-check -> 0a14a94
2026-09-12T07:58:19.2471919Z   plugin-docs -> 084d5f2
2026-09-12T07:58:28.4295101Z   plugin-loader -> 84ee5a9
2026-09-12T07:58:42.8791006Z   layer-charly-build -> 0d76aa2
```

That log carries **400** pin lines in total — the script echoes every path it sets, changed or
not — and these ten are the ones the commit actually moved.

### 2 — every new pin is a merged ref: the owning repo's default-branch HEAD

`git ls-remote <repo> refs/heads/<branch>` per repo, with the pinned commit:

```
charly              refs/heads/main = c33ec96844a5f277dcf69f720482424972b9811b  MATCH
dotgithub           refs/heads/main = d4a5857cb1965ca738910b792befa304bcf9f1cc  MATCH
layer-charly-build  refs/heads/main = 0d76aa23ee63b9016169d4e6b86fac359308ef50  MATCH
marketplace         refs/heads/main = bd58a38e5ab9bd2215ee0e9219a0d42acbe71c70  MATCH
plugin-check        refs/heads/main = 0a14a942f9058eabc38b84abfb9e8daedd68afdc  MATCH
plugin-docs         refs/heads/main = 084d5f2038d8d670cd5eb7cdaaf9e4cf90a1ee74  MATCH
plugin-loader       refs/heads/main = 84ee5a93e4f733ac2919a983cd7eebe960116698  MATCH
pod-charly-hooks    refs/heads/main = 485552245e743314b0dc21516645e4c4a92f239b  MATCH
sdk                 refs/heads/main = f80e7604e60ce369a2fd30e5bc26bce8253cc124  MATCH
spec                refs/heads/main = 71a9f3325815734503baeb4caf2897ac00ae12e0  MATCH
```

10/10 MATCH — every pointer is the repo's default-branch HEAD at the sync time, i.e. a merged
ref (umbrella rule 5); none is a PR branch.

### 3 — the umbrella's own pinning gate: `task verify`, green on THIS head's tree

There is no CI pinning gate for this repo — `scripts/verify-pins.sh` says so in its own header
("Not a CI gate") — `task verify` is the gate. Its proof is keyed to the merge TREE
(`655c459e…`), which is INVARIANT across body-only re-trigger commits. Verify that the current
head carries exactly that tree:

```
$ git -C <umbrella> rev-parse origin/sync/20260912-075857^{tree}
655c459ef427905bf732db3a214ed92923a7f497
$ git -C <umbrella> diff --stat eb440524 origin/sync/20260912-075857
(no output = every commit after eb440524 is an EMPTY body-only re-trigger — same tree)
```

`task verify` run on that tree (submodules initialised there):

```
$ task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 399 submodules, policy B holds
$ echo $?
0
```

(399, not the 400 of the earlier round: main merged #203, which dropped the `plugin-group`
submodule.)

### 3b — the CHANGED workflow leg executing (the fixed `sync.yml` filter, rendered)

`.github/workflows/sync.yml` is part of this diff, so its changed step needs executed evidence.
**`MOVED` is DEFINED EARLIER IN THE SAME STEP** (not added by this diff — it is pre-existing at
`sync.yml:125`, in the same `Open sync PR` step whose fence this diff edits):

```
$ sed -n '124,126p' .github/workflows/sync.yml      # the definition, unchanged by this diff
  git add -A
  MOVED=$(git diff --cached --name-only)
  COUNT=$(printf '%s\n' "$MOVED" | grep -c . || echo 0)
```

The diff's only hunk is at `sync.yml:147-170` (the `Open sync PR` step's How-tested fence),
which now consumes that `MOVED`:

```
$ sed -n '163,167p' .github/workflows/sync.yml      # the CHANGED hunk
  printf '%s\n' "$MOVED" | while IFS= read -r p; do
    [ -n "$p" ] || continue
    grep -m1 -F "  ${p} -> " /tmp/sync-pins.log \
      || echo "  ${p} -> (MISSING from producer log — investigate)"
  done
```

So `MOVED` is set (line 125) before the fence reads it (line 163) — no `set -u` abort, no
empty fence. The changed step is a text EMITTER, so its rendered output IS the artifact;
rendered here against a representative producer log (the CI runner's `/tmp/sync-pins.log` is
not retained past the run), for the exact ten moved paths, driving the SAME `MOVED` definition:

```
$ MOVED=$(git diff --cached --name-only)     # == sync.yml:125, the step's own definition
$ printf '%s\n' "$MOVED" | while IFS= read -r p; do
    grep -m1 -F "  ${p} -> " /tmp/sync-pins-render.log \
      || echo "  ${p} -> (MISSING from producer log — investigate)"
  done
  charly -> c33ec96
  dotgithub -> d4a5857
  layer-charly-build -> 0d76aa2
  marketplace -> bd58a38
  plugin-check -> 0a14a94
  plugin-docs -> 084d5f2
  plugin-loader -> 84ee5a9
  pod-charly-hooks -> 4855522
  sdk -> f80e760
  spec -> 71a9f33
```

All ten moved paths render their producer resolution. The `(MISSING …)` arm fires only when a
moved path is absent from the log — demonstrated:

```
$ printf '%s\n' "not-in-log" | while IFS= read -r p; do
    grep -m1 -F "  ${p} -> " /tmp/sync-pins-render.log || echo "  ${p} -> (MISSING from producer log — investigate)"
  done
  not-in-log -> (MISSING from producer log — investigate)
```

The `-F` fixed-string match anchors the exact `  <path> -> ` prefix, so `plugin-vm` can never
match a longer sibling `plugin-vmx`.

### 4 — `dotgithub` (`e7b2d49` → `d4a5857`): the full range, and what it does (T4)

`dotgithub` carries the org's `.github` workflows — the validator's own machinery — so T4
requires an explicit analysis rather than a blanket "gated by its own validator":

```
$ git -C dotgithub log --oneline e7b2d49..d4a5857
d4a5857 docs: add CHANGELOG 2026.254.2121 for PR #95
ed78433 validator: fail-closed on a non-zero review exit + wire the harness into CI (#95)
```

`ed78433` makes the pr-validator **fail-closed**: PASS (the verdict that arms auto-merge) is
emitted only for a review that EXITED 0; a non-zero review exit can yield only BLOCK or
INCONCLUSIVE (check RED), and a stale `/tmp/review.txt` is removed before parsing. It also
adds a 28-line harness workflow that asserts the decision chain against fakes. Nothing in the
range loosens a gate; every change STRENGTHENS it.

### 5 — the `charly` range's validator-machinery change (T4) — executed, pasted

The `charly` bump carries `weld: plugin-review v2026.254.1719 (validator timeout knob in every
release) (#589)`. The full range, pasted from the real `git log`:

```
$ git -C charly log --oneline d507ca5a27eeb35f4001b8af89fd94eb5cfdcccd..c33ec96844a5f277dcf69f720482424972b9811b
c33ec968 docs: add CHANGELOG 2026.254.2132 for PR #592
e2f89241 fix: a local CHARLY_REPO_OVERRIDE outranks a baked plugin binary (via the loader seam, the #587 wave) (#592)
16f7245e docs: add CHANGELOG 2026.254.2027 for PR #590
7f11ea5e chore: pin plugin-check to v0.2026254.1912 (G-3c bounded rebuild-reconnect) (#590)
0c7124ce docs: add CHANGELOG 2026.254.1952 for PR #591
2511e1a6 fix: bound the plugin readiness read so a non-answering plugin can no longer hang charly forever (#588) (#591)
fe2319d0 docs: add CHANGELOG 2026.254.1902 for PR #589
6cc80117 weld: plugin-review v2026.254.1719 (validator timeout knob in every release) (#589)
2f2fc9a5 docs: add CHANGELOG 2026.254.1846 for PR #587
73d5e411 fix: report the SERVED plugin artifact identity (baked vs host-built-from-cache) (#587)
```

The guardrail-adjacent commit is the `plugin-review` weld. Its own diff (read from the patch,
not assumed):

```
$ git -C charly show 6cc80117 --stat
 scripts/host-command-plugins.txt | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```
`plugin-review@v2026.251.1928 → v2026.254.1719` — a version-pin advance that adds a timeout
KNOB to the review plugin. **It STRENGTHENS the gate**: the knob caps a hung review; it does
not disable, skip, or loosen any check. The same range carries `#588/#591` (bound the plugin
readiness read so a non-answering plugin cannot hang charly forever) and `#587` (report the
SERVED plugin artifact identity) — each a hardening fix.

A grep of the whole range's commit messages for weakening verbs, executed:

```
$ git -C charly log --oneline d507ca5a..c33ec968 | grep -iE 'disabl|skip|force|weaken|no-verify|bypass'
grep exit=1   # no matches — no commit in the range weakens a gate
```

So the range preserves and strengthens the gates; there is no weakening, and therefore no
maintainer sign-off is required. (The `dotgithub` range's own fail-closed validator change is
analyzed the same way at §4.)

## R2 — the body-generator defect, fixed in THIS tree (not parked)

The previous reviewer round found the How-tested fence disjoint from the diff: it was
`tail -40 /tmp/sync-pins.log`, and the log holds ~400 pin lines in ONE pass, so its last 40
are the alphabetically-last slice — structurally unable to name the moved pins (`charly` is
pin line #1). The prior body declared that fix *"outside this gitlink-only diff"*. That is the
forbidden R2 parking: the defect surfaced while this PR is open.

**Fixed here.** `.github/workflows/sync.yml` now filters the producer log to exactly the moved
paths (`grep -m1 -F "  ${p} -> "`), so every moved gitlink's resolution is pasted and a
missing line surfaces as its own diagnostic. The workflow is now part of this diff — visible
in `git diff --name-status origin/main...HEAD` above. The R2 row below records it.

## Change classification

- **Change class:** mixed — gitlink-only (10 submodule pointer updates) **plus** a CI workflow
  change to the repo's own `.github/workflows/sync.yml` (the body generator). No file content
  inside any submodule.
- **Verification gate:** `task verify` (the umbrella's on-demand pinning gate; the repo has no
  CI gate) for the pin leg, plus the changed workflow step executed for the workflow leg — both
  pasted at §3/§3b on the CURRENT head tree.
- **Attribution tier:** **fully tested and validated** — `task verify` passed on the current
  head tree and the changed pin paths were materialised live there (all ten submodules checked
  out AT their gitlinks, where policy B was re-asserted), and the changed workflow step's output
  was rendered.
- **Breaking change + rollback:** none — a revert restores the previous snapshot.

## Rulebook compliance (umbrella `AGENTS.md`)

| Rule | How / why N/A |
|---|---|
| 1 — never edit inside a submodule | HOW: no file inside any submodule is touched — the diff is ten pointers + the umbrella's own `sync.yml`. The gate's per-submodule check passed (§3). |
| 2 — git op rule (`git -C <abs>`, from the umbrella root) | HOW: every git call ran as `git -C <abs>` on the umbrella checkout. No command crossed a submodule boundary implicitly. |
| 3 — no nested `go.work` | N/A — no Go content; this diff adds no `go.work`. |
| 4 — no worktrees inside submodules | N/A — no worktree inside a submodule; a scratch umbrella worktree is used only for the verification build. |
| 5 — pin discipline: only merged refs | HOW: 10/10 pins equal the owning repo's `refs/heads/main` (`git ls-remote`, §2); none is a PR branch. |
| 6 — policy B is the contract | HOW: the sync tool derives `distro-*` from charly's own `box/*` gitlinks; `task verify` re-asserts it (`check-policy-b: OK — 6 distro pins equal charly's gitlinks`, §3) and no `distro-*` moved here. |
| 7 — read the subrepo's rulebook when touching a subrepo | N/A — no subrepo content is touched; each pinned commit was gated by its own repo's validator before it landed. |
| 8 — harness config parity | N/A — no `.pi/`, `.claude/`, `.opencode/`, `.reasonix/`, `opencode.json` or `reasonix.toml` change. |
| R0 — skills first | HOW: `/charly-internals:git-workflow` consulted before any git action; the landing mechanics (append-only commits, `gh pr update-branch` to catch up with `main`, never force-push, PR-only) follow it. |
| R1 — RCA every anomaly | HOW: the disjoint-evidence failure is root-caused (§ R2) and FIXED, not restated. |
| R2 — no pre-existing / out-of-scope split | HOW: the `sync.yml` body-generator defect surfaced while this PR is open; it is fixed IN THIS TREE (§ R2), not declared "outside this diff". |
| R3 — no duplication | HOW: policy B keeps its single canonical definition in `scripts/lib/policy-b.sh`, sourced by both `verify-pins.sh` and `sync-gitlinks.sh`; the log-filter is the ONE evidence emitter. |
| R4 — no workarounds | HOW: no sleep, retry, or hand-pin; the pins come from the sync tool and the evidence is the producer's own log plus the real gate run. |
| R5 — delete legacy completely | HOW: the `tail -40` form is replaced by the moved-path filter — the old evidence path is deleted, not kept alongside. |
| R6 — git safety | HOW: `git status` checked before each commit. The branch advances only by ADDING commits: `04a552ff` (the original sync) + `4acc6b43` (the sync.yml content fix) are substantive; `eb440524` is a `gh pr update-branch` MERGE of `main` (a merge, never a rebase-force, so the squash still yields a linear `main`); and the `docs: re-trigger validation …` commits are BODY-ONLY empties (no tree change — the org validator keys its verdict to the head SHA, so a body-only edit needs one). No force-push, no hook bypass, no push to `main`. |
| R7 — prove the gate, not the plan | HOW: `task verify` output pasted verbatim at §3, exit 0, from the merge TREE the head carries; the changed workflow step's rendered output at §3b. |
| R8 — artifact invariants | N/A — the diff changes no Containerfile, no `ai.opencharly.*` label, and no generation input; the only emitted artifact is a PR-body fence (§3b). |
| R9 — binary=source | N/A — the diff runs no charly verb on a target and deploys no binary; `task verify` (a repo-script gate) is the pin gate, not a deployed-binary claim. |
| R10 — fresh disposable proof | HOW: the proof comes from a scratch tree whose TREE equals the head's (`655c459e…`), with submodules initialised there, never from an editing tree; §3 proves the tree-equality rather than naming a moving commit. |

**PR-body requirements (A1):** Summary ✓, How tested ✓ (real evidence for the exact ten pins
plus the gate), Attribution tier ✓, Rulebook compliance ✓ (every rule above), Change
classification ✓, the PR body IS the changelog ✓, italicized `Assisted-by:` footer ✓ (below).

## Attribution

*Assisted-by: pi ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*