# 2026.251.1929 — sync(gitlinks): F-wave completion — spec/sdk to the F merges, charly/plugin-box to post-tag main (policy B)

## Summary

Umbrella gitlink completion for the F-wave: the F-wave umbrella sync (#171) advanced charly + plugin-box but NOT spec/sdk — the four pins are now reconciled to each repo's origin/main (policy B):
- charly b5ea83a5e6 → b5ea83a5c4 (post-#171 CHANGELOG), plugin-box 18d3831c → c3177cf (post-#12 CHANGELOG)
- spec 94fe1b8d → 2529662 (spec#125: provider-class vocabulary single source + CandyKindCarrier, F3.1/F4.2)
- sdk b8b30069 → de6e405 (sdk#242 parser consolidation F4.3/4.4/4.5 + sdk#243 F5 loaderkit clean)

## How tested (pasted)

```
$ git diff --cached --submodule=short | grep "^[+-]Subproject" | wc -l
8   (= 4 gitlink moves)
$ bash scripts/check-policy-b.sh   # pre-commit gate
OK — 6 distro pins equal charly's gitlinks
```

Per repo: `git -C <r> rev-parse origin/main` == the staged gitlink (fetched + verified 4/4 before staging).

## Rulebook compliance

- R6: PR-only; no force-push; no direct main push. pre-commit gate (check-policy-b + check-harness-parity) ran green.
- R9: pin discipline — every gitlink equals its repo's merged origin/main; no PR branches, no dangling pins.

## Change classification

Class: umbrella submodule gitlink sync (4 pointers). Gate: policy B + parity (pre-commit) + the per-repo origin/main verification. Attribution: **analysed on a live system** (each pin verified live against its repo's fetched origin/main).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*