# 2026.241.2133 — chore(pin): sync gitlinks to upstream default-branch HEADs

## Summary

Syncs the umbrella's gitlinks to upstream default-branch HEADs (the umbrella was behind):

- **charly** `b9eac81a` → `feca5167` — upstream main: PR #461 bumped `box/cachyos` for the punktfunk beds. The `candy/` directory is LONG GONE upstream (deleted in #457); the local stale `candy/` leftovers (empty dirs + old binary copies from the dropped binary-sync, PR #454) were removed from the working tree.
- **distro-cachyos** `366b06f` → `2837800` — equals charly's own `box/cachyos` gitlink (policy B).
- **pod-sway** `11ebe05` → `1c5790c` — its own default-branch HEAD.

Also initialized the two punktfunk submodules (`layer-punktfunk`, `plugin-punktfunk`) that were uninitialized (their git resolved to the umbrella root, failing the pinning gate).

## How tested

- `bash scripts/verify-pins.sh` on the final tree → **PASS**:
  ```
  check-policy-b: OK — 5 distro pins equal charly's gitlinks
  verify-pins: OK — 346 submodules, policy B holds
  ```
- `git ls-remote https://github.com/opencharly/charly.git refs/heads/main` → `feca5167` (matches the new gitlink).
- `git ls-remote https://github.com/opencharly/pod-sway.git refs/heads/main` → `1c5790c` (matches).
- `git -C charly rev-parse HEAD` → `feca5167`; `git -C distro-cachyos rev-parse HEAD` → `2837800`; `git -C pod-sway rev-parse HEAD` → `1c5790c` (working trees match the staged gitlinks).
- `git ls-tree feca5167 candy/` → empty (candy/ untracked upstream — confirmed gone).

## Rulebook compliance

- Rule 5 (pin discipline): only merged refs (default-branch HEADs).
- Policy B: `distro-cachyos` equals charly's own gitlink; `pod-sway` follows its own default-branch HEAD.
- Rule 6 (git safety): `git status` checked before the sync; no force-push.

## Change classification

- **Change class:** gitlink sync
- **Verification gate:** fully tested and validated (verify-pins.sh PASS on the final tree)
- **Attribution tier:** fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*