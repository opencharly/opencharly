# 2026.252.0735 — docs: rebrand umbrella README to the agentic config orchestrator

## Summary

Rebrand the umbrella README to the new charly positioning: **"the agentic config orchestrator — everything is a plugin, nothing is mandatory."**

- `charly/` org-map row: "The open infrastructure compiler — for you and your agents." → "The agentic config orchestrator — everything is a plugin, nothing is mandatory."
- `docs/` org-map row: drops the "candy factory" voice → "Documentation site for OpenCharly — the agentic config orchestrator, its candies, and its plugins."
- `pi-review-action/` row: marked **Archived** — the org PR-review gate now runs the charly engine (`opencharly/action-review`)
- Pinning section: validator is now the charly review engine, not `pi-review-action`
- Audit fix: repo count 394 → **400** gitlinks (recomputed from live `.gitmodules`; all per-category counts verified correct)

## How tested

- `git diff` reviewed: 5 insertions, 5 deletions, README.md only
- Counts: `git config --file .gitmodules --get-regexp '^submodule\..*\.path$' | wc -l` → 400; per-category table row counts verified against header claims (5/6/10/5/22/13/2/15/138/106/74/3 = 399 + dotgithub = 400)
- Validator engine: `gh api repos/opencharly/.github/contents/.github/workflows/pr-validator.yml` — runs the charly engine via `opencharly/action-review` (replacing pi-review-action); `pi-review-action` archived=true, `action-review` archived=false
- `grep -i 'compiler|compile|candy factory'` on the new README: zero hits

## Rulebook compliance

- R0: `/charly-internals:git-workflow` + `/charly-internals:root-cause-analyzer` loaded before any git/gh step
- R1: every divergence RCA'd — stale count (gitlink growth since last regen), stale validator reference (action-review cutover landed 2026-09-08, README not updated)
- R5: corrected facts replace old ones in the same PR
- R6: PR-only, no force-push, no direct push to main
- R7: docs-only change; no pins, scripts, or code touched

## Change classification

- Change class: **docs-only**
- Verification gate: **docs gate**
- Attribution tier: **documentation reviewed**

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*