# 2026.243.1452 — chore: sync gitlinks — plugin-bpf + plugin-cardwire + distro-cachyos head

## Summary

Umbrella gitlink sync for the cardwire-support plan (task 5):

- Adds the two NEW external command plugin submodules: **plugin-bpf** (default-branch head; merged tag v2026.243.1059) and **plugin-cardwire** (default-branch head; merged tag v2026.243.1202).
- Bumps the other staged gitlinks to their default-branch heads (layer-desktop-media, marketplace, pod-charly-core, pod-selkies-core) as the sync script found them drifted.
- NOTE: `distro-cachyos` is NOT in this diff — per Policy B the distro-* pins follow charly's own gitlinks and charly re-pins distro-cachyos (now carrying check-cardwire-vm) on its next sync; the umbrella will pick that up in the following sync, by design.

## How tested

```
$ scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 358 submodules, policy B holds
```

Pre-commit gate also green (check-policy-b OK, check-harness-parity OK). The gate ran live on the final committed tree (R10).

## Rulebook compliance

- Policy B: distro-* equals charly's gitlinks (verified live above; unchanged by this diff).
- Pin discipline: only default-branch heads (merged refs); no PR branches.
- R6: clean status before commit; no force-push.

## Change classification

- Change class: infra/chore (gitlink sync: 2 new submodules + drift bumps).
- Verification gate: `task verify` GREEN on the final committed tree, pasted above.
- Attribution tier: `fully tested and validated` — the full pinning gate ran live on this exact tree (verify-pins + policy B + pre-commit gates).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*