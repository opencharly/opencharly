# 2026.247.1012 — pi: add pi-asciinema package to the main pi config (cast_read tool)

## Summary

Wire **`pi-asciinema`** (the new org pi package, repo `opencharly/pi-asciinema`, merged @ c85836f7f3, tag `v2026.247.0912`) into the umbrella's **main pi config** — `.pi/settings.json` `packages` gains one entry:

```jsonc
"git:github.com/opencharly/pi-asciinema@v2026.247.0912"
```

Effect: from the umbrella root, every agent gets the **`cast_read`** tool (asciinema `.cast` recording reader — summary / filtered events / ANSI-stripped text with markers, output hard-capped by `max_chars`) and **bare `pi -p "<prompt>"` works with zero extra parameters** (project packages auto-load on startup; auto-install on trust). This is the org-level consumption of the extension — the repo PR already proved the implementation and its full validation.

## How tested

1. **L3 bare `pi -p` matrix, run in this worktree with the extension loaded from the git ref on the tag** (proof that the config entry works end-to-end):
```
$ node pi-asciinema/scripts/validate-bare.mjs
PASS  preflight: umbrella .pi/settings.json has pi-asciinema package entry
PASS  preflight: dev-fixtures real casts present
PASS  preflight: pi binary on PATH — 0.84.4
PASS  preflight: big fixture generated
PASS  warmup: bare pi -p responds
PASS  summary on real eval-omarchy cast (pr-9332, v3)
PASS  text extraction on real eval-omarchy cast (omarchy-gif)
PASS  markers surfaced (committed markers.cast)
PASS  v2 compatibility (committed v2-session.cast)
PASS  malformed cast handled gracefully (committed malformed.cast)
PASS  bounds respected on large cast (max_chars=2000)
ALL BARE pi -p CHECKS PASSED
```
2. **Full pi -p validation** (repo PR #1 evidence): L1/L2 — 22/22 unit + direct-execute tests, typecheck clean, load check exit 0; bare matrix 6/6 PASS from the umbrella root (incl. real `eval-omarchy/media` casts); CI-variant 7/7 PASS incl. `--mode json` tool_call observability. All in opencharly/pi-asciinema#1 (validator PASS).
3. **R7 gate on the final tree (this branch):** `bash scripts/verify-pins.sh` → `verify-pins: OK — 395 submodules, policy B holds`; `check-policy-b: OK — 6 distro pins equal charly's gitlinks`.
4. `pi list` in this worktree shows `git:github.com/opencharly/pi-asciinema@v2026.247.0912` installed from the tag.

## Rulebook compliance

- Rule 8 (harness config parity): `.pi/settings.json` is umbrella-specific (not in `scripts/check-harness-parity.sh`'s SHARED diff list), so no forced charly twin; charly's `.pi/settings.json` intentionally stays its own fork.
- PR-only landing, feat/ branch, no force-push; org-wide `charly/pr-validator` gates this PR (the package ref is a merged tag `v2026.247.0912`, never a PR branch — rule 5 pin discipline).
- The R2 cross-repo sequencing dependency (tag must exist before this pin) was satisfied: repo merged + tagged before this PR was created.
- PR body = changelog (tag-on-merge contract).

## Change classification

- **Change class**: config change — pi package wiring in the main pi config (`.pi/settings.json`)
- **Verification gate**: fully tested and validated — final tree ran the full R7 pinning gate (`verify-pins` OK) and the changed runtime path (bare `pi -p` with the git-ref entry) executed live in this worktree: L3 matrix 6/6 PASS.
- **Attribution tier**: fully tested and validated

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*