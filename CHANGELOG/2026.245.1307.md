# 2026.245.1307 — chore: sync submodule gitlinks (eval-omarchy, layer-charly-distros, marketplace)

## Summary of changes

Sync three submodule gitlinks to their current main heads (the org's PR-only bump flow — the umbrella records gitlinks; the owning repos validated their own changes):

- `eval-omarchy` → be8e7bc (PRs #1/#2/#3/#4 landed: PT-EVAL template, #9332 rerun, repo provisioning, lane spike results; tags v2026.245.1124/1133/1146/1216)
- `layer-charly-distros` → 0add419 (PR #5: omarchy-eval skill source sync; tag v2026.245.1157)
- `marketplace` → 0942a29 (PR #273: corpus regen from the layer bump; tag v2026.245.1202)

`distro-omarchy` is intentionally NOT bumped: policy B requires its pin to equal charly's `box/omarchy` gitlink (fb7bf64) until charly re-vendors — that sync is a separate charly-side cutover.

## Change class

**hook/workflow / configuration** — submodule gitlink bumps only; zero repo content changes.

**Closes / relates to:** umbrella-side landing of eval-omarchy, layer-charly-distros and marketplace work

**Breaking change + rollback:** none — pin bumps; revert restores the prior pins.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** `task verify` (the full pinning gate) on the staged tree, plus the pre-commit umbrella gate.

```
$ task verify
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 391 submodules, policy B holds

$ git status --short (after gate)
M  eval-omarchy
M  layer-charly-distros
M  marketplace
# exactly the three intended gitlink bumps; distro-omarchy untouched (policy B)
```

- **Pre-commit gate** (ran on commit): check-policy-b OK + check-harness-parity OK (5 shared harness files identical to charly/).

**Evidence blocks:**

*1. Fresh-rebuild gate output*: task verify lines above.

*2. Each changed code path executing*: N/A — gitlink bumps; content lives in the owning repos (validated there).

*3. Docs-only evidence*: N/A — configuration bump; gate output above.

## Attribution tier

**fully tested and validated** — `task verify` GREEN on the final committed tree (391 submodules, policy B holds), pre-commit gate GREEN, tree clean apart from the three intended pins. (Umbrella change class = the pinning gate itself.)

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** git-workflow loaded (branch-per-change, PR-only, no direct push); policy-B contract read before staging.
- **R1 RCA + zero warnings:** the first verify attempt's failure was root-caused to the distro-omarchy pin vs charly's `box/omarchy` mismatch (policy B) — resolved by NOT bumping distro-omarchy here (the charly-side sync is the owner). Gate output zero warnings.
- **R2 no out-of-scope:** three gitlink bumps only; distro-omarchy deliberately excluded with the reason.
- **R3 no duplication:** the umbrella records pins; owning repos are the content source.
- **R4 no workaround:** policy B respected — no hand-pin of distro-omarchy beyond charly's gitlink.
- **R5 hard cutover + grep:** N/A — pins only.
- **R6 git safety:** status checked before/after; branch from current main; no destructive ops.
- **R7 runtime gate:** task verify (this repo's gate) green.
- **R8 artifact invariants:** N/A.
- **R9 binary == source + deps:** N/A.
- **R10 disposable + coverage:** N/A — no disposable target; the gate is verification.
- **RDD / ADE / SDD:** N/A.
- **Hard cutover:** one atomic commit; the charly-side distro-omarchy re-vendor is named (owned elsewhere).
- **Kernel/plugin boundary law:** N/A.
- **Disposable-only autonomy:** N/A.
- **Clean architecture + Go gates:** N/A — no Go.
- **CHANGELOG:** N/A — tag-on-merge writes it from this PR body.

---

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (fully tested and validated)*