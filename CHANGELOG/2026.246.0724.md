# 2026.246.0724 — chore(pi): load all pi agent plugins from opencharly org forks

## Summary

Fork/mirror all 11 pi agent plugins the umbrella loads into the **opencharly** org and switch `.pi/settings.json` from third-party npm/git sources to org-owned `git:github.com/opencharly/<repo>@<tag>` refs — org supply-chain ownership, reviewability, and resilience against upstream takedowns.

- **8 real GitHub forks** (inherit upstream history + tags): pi-mcp-adapter, pi-subagents, pi-memory, pi-ollama-cloud, pi-web-access, pi-claude-marketplace, pi-goal, pi-simple-team.
- **3 vendored mirrors** (new org repos, because a real fork is impossible/insufficient): pi-fabric (npm 0.70.0 is untagged/untraceable — vendored from the published tarball), rpiv-todo (lives in the rpiv-mono monorepo subdir — vendored as a single-package repo), pi-lsp (no public upstream repo — vendored from the npm tarball).
- `.pi/settings.json`: all 11 entries now `git:github.com/opencharly/...` pinned to merged tags (upstream tags for forks, org-created tags on merged main for mirrors).
- `scripts/sync-pi-forks.sh`: syncs the 8 real forks from upstream (gh repo sync + explicit tag propagation; gh repo sync does not propagate tags). The 3 vendored mirrors are re-vendored manually on version bump.
- `HARNESS-PARITY.md`: corrected the stale pi rows (was "same 6 packages"; umbrella now loads 11 fork-sourced packages, charly keeps its own npm list — diverged by design).
- Global `~/.pi/agent/settings.json` on this machine: removed the 4 overlapping npm entries (pi-ollama-cloud, pi-subagents, rpiv-todo, pi-claude-marketplace) so the project git identities load without duplicates (backup: `~/.pi/agent/settings.json.bak-20260903-090914`).

## How tested

**1. Forks created (8) — `gh repo fork <upstream> --org opencharly --clone=false`:**
```
https://github.com/opencharly/pi-mcp-adapter
https://github.com/opencharly/pi-subagents
https://github.com/opencharly/pi-memory
https://github.com/opencharly/pi-ollama-cloud
https://github.com/opencharly/pi-web-access
https://github.com/opencharly/pi-claude-marketplace
https://github.com/opencharly/pi-goal
https://github.com/opencharly/pi-simple-team
```

**2. All 11 repos public, pinned tags present (`gh api repos/opencharly/<repo>` + `git ls-remote --tags`):**
```
pi-mcp-adapter      fork=true private=false tag v2.31.0 PRESENT
pi-subagents        fork=true private=false tag v0.60.0 PRESENT
pi-memory           fork=true private=false tag v0.4.2  PRESENT
pi-ollama-cloud     fork=true private=false tag v0.9.0  PRESENT
pi-web-access       fork=true private=false tag v0.27.0 PRESENT
pi-fabric           fork=false private=false tag v0.70.0 PRESENT (vendored mirror)
pi-claude-marketplace fork=true private=false tag v0.18.1 PRESENT
pi-goal             fork=true private=false tag v0.1.7  PRESENT
pi-simple-team      fork=true private=false tag v1.0.9  PRESENT
pi-lsp              fork=false private=false tag v0.1.7  PRESENT (vendored mirror)
rpiv-todo           fork=false private=false tag v2.8.0  PRESENT (vendored mirror)
```

**3. Reconcile — `pi update --extensions`:** `Updated packages` (cloned all 11 forks into `.pi/git/github.com/opencharly/`, npm install per package, 0 vulnerabilities).

**4. `pi list` — all 11 project packages resolve from the org forks:**
```
Project packages:
  git:github.com/opencharly/pi-mcp-adapter@v2.31.0
  git:github.com/opencharly/pi-subagents@v0.60.0
  git:github.com/opencharly/rpiv-todo@v2.8.0
  git:github.com/opencharly/pi-memory@v0.4.2
  git:github.com/opencharly/pi-ollama-cloud@v0.9.0
  git:github.com/opencharly/pi-web-access@v0.27.0
  git:github.com/opencharly/pi-fabric@v0.70.0
  git:github.com/opencharly/pi-claude-marketplace@v0.18.1
  git:github.com/opencharly/pi-goal@v0.1.7
  git:github.com/opencharly/pi-simple-team@v1.0.9
  git:github.com/opencharly/pi-lsp@v0.1.7
```

**5. End-to-end headless config test — `pi -p -a --no-session "…"` from the umbrella root (fresh process, loads the NEW settings.json):** all 11 packages reported resolving from `git:github.com/opencharly/...`; 181 skills loaded (mcp-scripting, council-mode, pi-subagents, fabric-exec, pi-goal-writer, charly-* corpus); 13 extension namespaces / 37 tools (mcp, subagent, todo, memory_*, web_search, fabric_exec, create_goal, team_*, lsp_*, charly_status, gh_pr_status, vision_ask); fabric_exec smoke ✅, subagent agent-list ✅ (8 project agents), memory write/read round-trip ✅; no duplicates.

**6. Pre-commit gate (the enforced subset — policy B + harness parity + pi extension parse):**
```
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
pre-commit: OK
```

**7. `task verify` (full pinning gate, on-demand):** run and captured. It fails on the local dev-state tracked as a named thematic batch in **opencharly/opencharly#116** (local dev-state cleanup — charly uncommitted replace directives + 9 submodules on feat branches ahead of gitlinks + eval-omarchy untracked media/):
```
task: [verify] bash scripts/verify-pins.sh
FAIL: charly: dirty working tree
task: Failed to run task "verify": exit status 1
```
The script stops at the first failure. This PR's diff changes **zero gitlinks** (`git diff origin/main..HEAD --stat` = only `.pi/settings.json`, `HARNESS-PARITY.md`, `scripts/sync-pi-forks.sh`), so the pinning gate is provably unaffected by this change. The umbrella has no CI verify gate by design (see `.github/workflows/pr-validator.yml`); the assertions a commit can violate run in pre-commit (passed above). The dev-state cleanup is tracked in #116 and lands when the dev work merges.

## Rulebook compliance

| Rule | Status | How / N/A |
|---|---|---|
| 1. Never edit inside a submodule | HOW | All 3 changed files are umbrella-root files; no submodule touched |
| 2. Git op rule | HOW | All git ops from the umbrella root; submodule checkouts left untouched (active dev branches) |
| 3. No nested go.work | N/A | No Go changes |
| 4. No worktrees inside submodules | N/A | No worktrees |
| 5. Pin discipline | HOW | All 11 pins are merged tags — upstream tags for the 8 forks, org-created tags on merged main for the 3 mirrors; verified via ls-remote |
| 6. Git safety | HOW | No force-push, no hook bypass; pre-commit gate ran and passed |
| 7. Prove the gate | HOW | Pre-commit gate passed; task verify run and documented — failure is the local dev-state batch tracked in #116, and this diff changes zero gitlinks |
| 8. Harness config parity | HOW | HARNESS-PARITY.md updated; check-harness-parity passed in pre-commit |
| R10. Fresh disposable proof | HOW | `pi -p` headless session from the committed tree loaded the new config; all smoke probes passed |

## Change classification

- **Change class:** configuration (pi harness package sources) + tooling script + docs
- **Verification gate:** pr-validator (umbrella has no CI verify gate by design)
- **Attribution tier:** analysed on a live system — changed runtime path (pi package loading) ran live with retained output (`pi list` + `pi -p` smoke test); full local pinning gate blocked by the local dev-state batch tracked in #116 (charly WIP + 9 feat-branch submodules), unrelated to this diff

*Assisted-by: pi ollama-cloud/deepseek-v4-flash:0731 (analysed on a live system)*