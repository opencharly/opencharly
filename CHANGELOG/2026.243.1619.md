# 2026.243.1619 — docs: record the dotgithub submodule in the org map; drop the stale 344 count from the pre-commit comment

## Summary

Corrects two stale claims in the umbrella repo (docs-only):

1. **README org map** — `README.md` said `opencharly/.github` was *"Not a submodule"*, but it has been pinned as the `dotgithub/` submodule (gitlink 6eace9f, `submodule.dotgithub.url = https://github.com/opencharly/.github.git`). The org-map table now lists `dotgithub/` and the old paragraph is replaced with an accurate description of the path alias (pin at `dotgithub` so the umbrella keeps its own `.github/workflows/` for CI dispatchers).
2. **pre-commit hook comment** — hardcoded "the 344 `git ls-remote` default-branch audit", which drifts on every sync (now 389). Replaced with a count-free form.

## How tested

1. **Pre-commit gate on the commit** (hook env simulated — linked worktree):
```
$ GIT_DIR=$(git rev-parse --absolute-git-dir) GIT_INDEX_FILE=$(git rev-parse --git-path index) bash scripts/check-policy-b.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
$ GIT_DIR=$(git rev-parse --absolute-git-dir) GIT_INDEX_FILE=$(git rev-parse --git-path index) bash scripts/check-harness-parity.sh
check-harness-parity: OK — 5 shared harness files identical to charly/
```
2. **Real commit through the hook**: `git commit` (commit 9aa9df0) ran pre-commit policy-B + harness-parity and passed.
3. **Full pinning audit (R10) on the committed tree**:
```
$ cd /tmp/umbrella-readme-fix && task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 389 submodules, policy B holds
```
Post-audit `git status --short` clean; diff is exactly README.md + hooks/pre-commit.

## Rulebook compliance

- R1: stale-claim divergences RCA'd and fixed in-tree (dotgithub has been a submodule since the sync PRs; the 344 count was a snapshot that drifted).
- R3/R10: verified on the final committed tree with the full pinning gate; no duplication.
- R6: PR-only landing via feat/ branch off fresh `origin/main`; no force-push, no direct push.

## Change classification

- Change class: docs-only (README + a comment line in hooks/pre-commit).
- Verification gate: `task verify` full pinning audit + pre-commit gate, both passed live.
- Attribution: `documentation reviewed` — docs-only change class; no runtime path exists to execute.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*