# 2026.265.2229 — feat: register layer-qdrant, plugin-qdrant, pod-qdrant as submodules

## Summary

Registers the three merged Qdrant-integration repos as umbrella submodules, pinned to their default-branch HEADs.

- **`.gitmodules`** — adds `layer-qdrant`, `plugin-qdrant`, `pod-qdrant` (url + `branch = main`).
- **gitlinks** — `layer-qdrant` @ `b641660` (main; tagged `v2026.265.2042`), `plugin-qdrant` @ `29a9645` (main; tagged `v2026.265.2042`), `pod-qdrant` @ `cc69828` (main; tagged `v2026.265.2201`).
- **`README.md`** — the org-map gains one row per repo in its section (`layer-qdrant` → Other layers, `plugin-qdrant` → Plugins, `pod-qdrant` → Pods); the three section counts and the header's "(N today)" bump 138→139, 109→110, 74→75, 403→406.

## Change class

`cross-repo` (umbrella submodule registration). **Closes / relates to:** the Qdrant integration umbrella — the three repos themselves are already merged (`layer-qdrant` #2, `plugin-qdrant` #2, `pod-qdrant` #1). **Breaking change + rollback:** none; rollback = revert this commit (removes the three submodules + rows).

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** the umbrella's own gates — `scripts/check-org-map.sh` (README ↔ `.gitmodules` reconciliation), `scripts/check-policy-b.sh`, and the pre-commit hook subset. This is a pin/view change, not a runtime change, so there is no `charly check` bed for it (and none is claimed).
- **Eval beds run:** `N/A — umbrella submodule registration; no charly runtime path changed`. The runtime proof for the composed stack is the `check-qdrant-pod` R10 bed in `pod-qdrant` (14 phases / 42 assertions / 40 PASS / 2 SKIP / 0 warnings), pasted in that repo's merged PR.
- **Fresh rebuild:** `N/A — no binary`.
- **Did the CHANGED code path execute live?** The CHANGED path is the umbrella's gate scripts — yes:
  - `scripts/check-org-map.sh` (pasted below)
  - `scripts/check-policy-b.sh` → `check-policy-b: OK — 6 distro pins equal charly's gitlinks`
  - the pre-commit hook ran on the commit itself: `pre-commit: OK` (policy-b + harness parity + both self-tests)
- **Concurrency:** `N/A — a pin/view change`.

*1. Fresh-rebuild gate output:*
```
$ bash scripts/check-org-map.sh
section                    claimed  rows  sorted
### Core & contract              5     5     yes
### Distro image families        6     6     yes
### Package repositories        10    10     yes
### Product & tooling            6     6     yes
### Charly candy layers         22    22     yes
### Omarchy layers              13    13     yes
### Omarchy evaluation           2     2     yes
### Check & fixture layers      15    15     yes
### Other layers               139   139     yes
### Plugins                    110   110     yes
### Pods                        75    75     yes
### VMs                          3     3     yes

submodules in .gitmodules : 406
submodules with no row    : none
header '(N today)'        : 406  matches=True

OK: all sections reconcile and are sorted; every submodule has a row

$ bash scripts/check-policy-b.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks

$ git commit -m "…"   # the real commit, hook active
pre-commit: running the umbrella gate…
check-policy-b: OK — 6 distro pins equal charly's gitlinks
check-harness-parity: OK — 5 shared harness files identical to charly/
sync-dispatcher: self-test OK (splice writes, steady-state passes, stale fails)
sync-pin-evidence: self-test OK (per-pin classification both ways; coverage both directions; staged-gitlink reads the index + falls back to HEAD)
pre-commit: OK
```

*2. Each changed code path executing* — the gate output above (check-org-map reconciles the README against `.gitmodules`; policy-b compares the distro pins; the pre-commit hook is the commit gate).

*3. Docs-only evidence* — `N/A — not docs-only (`.gitmodules` gitlinks change)`.

## Attribution tier

`fully tested and validated` — the umbrella's gates (check-org-map, policy-b, pre-commit) all pass on the final committed tree, and the registered gitlinks point at the merged default-branch HEADs of the three repos.


## Harness rulebook compliance — state HOW each is satisfied

- **R0 skills:** `/charly-internals:git-workflow` (umbrella pinning + `task sync`/`verify`), `/charly-internals:strict-policy` loaded.
- **R1 RCA + zero warnings:** the three repos each went through several validator rounds; every BLOCK was root-caused and fixed (schema splice, host-based verb docs, secret store path, bed plan discovery, duplicated bed checks, box ADE plan). Zero warnings on the final gates.
- **R2 no out-of-scope:** the org new-repo bootstrap gap surfaced during landing was root-fixed in `opencharly/.github` #112 (issue #111).
- **R3 no duplication:** the README rows follow the existing per-section format; counts derive from the tables (`check-org-map.sh` asserts it).
- **R4 no workaround:** the gitlinks pin merged default-branch HEADs, never a PR branch; the gate scripts are the umbrella's own.
- **R5 hard cutover + grep:** `N/A — purely additive, no identifier removed or renamed`.
- **R6 git safety:** no force-push; the change is isolated in a linked worktree so no other session's dirty submodules were touched (rule 9); landed via a `feat/` branch + PR.
- **R7 runtime gate:** `N/A — a pin/view change`; the runtime proof is the R10 bed in `pod-qdrant`.
- **R8 artifact invariants:** `N/A — no artifact`.
- **R9 binary == source + deps:** `N/A — no binary`.
- **R10 disposable + coverage:** `N/A — no runtime change`; the umbrella's own gates (org-map + policy-b + pre-commit) are the change-class gate and pass.
- **RDD / ADE / SDD:** `N/A — no candy/schema edit; the three repos carry their own ADE`.
- **Hard cutover:** one commit; no TODOs.
- **Kernel/plugin boundary law:** `N/A — umbrella view`.
- **Disposable-only autonomy:** `N/A — no destroy/rebuild`.
- **Clean architecture + Go gates:** `N/A — no Go`.
- **CHANGELOG:** `N/A — the tag-on-merge workflow writes it from this PR body`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*