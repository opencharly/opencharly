# 2026.254.1644 — chore: sync docs gitlink to the post-#118 corpus-sync main (e6f8148e)

## Summary

Syncs the umbrella's docs gitlink to the post-#118 corpus-sync main: docs `3ec7cac61a8dfb85623ca78c5b66f70d49212987` -> `e6f8148e980fbcff908448760a86169cb582d81d` (docs main HEAD verified before the bump). One entry, `1 file changed, 1 insertion(+), 1 deletion(-)`; `git update-index --cacheinfo` per the repo's own convention (docs follows its default-branch HEAD; docs is not charly-pinned). This PR is one targeted step of the **standing umbrella gitlink-refresh cycle** — the `task sync` / `scripts/sync-gitlinks.sh` flow (with `verify-pins.sh`) that aligns the umbrella's gitlinks to their repos' default-branch HEADs; that same flow produced #191, #192, #193 and this PR, and it owns the remaining gitlink drift as repos advance (the refresh cycle re-runs it; each step is a targeted single-repo bump like this one rather than a 400-module sweep in an unrelated PR).

## How tested

Real gate output from a fully-populated checkout of the pushed tree (branch feat/sync-docs-corpus @ a7a61b11):

```
$ bash scripts/verify-pins.sh        (FIRST run)
FAIL: docs: dirty working tree
$ git -C docs submodule update --init --recursive   (realign docs' nested submodules)
$ bash scripts/verify-pins.sh        (FINAL run)
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 400 submodules, policy B holds
```

## Rulebook compliance

- **R1 — the first gate FAIL was RCA'd and fixed, not parked:** docs' nested `marketplace` submodule had been initialized at its pre-#118 commit (a checkout-ordering artifact of initializing docs at the old gitlink first), which made docs dirty; the realign (`git -C docs submodule update --init --recursive`) put marketplace at the pin docs@e6f8148e records (`bd58a38`), and the gate is green (both the FAIL line and the green output are pasted above, verbatim).
- **R2 — the broader gitlink state is routed to its NAMED owner:** the umbrella gitlink-refresh cycle (`task sync` + `scripts/sync-gitlinks.sh` + `verify-pins.sh`) — the standing flow named in the Summary, which produced the #191/#192/#193 syncs and re-runs as repos advance. The live checkout's other dirty submodules are in-flight worktrees owned by their own sessions' branches (not content of this PR), which is why the gate ran in a clean, fully-populated clone.
- Policy B holds (gate output above); no direct-main push, no force-push; `main` never written.

## Change classification

- **Change class:** submodule gitlink sync (view update).
- **Verification gate:** `verify-pins` (policy B + 400 pins) on a fully-populated checkout — FAIL output and green output both pasted.
- **Attribution tier:** analysed on a live system — the gate ran live with retained output.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*