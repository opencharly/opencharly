# 2026.235.0737 — chore: sync pins after the org-infra rollouts + un-skip sdk in the default-pin loop

## Summary

The org-infra rollouts advanced several default branches; this sync records the assembled state:

- `charly` → `8994ec65` (main HEAD — carries the tag-on-merge CHANGELOG commits on top of the cutover landing)
- `sdk` → `9bd558e` (its own default HEAD — the dispatcher fix + changelog; **sdk now follows the every-other-repo rule**)
- `spec` → `d62d030` (its default HEAD — the dispatcher fix)
- `dotgithub` → `a6838ed` (its default HEAD — the dispatcher + ruleset-script landings)
- `plugins`/`docs` unchanged (mirror charly's gitlinks)

Plus a leftover fix from the sdk de-submodule policy (opencharly/opencharly#10): `scripts/sync-gitlinks.sh` still skipped `sdk` in the default-branch pin loop (the old charly-pinned skip list). sdk now follows the default-branch rule and its pin tracks its own default HEAD.

## How tested

**Sync — executed (pasted):**

```
$ bash scripts/sync-gitlinks.sh
  charly -> 8994ec65
  docs -> 8a8fecb
  plugins -> 2a0c4c2
  sdk -> 9bd558e
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'
```

**Gate — executed on the assembled tree:**

```
$ task verify
verify-pins: OK — 23 submodules, policy B holds
OK: .pi/extensions/umbrella-gates.ts parses (node type-strip pass)
```

**Org ruleset state — executed (the canonical owner script, now on .github main):**

```
$ bash dotgithub/scripts/branch-protection.sh verify | grep -c "verified main ruleset (strict"
23
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | the sdk-skip leftover in sync-gitlinks root-caused to the #10 policy change removing sdk from CHARLY_PINNED without updating the skip list — fixed here |
| R3 | sync-gitlinks.sh remains the only pin mechanism; the skip list now matches the policy |
| R4 | no workarounds — the sync is the policy's own operation |
| R5 | the stale sdk skip removed |
| R6 | status checked before resets; no force-push; feat-branch + PR landing |
| R7 | sync + verify outputs pasted above |
| R10 | N/A — submodule-pointer + script sync; the gate is verify |

## Change classification

- **Change class**: gitlink sync + one-line script fix
- **Verification gate**: `task verify` on the final tree
- **Attribution tier**: analysed on a live system — sync + verify executed live with retained output
- **Landing**: fresh `charly/pr-validator`; tag-on-merge

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*