# 2026.253.0918 — chore(umbrella): sync the gitlinks for the RCA-fix chain

## Summary

Syncs the umbrella's recorded submodule gitlinks for the RCA-fix chain — SEVEN merged refs:

- **charly#578** — the compiled-in plugin-vm pin bump (the keeper-restart fix).
- **spec#126** — WaitForSSH aborts immediately on a permanent ssh failure (the check-live hang regression).
- **plugin-vm#30** — the snapshot-anchored keeper restart (chmod the active disk writable before qemu opens it, restore 0444 after).
- **plugin-omarchy#2** — the omarchy verb uses the shared verdict pipeline (expect_non_zero + the step-level matchers).
- **eval-omarchy#59** — the golden carries the omarchy verb.
- **plugin-deploy-vm#15** — the spec pin (the WaitForSSH fix) + the FleetNode → Deploy rename.
- **plugin-check#41** — the spec pin (the WaitForSSH fix).

Policy B: the pins are the merged refs (the tag-on-merge CHANGELOG commits).

## How tested (pasted)

```
$ ./scripts/sync-gitlinks.sh
SYNC_EXIT=0 - sync-gitlinks: done
$ git show --stat HEAD
 charly           | 2 +-
 eval-omarchy     | 2 +-
 plugin-check     | 2 +-
 plugin-deploy-vm | 2 +-
 plugin-omarchy   | 2 +-
 plugin-vm        | 2 +-
 spec             | 2 +-
 7 files changed, 7 insertions(+), 7 deletions(-)
$ git diff HEAD~1 --submodule=short
 charly:            d3898fc -> 993f105
 eval-omarchy:      1455832 -> b7b53db
 plugin-check:      0465065 -> 44eca80
 plugin-deploy-vm:  1e3826c -> b95e48f
 plugin-omarchy:    93d96a4 -> 394a59b
 plugin-vm:         84702f2 -> c332b82
 spec:              2529662 -> a2b6a65
```

Only the seven merged refs are committed. The sdk + plugin-wl worktree movements (other sessions' landings) are NOT part of this commit — they stay as worktree state.

## Rulebook compliance

- R0: git-workflow + the umbrella AGENTS.md loaded.
- R3: only the seven merged refs are bumped.
- R4: no workarounds — the sync is the canonical path.
- R6: no force-push, no hook bypass, PR-only landing.
- R7: the sync output pasted (the committed refs).
- R9: the pins are merged refs, never PR branches.
- R10: each fix's live proof pasted in its own PR (referenced).

## Change classification

- Change class: chore (submodule pin sync)
- Verification gate: the sync output (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*