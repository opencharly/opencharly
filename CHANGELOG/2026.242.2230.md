# 2026.242.2230 — chore(sync): third wave — docs net-state + parallel defaults

## Summary

The **third gitlink wave** of the mise landing (policy B sweep), recorded from a fresh `origin/main`:

- `docs` → the merged net state of opencharly/docs#103 + #104: the charly CI pin bump to `7db4f9889c` followed by its revert — the **net pin is the pre-migration one** (deploy green); the re-advance of the pin is owned by the **plugin-docs standalone migration** (the consuming-project wiring that makes `charly docs` dispatch to `github.com/opencharly/plugin-docs/candy/plugin-docs` at a pinned tag — the immediate-next cutover of the docs deploy, its own PR; opencharly/docs#103's RCA names the two blocking failures precisely).
- The parallel defaults sweep: `charly` (#497), `layer-charly-distros` (the omarchy base-image skill), `plugin-service` (scope: user + none_failed), `plugin-user` (groups / not_groups), `plugin-wl`, `vm-charly-vm` — every submodule at its default-branch HEAD / charly's gitlinks per policy B.

## How tested

```
$ task sync
exit=0   # docs, charly, layer-charly-distros, plugin-service, plugin-user, plugin-wl, vm-charly-vm
```

```
$ task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks
verify-pins: OK — 349 submodules, policy B holds
verify-exit=0
```

## Rulebook compliance

- **Policy B** — submodule pins at default-branch HEADs / charly's gitlinks; `check-policy-b: OK` pasted.
- **B6/B14b — the named batch:** the docs pin re-advance is owned by the **plugin-docs standalone migration** PR (documented precisely in opencharly/docs#103's RCA — the two live failures: `unexpected argument docs` + the sshd/fleet link-gate); this umbrella PR records the reverted net state only.
- **R7/R10** — pasted `task verify` output on the final staged tree.

## Change classification

- **Change class:** gitlink sync (pointer updates)
- **Verification gate:** `task sync` + `task verify` pasted
- **Attribution tier:** analysed on a live system