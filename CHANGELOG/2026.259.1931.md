# 2026.259.1931 — chore: sync gitlinks — eval-omarchy v2026.259.1834, marketplace v2026.259.1847

chore: sync gitlinks — eval-omarchy v2026.259.1834, marketplace v2026.259.1847

Advances the umbrella's submodule pins to the merged default-branch HEADs of
this session's consumer chain (the plugin-pipeline openai-go SDK cutover):

- eval-omarchy: caf4ee7 -> 579d769 (v2026.259.1834, the merge of
  opencharly/eval-omarchy#79, which pinned plugin-pipeline v2026.259.1825)
- marketplace:   d66f6f4 -> d7dd377 (v2026.259.1847, the merge of
  opencharly/marketplace#303, which advanced the eval-omarchy ref)

Plus the other gitlinks the canonical `task sync` (scripts/sync-gitlinks.sh)
advances to their own default-branch HEADs; 25 pins total, every one equal to
its repo's origin/main (no PR branch is pinned).

The sync also closed two stale automated PRs it supersedes (same disposition as
the repo's own sync PRs; RCA posted on each):

- opencharly/opencharly#206 — its pins (eval-omarchy caf4ee7, marketplace
  d66f6f4) were ALREADY in main via #207, so against current main it carried no
  advance and was `BEHIND`; it predated the three merged advances above.
- opencharly/marketplace#301 — a stale refresh whose only ref change moved
  eval-omarchy BACKWARD (`v2026.258.1957` from main's `v2026.259.1014`) and
  re-added the 7 orphan skills #302 deleted; being `CONFLICTING/DIRTY` and
  matching the refresh workflow's own skip-search, it deadlocked every later
  automated refresh. Superseded by marketplace#303.

How tested — the full pinning gate (R7), pasted:

```
$ bash scripts/check-policy-b.sh
check-policy-b: OK — 6 distro pins equal charly's gitlinks

$ bash scripts/verify-pins.sh
verify-pins: OK — 399 submodules, policy B holds
```

Each staged pin was checked equal to its repo's `origin/main` (rule 5 — only
merged refs are pinned):

```
$ for p in $(git diff --cached --name-only); do [ "$(git ls-files -s $p|awk '{print $2}')" = "$(git -C $p rev-parse origin/main)" ] && echo "OK $p"; done
OK charly ... OK eval-omarchy ... OK marketplace ... OK spec
(25/25 OK)
```

Rulebook compliance: R1 (both stale PRs root-caused before closing); R4/R5 (the
pins come from `task sync`, not a hand-pin; nothing left behind); R6 (no
force-push; `git status` checked before every destructive step; the pre-existing
submodule scratch edits and the plugin-check `keep_on_failure` WIP were
PRESERVED — patches under /tmp/opencode/submodule-dirt-backup/ and a recoverable
`git stash` in plugin-check — never discarded); R7 (the full gate is pasted
above, run on this tree).

Change classification: pinning/gitlinks (policy B). Verification gate: `task
verify` (scripts/verify-pins.sh + scripts/check-policy-b.sh). Attribution:
fully tested and validated.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*