# 2026.243.0857 — feat(pi): make subagent/fabric delegation discipline explicit in the system prompt

## Summary

Make subagent/fabric delegation discipline EXPLICIT in the pi system prompt.

`.pi/APPEND_SYSTEM.md` gains a `# Delegation discipline` section next to the existing tool-usage rules: heavy/long-running work (bed runs, full builds, regeneration, log archaeology, repo-wide greps, build loops) is ALWAYS delegated to a child and NEVER run in the parent's foreground bash; bed runs use executor children and return the step matrix; fabric_exec is the batch surface for pi.* calls; a child's failure triggers R1 in the parent; verify the child's resolved tool set before delegating; prefer workflowScript (runs.all/lanes/host) for structured multi-child work and keep one writer per cwd/worktree.

Motivation: an active audit session ran a 10-minute checkbed in the parent's foreground instead of delegating to the executor agents the org's own skills define (check-bed-runner etc.), against the harness's own stated discipline. This change codifies the rule so future sessions treat delegation as mandatory for that class of work.

## How tested

- Edit applied to `.pi/APPEND_SYSTEM.md`; full file read back (20 to 42 lines); the new section is present verbatim after the existing tool-usage bullets (see diff).
- No code paths touched: this is system-prompt text only.
- Fresh-session probe (the real delivery check for an appended system prompt) will confirm the rules are present on the next session start; the text is read from this file at session load.

## Rulebook compliance

- R4 (no workarounds): the discipline text operationalizes existing pi-mechanics; nothing patched around.
- Change is confined to the harness-config layer the umbrella owns (`.pi/` mirror policy per AGENTS.md rule 8 — no harness-parity files touched, no fork).
- No submodule edits; no gitlink changes; `task verify` unaffected (config-only).

## Change classification

- Change class: Documentation-only (system-prompt/config text).
- Verification gate: documentation reviewed (file diff + read-back; no runtime path executed).