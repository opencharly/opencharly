# 2026.240.1124 — chore: sync gitlinks; track workspace + plan; ignore pi cache

## Summary of changes

Catch the umbrella up with upstream main: bump all 344 submodule pins to policy B, and clean up the working tree so the sync can land.

- **`chore: sync gitlinks` (a8a4a20)** — ran `task sync` (`scripts/sync-gitlinks.sh`), the canonical policy-B sync: `charly` → its default-branch HEAD (`a2cc84b8`, was `2be08b5c`), `distro-*` → exactly charly's own `box/*` gitlinks, every other submodule → its own default-branch HEAD. **325 of 344 submodules moved** (19 were already at their upstream HEADs).
- **`chore: track the workspace file and cutover plan; ignore the pi marketplace cache` (17ca856)** — add `opencharly.code-workspace` (VS Code workspace) and `plan/candy_to_repo.md` (candy de-submodule cutover landing status) to the repo; gitignore `.pi/git/` (the pi marketplace cache cloned on startup, sibling of the already-ignored `.pi/npm/`).
- **Stray-checkout cleanup (not in the diff, but required for a clean tree):** 186 submodules carried an untracked, unregistered `charly/` checkout (the "stray charly submodule" defect — gitdir reflog: `clone: from https://github.com/opencharly/charly.git` on 2026-08-23 10:59, checked out at a stale Aug-24 charly commit). Only plugin-gpu's repo had fixed it (its commit c2f2474). These made every affected submodule dirty, failing `verify` (verify-pins.sh: "dirty working tree"). Removed the 186 stray working-tree dirs + their orphaned gitdirs; verified each was untracked and unregistered before removal. The top-level `charly` submodule's `charly/` dir is legitimately tracked (the Go package) and was skipped.

## Change class

`candy/box-config` — submodule gitlink sync + repo hygiene (`.gitignore`, two added files). No Go, no runtime code path, no schema.

**Closes / relates to:** none

**Breaking change + rollback:** none — gitlink bumps are additive (each pin moves to a merged default-branch HEAD; no PR branch is ever pinned). Rollback is a revert of the sync commit.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** `task verify` — the umbrella's full pinning gate (`scripts/verify-pins.sh`: every pin vs live remote default-branch HEAD + policy B + the 344-remote branch audit; plus the gates-syntax check). This is the umbrella's R10 gate for a gitlink sync — there is no `charly check` bed for a superproject pin bump.
- **Eval beds run:** N/A — no `charly check run` bed applies to a superproject gitlink sync (no image, no deploy, no code path).
- **Fresh rebuild:** N/A — no binary involved; the changed "code path" is the pinning itself, which the gate re-derives live from every submodule's remote.
- **Did the CHANGED code path execute live?** yes — `scripts/sync-gitlinks.sh` ran live (fetched + pinned all 344 submodules), and `scripts/verify-pins.sh` re-derived every pin against live remotes (344 `git ls-remote` calls) on the final committed tree, passing with zero warnings.
- **Concurrency:** N/A — no shared-state change (no loader/discover walk, deploy ledger, podman store, or build lock).

**Evidence — put each item in the block whose label names it.**

*1. Fresh-rebuild gate output* (`charly version` + the gate run, at zero warnings):
```
N/A — no binary involved in a gitlink sync. The gate is `task verify` (below), which re-derives every pin live.
```

*2. Each changed code path executing* (the lines proving the changed branch ran, not that it built):
```
$ task sync
  charly -> a2cc84b8
  ... (325 submodules pinned)
sync-gitlinks: done — review with 'git status' and 'git diff --submodule'

$ git diff --cached --name-only | wc -l
325

# policy B spot-check on the final tree (distro-* == charly's box/* gitlinks):
distro-arch: umbrella=98941f3d charly=98941f3d match=YES
distro-cachyos: umbrella=7a9a9b4b charly=7a9a9b4b match=YES
distro-debian: umbrella=6cf14eb0 charly=6cf14eb0 match=YES
distro-fedora: umbrella=46f7cf7e charly=46f7cf7e match=YES
distro-ubuntu: umbrella=a371170b charly=a371170b match=YES
charly: umbrella=a2cc84b8 origin/main=a2cc84b8 match=YES
```

*3. Docs-only evidence* (the R5 grep self-test result + the cross-reference/markdown review):
```
N/A — not a docs-only change.
```

## Attribution tier

`fully tested and validated` — `task verify` passed on the final committed tree (pasted below), and the changed pins were re-derived live by the gate against every submodule's remote.

```
$ task verify
task: [verify] bash scripts/verify-pins.sh
check-policy-b: OK — 5 distro pins equal charly's gitlinks
verify-pins: OK — 344 submodules, policy B holds
task: [verify] node --disable-warning=ExperimentalWarning scripts/check-umbrella-gates-syntax.mjs
OK: /home/atrawog/Sync/Atrapub/coder/pi/opencharly/.pi/extensions/umbrella-gates.ts parses (node type-strip pass)
$ echo $?
0

# zero warnings — the only "warn" match in the log is the --disable-warning flag itself.
# final tree clean:
$ git status --short
(empty)
```

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** loaded `/charly-internals:git-workflow` (sync-to-upstream / gitlink policy / task sync triggers) before the first tool call; followed B4 sync-to-upstream + the PR-only landing discipline.
- **R1 RCA + zero warnings:** two anomalies RCA'd before remediation: (1) the dirty `plugin-gpu` submodule (all 15 files staged for deletion — fresh-clone reflog, gitlink correct at c2f2474 = origin/main HEAD; restored, non-destructive); (2) the 186 stray `charly/` checkouts (gitdir reflog proves a 2026-08-23 10:59 clone-into-every-submodule script; unregistered in every .gitmodules; stale at an Aug-24 charly commit; removed after verifying untracked + unregistered). Gate output has ZERO warnings.
- **R2 no out-of-scope:** the stray-checkout cleanup and the untracked-file handling are in-scope — both are required for the sync PR to land (a dirty submodule fails `verify`, and the sync workflow's `git add -A` would sweep untracked files). Nothing parked as "pre-existing/follow-up".
- **R3 no duplication:** used the canonical `scripts/sync-gitlinks.sh` + `scripts/lib/policy-b.sh` (the single policy-B definition); did not re-implement pinning policy.
- **R4 no workaround:** no sleeps/retries/hand-pins; the sync is the canonical `task sync` path, and the stray-checkout removal is a real fix (the same defect plugin-gpu's repo fixed in c2f2474), not a bypass.
- **R5 hard cutover + grep:** N/A — purely additive (gitlink bumps + 2 new files + 1 .gitignore line); no identifier removed or renamed. False-claim sweep: the PR body claims only what the pasted outputs show.
- **R6 git safety:** `git status` + `git stash list` checked before the destructive stray-checkout removal; each removed dir verified untracked + unregistered first; the top-level `charly` submodule's tracked `charly/` dir was explicitly skipped.
- **R7 runtime gate:** the umbrella's runtime gate for a gitlink sync is `task verify` — ran on the final committed tree, exit 0, output pasted above.
- **R8 artifact invariants:** N/A — no generation change (no Containerfile/label emission).
- **R9 binary == source + deps:** N/A — no binary involved.
- **R10 disposable + coverage:** N/A for beds — no image/deploy/code path changes. The change ships the proof that would fail without it: `task verify` re-derives every pin live and would FAIL if any pin were stale or policy-B-violating (it passed).
- **RDD / ADE / SDD:** N/A — no candy/box/schema edits.
- **Hard cutover:** one atomic commit per concern (sync; hygiene), both on the feat/ branch, squashed at merge.
- **Kernel/plugin boundary law:** N/A — no core/sdk change.
- **Disposable-only autonomy:** N/A — no destroy/rebuild.
- **Clean architecture + Go gates:** N/A — no Go code changed.
- **CHANGELOG:** N/A — the umbrella's `tag-on-merge` workflow writes `CHANGELOG/<CalVer>.md` from the merged PR body at merge time (last entry authored by `charly-auto-merge[bot]`); no CHANGELOG file is staged in the PR.

---

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*